# 5-에이전트 감사 (2026-08-02) — 발견·조치 대장


## ✅ 0053 + 클라 후속 완료 (2026-08-03, 커밋 71cf3c6·ec4724b — 하네스 202/202)
- 0053 서버(71cf3c6, Opus 저자+적대리뷰 3기+테스트): §1 결제 method_consent 박제 · §2 save_run_trace append 병합 · §3a run_photo_allowed(당사자 게이트) · §4 자기 rejected/withdrawn 카드 · §5 session_detail/roster people host/full 게이트(리뷰 P1) · photo 오라클 차단(리뷰 P2). §3b 사진 비공개 버킷만 다음 슬라이스.
- 클라 후속(ec4724b): kstParts KST 고정 · 챗 seq 가드 · AckStack 단일 스토어 · 반환 대리확인(custody_override) 탈출구.
- **감사 발원 항목 전량 소진** (P0 5·P1 17·P2 다수 + 서버 0052·0053). 남은 것 = §3b 사진 비공개 버킷(대공사, 별도) + 디자인(클럽 홈 재도색·안무 4종 — 기기 리뷰).


Opus 에이전트 5기(계약 정합성 / React 정확성 / 상태 머신 / 프라이버시·보안 / 엣지 케이스)가 클럽 위탁 코드 전체를
마이그레이션 정본과 대조 감사. 상태: ✅=이번 커밋에서 수리 · 🔜0052=서버 마이그레이션 필요(하네스 게이트로 진행) · 📋=클라 후속 · ℹ️=기록만.

## P0 (전부 ✅)
- ✅ 콘솔 재검토 버튼 2개가 session_approve_dog 호출 → 항상 not_pending. 정본 session_review_dog 래퍼 신설(reviewDelegation)·교체. 미수리 시 재검토 개는 배정 영구 불가→T-10 자동 전액 환불.
- ✅ 콘솔 종료 차단 사전판정이 미인계 개(custody_phase 기본값 'with_custodian')까지 세어 「세션 종료」 영구 비활성. 서버 술어(_club_dogs_unresolved)와 동일하게 교정.
- ✅ 런 화면 재진입 시 trace/km 0 리셋 → 서버 트레이스 덮어쓰기 유실 + 정산 km 과소. 마운트 시 runs.trace 시드 + 동일 게이트 km 재계산.
- ✅ Alert.prompt(iOS 전용) 4곳 = 안드로이드 죽은 버튼 (이의·거절 사유·케이스 접수·챗 신고). 공용 사유 입력 시트로 교체.

## P1 (✅ 클라 수리)
- ✅ 러닝 종료 duration을 Math.max(60,·)로 지어냄 + started_at 실패 시 60초 박제 → 실측 그대로, 실패 시 종료 차단+재시도.
- ✅ 늦게 인계된 아이가 그룹 누적 km·최초 경과로 정산되던 것 → 마리별 started_at 기준 km 절단·경과 계산 (dog_run_segments 의미론 준수).
- ✅ startTracking 거부 시 gpsOn=null 영구 고착(정산 불가) → catch로 false 수렴, null(준비 중)/false(권한) 문구 분리.
- ✅ 트레이스 저장 실패 침묵 + 나쁜 픽스 1쌍이 이후 전 배치 영구 거부 → 전송 전 8m/s 게이트(서버 등장방형 근사와 동일)로 세척 + saveLag 배너 + 언마운트 최종 저장. km 합산에도 8m/s 게이트.
- ✅ load() 실패가 board=null/access='none' 리셋 → 카드·버튼 전멸/오탐. 이전 상태 유지로.
- ✅ ack 배너: 폴-낙관제거 경합 + busyIds 미해제로 부활 배너 확인 버튼 영구 사망 → busyIds 필터+성공 후 재동기화.
- ✅ 참여 취소가 위탁 행(동의서·결제 기록 cascade)까지 삭제 — 진행 위탁 있으면 문에서 차단+설명. (서버 custody 가드는 🔜0052)
- ✅ owner_only 세션의 러너 확약 죽은 버튼 → format 게이팅 + format_closed/session_full 매핑.
- ✅ startClubSeries가 p_route/p_format 미전달 → 자동 생성 세션 전부 owner_only·무코스. 전파 + 실패 삼킴 해소.
- ✅ 이의: safety 경로 부재(안전 우려 문이 objectionUsed로 소멸) → preference(T-20·1회 사전 게이팅)/safety(인계 전 상시) 분리. 인계 후 죽은 링크 제거.
- ✅ 러닝 중 SOS(club_sos)가 p_dog null → 지급 보류 미작동+보호자 케이스 열람 불가 → incidentOpen(S1, dog+booking+location)으로 교체, 다견 선택.
- ✅ 케이스 대상견 myDogs[0] 자동 지정 → 다견 선택 시트.
- ✅ 진료 한도 0 입력이 조용히 20만 박제 → 명시 입력 보존, 빈 값만 서버 기본.
- ✅ 결제 시트: dog_slot_clash/not_payable/session_closed/route_required 매핑 추가, 죽은 hold_expired 제거.
- ✅ 호스트 전화번호가 HOST 태그에 가려 미표시(로그만 남음) → 태그+번호 칩 병행, tel: 링크.
- ✅ limited(거절·철회 포함)에게 전체 사람 로스터 렌더 → none/limited는 인원수만. 로스터 프리페치도 실사용 시점(참가자 탭/러너)으로 축소 — "보이면 기록" 고지와 실제 로그 시점 정합.
- ✅ done 결과 화면이 배지·심각도·이슈 드롭(환불 진행·정산 보류·외부 보호 증발) → 카드 문법 통일.
- ✅ feature_disabled(전 액션 공통) 원문 노출 → clubRpc 공통 번역.
- ✅ 챗이 '가장 오래된 300건' 고정 → 최신 300건.

## P2 (✅ 수리)
- ✅ 재검토 개가 배정 섹션에 중복 노출 + review_pending 등 미매핑 → 필터+매핑. 만료 제안 '00:00 남음' → '소멸·재제안'. 철회/승인/거절/체크인/참여/제안 응답 등 미매핑 코드 일괄 한국어화.
- ✅ 인시던트 조회 실패가 차단 배너를 지워 종료 버튼 오활성 → 이전 값 유지. 케이스 화면 영구 로딩 → 오류+재시도.
- ✅ 체크인 창이 렌더 시점 고정 → 참여 대기 중 30초 틱. 다견 RSVP dogs[0] 고정 → 선택. 시리즈 실패 삼킴 → 분기 알림.
- ✅ 런 사진 부분 실패 후 재시도 중복 → 실패 아이 안내. 챗 사진 미리보기 빈따옴표 → '사진'. 신고 무피드백 → 접수 확인. HOLDING 무료 취소 문 부재 → 추가.

## ✅ 0052 완료 (커밋 9623908 — 하네스 196/196 + UPGRADE OK, Opus 저자+적대 리뷰 2기+테스트)
1·2·3·4·5(+세션취소환불)·6·7·8·12 완료. 리뷰 추가 발견도 반영: 인시던트 NULL 우회 P0 차단 · 보드 등급별 페이로드(limited 자기 개만·none 최소·미확약 러너 me 유지) · openIncidentId 세션 필터 · no_show host_channel 완화. 클라 소비 완료(peopleCount·딥링크·스토리지 삭제·serviceState 가드).
남은 서버 후속(다음 슬라이스): 9(p_method_consent 결제 동의 박제 — 오버로드 drop 필요) · 10(save_run_trace append 의미론) · 11(photo_consent 강제 + 챗/런 사진 비공개 버킷) · board rejected/withdrawn 노출 · _club_shell_access limited 생존 조건(거절·철회 후 영구 열람 차단).

## 🔜 (구) 0052 후보 원목록 (참고)

### (구) 0052 후보 (서버 — 하네스 게이트로 다음 슬라이스)
1. club_delegation_board 당사자 게이트 부재 (세션 id만으로 전원 명단·결제·커스터디 열람) — not_party 게이트.
2. club_session_detail people 무게이트 (실명+강아지 이름) — 접근 없으면 인원수만.
3. club_session_roster v_people을 host/full로 제한 + _club_shell_access limited에서 rejected/withdrawn 강등 검토.
4. session_cancel_rsvp가 custody 구분 없이 session_dogs 삭제 — and custody='owner_handled' 가드.
5. 챗 host_channel 수신자에 세션 자격 검증 (insert/select 정책).
6. club_critical_titles 누락: '위탁 신청 거절'·'위탁 취소 — 전액 환불'·'위탁 미진행 — 전액 환불' (거절·환불이 ack 배너에 안 뜸).
7. club_dog_ui_state: partial(조기 종료)을 '완료'로 평탄화 — 별도 낱말/배지. 보드 dogs에 openIncidentId(케이스 문) 추가.
8. 보드 필터가 rejected/withdrawn 행 제외 → REFUSED 도달 불가(거절이 흔적 없이 증발) — 노출 검토.
9. session_pay_delegation에 배정 방식 동의 박제 (p_method_consent) — 현재 UI 체크만.
10. club_save_run_trace append 의미론 검토 (덮어쓰기 유실 계열의 근본 해법).
11. 사진 프라이버시: photo_consent를 읽는 곳이 0곳 — 피드 공유·다견 복제 업로드 시 동의 필터 + 챗/런 사진 비공개 버킷 전환 검토. 챗 삭제 시 스토리지 원본 잔존.
12. club_incident_resolve를 케이스 화면에서 호스트에게도 (detail에 isHost 필드).

## 📋 클라 후속
- transfer_pending UI (이양 수락/취소 문 — transferInitiate/Accept/Cancel 래퍼 미사용) · custody_override 문(반환 한쪽 미확인 탈출구, 콘솔).
- kstParts가 기기 로컬 타임존 사용 (KST 고정 아님) — 전 앱 공용이라 별도 검증 후 교체.
- 챗 reload 경합(seq 가드)·AckStack 다중 인스턴스 폴 통합 · 러너 칩 만석 판정에 서버 부하식 값 요청.
- pull-to-refresh 400ms 가짜 스피너 3곳 (load가 프라미스 미반환) · 홈 그리팅 애니 언마운트 정지.

ℹ️ 오탐 방지 기록·계약 정합 확인 목록은 에이전트 원문 리포트 참조 (세션 로그).
