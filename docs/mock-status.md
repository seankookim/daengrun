# Mock vs Real 상태 대장

> 앱에서 진짜로 동작하는 것 / 목업(가짜)인 것의 목록. 백엔드(Supabase) 붙일 때마다 업데이트.
> 마지막 업데이트: 2026-07-22 (v0.14)

## ✅ 진짜로 동작 (클라이언트 로직)

- 화면 이동 전체 (owner/runner 전 플로우)
- 요청 화면 가격 계산 (거리·옵션 실시간 합산)
- 시간 슬롯 선택 → 선택값 반영
- 코스 선택 → 선택 상태
- 러너 수익 비례 정산 계산 (실제 km 기준)
- 러닝 시뮬레이션 (타이머 기반 거리·페이스·진행률)
- 내 일정 필터링
- 다크/라이트 모드 전환 (홈·기록 화면 — 전 화면 확대 예정)
- 채팅 입력·전송 (로컬 상태만, 상대 응답 없음)
- 인계 확인 단계 진행 (양측 화면 존재 · 상대방 확인은 타이머로 가짜 처리)
- 러너 종료 사유별 정산 계산 (컨디션/보호자 요청+잔여50%/개인 사유)
- 보호자 종료 시트 (사유 선택 필수 · 최소 요금 고지 · 확인 경고)

## ⛔ 목업 (백엔드 필요)

| 항목 | 현재 상태 | 실제 구현 시 필요한 것 |
|---|---|---|
| 계정/로그인 | 없음 (역할 선택만) | Kakao OAuth + Supabase Auth |
| 푸시 알림 전체 | Alert 팝업으로 흉내 | Expo Notifications + 서버 트리거 |
| 수락 알림 (러너→보호자) | Alert "전송되었어요" | 예약 상태 머신 + 푸시 |
| 알림 센터 데이터 | 하드코딩 8건, 탭 필터 장식 | notifications 테이블 + 읽음 처리 |
| 채팅 상대방 | 응답 없음 (시드 대화만) | Supabase Realtime |
| 지도 전체 | View로 그린 가짜 지도 | react-native-maps + 실 GPS |
| GPS 트래킹 | 타이머 시뮬레이션 | expo-location (러너 기기) |
| 바디캠 | 스타일된 박스 | 하드웨어 + 스트리밍 인프라 |
| 결제 | 화면만 | 카카오페이/토스페이먼츠 + 슬롯 홀드 |
| 슬롯 홀드 | 2.2초 모달 데모 | 서버 원자적 홀드 (calendar.md) |
| 러너 가용성/슬롯 공급 수 | 하드코딩 | 가용성 엔진 (calendar.md) |
| 매칭 점수/AI 추천 | 하드코딩 98% | 실제 매칭 알고리즘 |
| 예약 데이터 | store.ts 4건 하드코딩 | bookings 테이블 |
| 취소/환불 | Alert | 결제 취소 API + 정책 엔진 |
| 반복 예약 | "준비 중" 칩 | 시리즈 데이터 모델 |
| 커뮤니티 피드 | 하드코딩 4포스트, 좋아요 정적 | posts 테이블 + 미디어 업로드 |
| 샵 구매 | Alert | 커머스 백엔드 (또는 외부몰 연동) |
| 마이 카드 획득 | 6장 하드코딩 | 러닝 완료 트리거 + 조건 엔진 |
| 체력 나이/주간 목표 | 하드코딩 1.8살/15km | 수의 검증 공식 + 실데이터 |
| 날씨/미세먼지 | 하드코딩 24° | 기상청/에어코리아 API |
| 사진 전송/갤러리 | Alert | 스토리지 + 권한 |
| 안심 통화 | Alert | 번호 마스킹 (Twilio류) |
| 러너 인증(7단계 대응) | 배지만 존재 | 신원확인 절차 + KYC |
| 프로필 사진 전체 | 모노그램 원 | 이미지 업로드 |
| 테마 유지 | 앱 재시작 시 초기화 | AsyncStorage |
| 보호자 종료 → 러너 강제 알림 | Alert 안내문만 | 푸시(critical alert급) + 러너 화면 강제 배너 |
| 위젯 '곧 시작' 상태 전환 | demoImminent 플래그 하드코딩 | 예약 시각 기준 실시간 계산 (30분 전) |
| 러너 이동/도착 감지 | 타이머 시뮬레이션 | 러너 GPS + 지오펜스 |
| 사진/휴식 요청 (보호자→러너) | Alert | 푸시 + 러너 화면 표시 |
| 보급/픽 드랍 판정·확률 | 하드코딩 (215회=드랍) | drops 테이블 + 서버 확률 롤 + 회계 |
| 기어 수령/배송 | Alert | gear_claims + 배송 연동 |
| 매칭 부스트 적용 | UI만 | 매칭 랭킹 가중치 |

## ✅ 실화(實化) 완료

- **안심 코스 카탈로그** — request 화면이 Supabase `routes`에서 로드 (실패 시 목업 폴백, "실시간 코스 정보" 표시). 첫 라이브 데이터.
- **인증** — 이메일 OTP 로그인 실동작, 세션 유지, profiles upsert. (카카오: Expo Go 리다이렉트 이슈 — dev build에서 해결 예정)
- **예약 생성 파이프라인** — 결제하기 → ensureDog → create-booking-hold (서버 가격·원자 홀드) → payment_ok → matching 상태. 홀드 모달에 "서버 홀드 확보" 표시. 실패 시 오프라인 데모 모드 폴백.
- **내 일정 실예약 표시** — DB 예약이 LIVE 배지로 목업 위에 병합. 매칭 전 예약은 관리 시트 게이트.
- **지명 매칭 (v0.18)** — 매칭 화면에 실러너 목록(● LIVE), 지명 요청 → runner_pending → 러너 인박스 ★ 지명 요청 표시. 목업 러너 카드는 데모 표기.
- **인계 동기화 정직화** — 보호자 화면의 '러너 도착'이 서버 상태(runner_enroute) 기반. 러너 미트업 진입 시 enroute 보고. 가짜 타이머는 데모 경로에만.
- **알림 읽기** — 알림 센터가 notifications 테이블 표시(● LIVE 섹션) + 모두 읽음 실동작. 배달(푸시/실시간)은 다음 세션.
- **러너 홈 실요청 배너** — 인박스 요약 스트립 (데모 목록은 표기).
- ⚠ 필요: `npx supabase functions deploy transition-booking` (request_runner 액션 추가됨)
- **러너 루프 전체 (v0.17)** — 역할 선택 시 runners 행+가용시간 생성(테스트용 즉시 인증), 요청 인박스가 실 matching 예약 표시, 수락→confirmed, 양측 인계 confirm_handoff+상태 폴링→picked_up, 러닝 시작→active+runs 행, 종료→settle-run(사유별 실지급·원장·드랍 롤). **한 예약이 상태 머신 전 구간을 실주행.** 필요: `supabase db push` (0004 — 러너 인박스 가시성 정책).

## 백엔드 진행 상황

- ✅ 스키마 v1 (`supabase/migrations/0001_init.sql`) — 28 테이블, 상태 머신 트리거, pglast 구문 검증 완료
- ✅ RLS v1 (`0002_rls.sql`) — 역할별 접근, platform_only 리뷰, 돈 테이블 서버 전용
- ✅ 시드 (`seed.sql`) + 셋업 가이드 (`docs/backend.md`)
- ✅ Sean: 프로젝트 생성 + db push + routes 시드 완료 (2026-07-22)
- ✅ Phase 2 코드: 가용성 엔진 (`0003` — is_slot_available/count_available_runners/purge_expired_holds), Edge Functions 4종 (create-booking-hold, transition-booking, settle-run, open-drop)
- ⏳ Sean: `npx supabase db push` (0003) + `npx supabase functions deploy` + Kakao provider
- ✅ Phase 3a: 로그인 (이메일 OTP 실동작 + 카카오 스텁), 세션 유지 (AsyncStorage), 라우트 가드, 역할 선택 시 profiles upsert, 로그아웃
- ⏳ Sean: `cd app && npx expo install @react-native-async-storage/async-storage` 후 재시작 (버전은 package.json에 지정됨 — `npm install`로도 충분)
- ⏳ Phase 3b: 카카오 OAuth (provider 설정 후), bookings 화면 → 상태 머신 연결, Realtime (chat/status), 매칭 자동배정, 실좌표 트레이스

## 2026-07-23 세션: 인계 side 버그 + E2E 하네스

- ✅ **confirm_handoff side 버그 수정** — 한 계정이 양측(솔로 테스트)이면 `isOwner` 우선 검사 때문에 양쪽 확인이 모두 owner 타임스탬프에 기록 → 영원히 picked_up 불가. 이제 클라이언트가 `meta.side`('owner'|'runner') 선언, 서버가 자격 검증 + 확인 후 재조회(동시 확인 레이스 방지). **⏳ Sean: `npx supabase functions deploy transition-booking` 필수** (request_runner 배포도 아직이면 함께 해결됨)
- ✅ 보호자 미트업 재진입 시 이미 확인했으면 'waiting' 복원 (버튼 재노출 방지)
- ✅ **E2E 하네스** `scripts/e2e.mjs` — 전체 루프(hold→결제→RLS 노출→수락→enroute→양측 인계→start→settle→원장 검증)를 자동 실행. `--solo`(한 계정 양측 = Sean 수동 테스트 재현), `--keep`(데이터 유지). **⏳ Sean: 루트 `.env`에 `SUPABASE_SERVICE_ROLE_KEY=` 추가 후 `node scripts/e2e.mjs`** (Dashboard → Settings → API). 샌드박스에서 supabase.co 직접 접근이 차단되어 잔심부름은 이 스크립트 한 줄로 대체
- ✅ 보안: `app/.env`를 git 추적에서 제거(+.gitignore) — 서비스 키 커밋 사고 예방
- ✅ 보호자 탭 개편: '기록' 탭 → **'내 일정'** 탭 (예약 관리가 접근 빈도상 탭 자격; 기록은 홈 히어로·스탯에서 진입 유지)

## 2026-07-23 세션 2: 데모 전락 사고 근절

원인 하나가 버그 셋을 만들었다: 트랜잭션 화면들이 인메모리 booking id(draft/runnerJob)에 의존 →
리로드 등으로 유실되면 **조용히 데모 모드로 전락** → 가짜 자동도착·가짜 상대확인·정산 누락(위젯 ● LIVE 좀비).

- ✅ 보호자/러너 미트업: 데모 타이머 전면 삭제. id 없으면 서버에서 진행 중 예약 복원(fetchCurrentOwnerBookingId/RunnerJobId), 없으면 안내 후 닫힘 — 이 화면들은 이제 실예약 전용
- ✅ run.tsx: **자동 완주가 서버 정산을 건너뛰던 버그 수정** (settle 없이 done 직행 → 예약 영원히 active). 이제 자동/수동 모두 settle-run 경유 + 중복 정산 가드 + id 복원
- ✅ 내 일정: 진행 중(● LIVE) 카드에 라이트 코랄 '실시간 보기' 버튼 (Sean 요청)
- ✅ 홈 위젯 실시간 보기 버튼이 draft.bookingId 설정
- ✅ e2e `--directed`: 지명(request_runner) 경로 서버 테스트 추가
- 남은 목업(다음): 미트업 픽업 좌표/강아지 카드 표기(runRequests[0] 목업), owner/live 지도 데모, run.tsx 거리 가속 타이머(실 GPS 전까지)

## 2026-07-23 세션 3: 인계 타임스탬프 위생 + 상태별 시트

- ✅ **runner_accept/decline이 인계 타임스탬프 초기화** — 이전 시도의 잔재 stamp가 남으면 한쪽 확인만으로 즉시 picked_up ("러너는 대기 중인데 보호자 화면은 이미 LIVE" 사고). **⏳ Sean: `npx supabase functions deploy transition-booking` 재배포 필요**
- ✅ 양쪽 미트업 화면이 **양측 확인 상태를 각각 서버 진실로 표시** ('내 인계 확인' / '상대 인계 확인' 별도 스텝) — 누가 누굴 기다리는지 추측 불가능하게. 러너 화면도 전 단계 폴링으로 전환
- ✅ 내 일정 시트 상태별 액션: 진행 중 → 실시간 보기만(변경·취소 불가 안내 + SOS 안내), 완료 → 기록 보기만, 시작 전 → 기존 변경·취소
- ✅ e2e: 수락 시 타임스탬프 초기화 검증 추가

## 2026-07-23 세션 4: picked_up ≠ active (라이브 조기 점화 수정)

- ✅ BookingStatus에 `handoff` 추가 — picked_up(인계 완료·시작 대기)이 active(러닝 중)로 뭉개져 보호자 라이브 UI가 조기 점화되던 문제. 위젯: '인계 완료 · 러닝 시작 대기 중' 버튼(미트업으로), 코랄 실시간 보기는 **start_run 이후에만**. 일정 카드/시트도 동일 구분
- ✅ 보호자 미트업: picked_up이면 '러너가 곧 러닝을 시작해요'에서 대기, **active가 되어야만** 라이브로 자동 전환 (1.2s 연출 타이머 제거 — 서버 상태만 따름)
- ✅ `scripts/wipe-test-data.mjs --yes` — 예약·러닝·정산·알림·채팅 전체 초기화(계정·강아지·러너 유지). 테스트 전 백지화가 한 줄로
- 참고: 이전 수정 검증이 안 됐던 이유 = 기존 예약이 수정 배포 이전의 stamps를 이미 들고 있었음. wipe 후 새 예약으로 재검증 필요

## 2026-07-23 세션 5: 추천 매칭 복원 + 러닝 리포트

- ✅ **러너 추천 UI 실데이터로 복원** — 데모 정리 때 추천 경험(배너·1순위 다크카드·매치 바)이 데모 전용으로 묶여 실예약에선 밋밋한 리스트만 남았었음. 이제 실러너를 응답률(35%)·경험(30%)·페이스 적합(35%) 가중합으로 점수화해 1순위 추천 카드 + 대안 리스트 렌더. 실매칭 엔진(견종 경험·후기·거리) 전까지의 v1
- ✅ **러닝 리포트 화면** `/owner/report?bid=` — 실거리 vs 계획, 시간, 페이스, 종료 사유(컨디션 종료는 코랄 + 러너 메모 + 안내), 결제. 러닝 전 예약이면 상태 안내로 대체
- ✅ **알림 탭 가능** — kind/ref_id 로드, 보호자→리포트, 러너→요청/캘린더 ('자세히 보기 ›' 표시)
- ✅ e2e cleanup이 양쪽 E2E 계정 러너를 online=false 처리 (추천 목록 오염 방지). 이미 떠 있는 E2E 계정 제거는 SQL 1줄: `UPDATE runners SET online = false WHERE profile_id IN (SELECT id FROM auth.users WHERE email LIKE '%daengrun.test');`

## 2026-07-23 세션 6: 러닝 리포트 = 개별 러닝 기록 페이지로 승격

- ✅ 리포트 확장 — 히어로(코랄 실거리 km + 시간·페이스·계획), **목표 달성 바**(거리 %, 페이스 목표 대비 %), 러너·코스·지역 카드, 러너 노트(메디컬), 하이라이트 사진 슬롯(runs.photos — 실사진은 카메라 연동 후), 결제. 내 일정 완료 카드 '러닝 기록 보기' → 리포트 (콜렉터블 /cards 아님)
- 방향: /cards(콜렉터블)는 도파민 갤러리로 홈 최근 활동 위젯에서만 진입 — 기록의 진실은 리포트가 담당

## 2026-07-23 세션 7: 신원 레이어 + 체력 리포트 허브

- ✅ **프로필 실화** — 마이 화면: 실이름·동네 표시/편집(시트), 사진 탭 → 갤러리 → Supabase Storage 업로드 → profiles.avatar_url (김민준 목업 은퇴). Avatar 컴포넌트(사진, 없으면 모노그램) — 매칭 추천/대안 카드에 실사진. **⏳ Sean: `npx supabase db push` (0006_avatars — 스토리지 버킷+정책) + Metro 재시작 (expo-image-picker 설치됨, package.json 반영)**
- ✅ **체력 리포트** `/owner/fitness` — 모프 링 탭 도착지(기존 /owner/dog 블랜드 페이지 대체). 주간 링(실집계)·이번주 횟수/평균 페이스/스트릭·체력 나이 카드·**주간 목표 편집(± → dogs.weekly_goal_km 실저장, 홈 링 즉시 반영)**·8주 막대 추이·최근 러닝 → 리포트 링크
- ✅ **홈 히어로 링 실데이터** — weekKm/goal/스트릭/횟수/평균 페이스 전부 완료 러닝 집계 (가짜 12.5km 은퇴, 빈 상태 문구 '이번 주 첫 러닝을 예약해보세요')
- 다음 확산 지점: 리포트·일정 시트·채팅에 아바타 적용, 러너 자기소개(bio) 필드, 반려견 프로필 위저드(사진 포함)

## 2026-07-23 세션 8: 러너 스토어프런트 + 동네 러너 탐색

- ✅ **러너 공개 프로필** `/runner-profile/[id]` — 히어로(사진·티어·훈련사·온라인·★평균), 스탯 스트립(러닝·누적km·페이스·응답률), 자기소개+전문분야 칩, 러닝 가능 시간(availability rules 압축 표시), 보호자 후기 리스트, CTA '이 러너와 예약하기'(draft.preferredRunnerId → 예약 플로우) + 채팅(Realtime 후). 본인이 보면 미리보기 모드
- ✅ **자기소개 편집** — 마이 프로필 설정 시트에 러너 bio(300자, runners.bio) + '내 공개 프로필' 메뉴(미리보기)
- ✅ **매칭 카드 → 프로필 탭스루** + 선호 러너 최상단 '내가 고른 러너' 배지 (지명 후 소거)
- ✅ **우리 동네 러너 셸프** (홈) — 온라인 러너 가로 카드 → 프로필. 탐색형(supply-first) 매칭의 시작점 — 콜드스타트에서 즉시매칭보다 자연스러운 동선
- 다음: 프로필에서 시간대 선택→바로 예약(슬롯 피커), 채팅 문의(Realtime), 후기 작성 동선(보호자→러너), 디스인터미디에이션 가드(추후)

## 2026-07-23 세션 9: 가용시간 시스템 전체 연결

편집(러너) → 표시·슬롯 선택(보호자) → 실예약 시각 → 서버 검증까지 한 줄로 연결.

- ✅ **가용시간 편집기 실화** (`/runner/availability`) — 요일 토글·±30분 스테퍼·전체 적용·저장 (runner_availability_rules 전체 교체). 예약 규칙은 '준비 중' 표기, 목업 예외일정 제거
- ✅ **프로필 슬롯 그리드** — 러너 프로필에서 7일 날짜 스트립 + 60분 슬롯, 슬롯별 서버 `is_slot_available` 검사(규칙+예약충돌+휴식버퍼, '확인 중→가능/마감'). 슬롯 탭 → 코스·옵션 선택으로
- ✅ **+3h 하드코드 은퇴** — 예약 슬롯 시트가 실날짜(오늘~+6일)·실시각 생성, draft.scheduledAtIso → create-booking-hold. 가짜 '러너 N명'·인기·할인 배지 제거, 최소 2시간 통보 반영, '가장 빠른 시간' 실동작
- ✅ **지명 컨텍스트 예약** — 프로필에서 온 경우: 슬롯 시트가 그 러너 가용시간 밖을 '러너 불가'로 비활성, 헤더에 ★ 러너 표시, 결제 후 **지명 자동 전송** → 내 일정 (실패 시 수동 리스트 폴백)
- 남은 것: 예약 규칙(통보시간·최대세션·휴식) 편집 반영, 예외 일정(휴가), 요일 다구간

## 2026-07-23 세션 10: 프로필 풀블리드 + 갤러리 + 슬롯 탭 수정

- ✅ **슬롯 탭 안 되던 원인** — `disabled={isMe}`: 솔로 테스트에서 Sean은 모든 러너가 본인. 본인 예약 허용으로 수정 (owner==runner 루프가 테스트 체제)
- ✅ **프로필 풀블리드 개편** — 인스타식 좌우 마진 제로: 히어로·섹션 전폭 + 내부 패딩, 카드 테두리 대신 헤어라인 구분
- ✅ **러너 갤러리** — runners.photos + avatars 버킷 {uid}/gallery/*. 3열 엣지-투-엣지 그리드, 본인이면 ＋타일로 업로드·길게 눌러 삭제. **⏳ Sean: `npx supabase db push` (0007_runner_photos)**
- 참고: 사진 업로드는 새 dev build 필요 (image-picker 네이티브)

## 2026-07-23 세션 11: 러닝 리포트 = 공유 가능한 '러닝의 프로필 페이지'

- ✅ **리포트 v2 풀블리드** — 코랄 실거리 히어로 + **개인 기록 배지**(N번째 러닝·🏆 역대 최장 거리·⚡ 역대 최고 페이스·TOP3 — 내 역사 기준, fetchRunStandings), 사진 그리드(runs.photos, 엣지-투-엣지), 목표 바, 러너·코스, 러너 노트, 결제
- ✅ **↗ 자랑하기 공유** — RN Share 네이티브 시트 (헤더 ↗ + 하단 CTA): 거리·시간·페이스·코스·러너·배지 텍스트. 딥링크/OG 이미지는 웹 랜딩 후
- ✅ **러너 종료 플로우에 사진 단계** — done 화면 '오늘의 순간' (최대 6장 → runs.photos → 보호자 리포트·공유에 즉시 반영). "보호자 만족도와 재지명율이 올라가요" 인센티브 카피
- 보류(다음 세션들): 동네 피드(미니 인스타), 리더보드+드랍 인센티브(서버 집계 RPC 필요 — 러닝 데이터는 RLS로 타인 비공개), 바디캠 스틸(카메라 세션)
- 진입점 확인: 알림 탭 · 내 일정 완료 카드 · 체력 리포트 최근 러닝 → 모두 리포트로

## 2026-07-23 세션 12: 역할 게이트 + 러너 관제탑 홈 + 실드랍 트레일

- ✅ **프로필 역할 분리** — 편집(사진)은 `러너 모드 + 본인`만, 예약(슬롯 그리드·CTA·확인 바)은 `보호자 모드`만. 러너 모드에선 가용시간이 정적 요약으로. 솔로 계정에서 두 모드가 겹치던 혼선 해소
- ✅ **러너 홈 = 관제탑** — 진행 중 작업 카드(서버 단계별: 픽업 대기/이동 중/시작 대기/러닝 중 + 해야 할 액션 + **네이버 픽업 길찾기** 숏컷), 주간 스탯, 다음 예약 3건, 최근 완료 3건, 새 요청 배너/오프라인 안내
- ✅ **온라인 토글 실화** — runners.online 실저장 (끄면 추천·동네 셸프에서 제외, 문구로 안내)
- ✅ **드랍 트레일 실카운트** — 하드코딩 '5회' 은퇴. runners.total_runs 기반: 보석길 5칸(✓ 채워짐) + 보급상자, 픽 드랍 10회 깃발 진행바, 상태별 카피
- ⏳ Sean: **이미지 크래시 = 구 바이너리** — app.json 권한 수정이 폰에 안 들어감. `npx expo run:ios` 성공 1회 필요
- 픽업 길찾기 좌표는 아직 목업(서울숲) — 주소 실화 세션에서 실좌표

## 2026-07-23 세션 13: 마찰 제거 배치 (양쪽)

원칙: 항상 사용자 노력 최소화. 두 번째 예약이 첫 예약보다 중요하다 (관계 서비스).

- ✅ **⟳ 이대로 다시 예약** — 리포트·내 일정 완료 시트: 거리·페이스·코스(리포트 경유 시)·러너 지명까지 프리필, 시간만 고르면 끝
- ✅ **당겨서 새로고침** — 내 일정·요청·캘린더·수익·알림 전부 RefreshControl
- ✅ **단골 배지** — 러너 인박스: 함께 완주한 이력이 있는 강아지 요청에 '⟳ N번째 함께' (수락 결정 인지 부담 ↓, 안정 페어 유도)
- 논의 필요 대기열: ★ 단골 러너 즐겨찾기(테이블 1개), 위젯 인라인 인계 확인, 단골 자동 수락 규칙(동의 의미론 변화), 푸시 알림(Apple Developer 등록 선행), 주소 저장

## 2026-07-23 세션 14: Realtime + 실채팅 (마지막 큰 목업 함락)

- ✅ **0008 마이그레이션** — chat_threads INSERT 정책(읽기 전용 채팅의 역설 해소) + supabase_realtime 발행에 chat_messages/bookings/notifications 등록. **⏳ Sean: `npx supabase db push` 필수**
- ✅ **실채팅** — 예약당 스레드 1개(ensureThread, 레이스 안전), Realtime INSERT 구독으로 실배달(전송 후 리페치 폴백), 상대 실명 헤더 + 예약 컨텍스트 스트립, bid 없으면 역할별 진행 중 예약 자동 해석, 예약 없으면 정직한 빈 상태. 데모 대화(김민준 시드) 은퇴
- ✅ **미트업 실시간 동기화** — 양쪽 화면이 bookings UPDATE 구독으로 즉시 반응 (2.5s 폴링 → 8s 폴백으로 강등). 인계 확인이 상대 화면에 ~즉시 표시
- ✅ 채팅 진입점 전부 bid 전달: 위젯 2곳·일정 시트·양쪽 미트업
- 남은 채팅 고도화: 사진 첨부, 안심 통화, 30일 보관 크론, 읽음 표시

## 2026-07-23 세션 15: 러닝 이벤트 (응가 도장) + 라이브캠 SKU

- ✅ **러닝 이벤트** — 러너 러닝 화면에 원탭 스트립 💩응가/🍖간식/💧물/📷사진(업로드 포함). runs.events(jsonb)에 기록 + **보호자에게 즉시 알림** ('응가 완료 💩'). 리포트에 스탬프 칩(💩 응가 ×2 등). 목업 '사진 전송' 버튼 은퇴
- ✅ 0009: runs.events + **notifications 당사자 insert 정책** (예약 당사자만, booking kind만). **⏳ Sean: `npx supabase db push`**
- ✅ **라이브캠 애드온 SKU** — 3,900원, 클라+서버 가격표. 카메라 실장비 전 수요 측정용. **⏳ Sean: `npx supabase functions deploy create-booking-hold`**
- 보류(설계 대기): 응가 쿠폰(1/run 양측) → 인센티브 경제 세션에서 드랍·댕마일과 통합 원장으로, 간편 인증샷(네이티브 view-shot — 다음 리빌드에 동승), km 마일스톤 알림(실GPS 후), 선호 옵션 실화(반려견 프로필 위저드)

## 2026-07-23 세션 16: 반려견 프로필 실화 (마지막 카드보드 은퇴)

- ✅ **/owner/dog 재구축** — 실편집기: 사진(avatars {uid}/dogs/*), 이름·견종·생일·몸무게·중성화, 러너 전달 성향 메모(200자), **선호 태그 10종 멀티선택**(dogs.preferences.tags — '선호 옵션 실화' 완료). 0010: dogs.photo_url **⏳ Sean: `npx supabase db push`**
- ✅ **러너 요청 카드에 반려견 실면모** — 사진 아바타 + 선호 태그 칩 (러너가 수락 전에 아이를 안다)
- ✅ 예약 화면 강아지 카드 실데이터 + '프로필 ›' 진입, 마이 메뉴 '반려견 프로필', 홈 인사·주간 목표 라벨 실이름
- 남은 것: 다견 가구(반려견 추가 버튼은 스텁), 예방접종 기록, 체력 나이 산식

## 2026-07-23 세션 17: 다견 가구 · 예방접종 · 체력 나이 산식

- ✅ **다견 가구** — 반려견 프로필에 스위처 칩 + ＋추가(이름 입력 → 즉시 편집), 예약 화면에 다견 선택 칩(선택한 아이로 예약 생성), 반려견 추가 버튼 실동작
- ✅ **예방접종** — 4종 체크(종합백신·광견병·코로나 장염·켄넬코프, dogs.vaccinations), 러너 요청 카드에 '💉 백신 N종' 신뢰 칩
- ✅ **체력 나이 v1 산식** — 생일 기반 실나이 − (최근 4주 주간평균/목표 비율 ×1.8 + 스트릭 보정), dogs.fitness_age에 자동 저장, 홈 링·체력 리포트에 표시. '베타 산식' 명시, 수의 검증 산식으로 교체 예정. 생일 없으면 '생일을 등록하면 측정돼요'
- 마이그레이션 불필요 (기존 컬럼 활용) — 코드만, r로 리로드

## 2026-07-23 세션 18: UI 감사 실행 (P0·P1·P2)

- ✅ **P0 정직성**: 양쪽 미트업 실신원(fetchMeetupInfo — 김민준·초코 목업 완전 은퇴, 가짜 6분 ETA 삭제, 실메모·실사진·실코스), 예약 시간 명시 선택 강제(미선택 시 결제 대신 슬롯 시트, +3h 폴백 은퇴), 홈 가짜 리워드 비컨·목업 최근활동 카드·목업 '내 주변 인기 러너' 섹션 제거
- ✅ **P1 데드 UI**: 일정 주간/월간 토글, 알림 필터 탭·필터 버튼, 페이스 가이드 버튼 제거. '같은 러너로 일정 변경' 실동작(지명 프리필). 매칭 화면 기대치 문구 추가
- ✅ **P2 리텐션**: 러너 홈 **티어 진행바**(베테랑 30회/18% → 마스터 100회/15%, v1 잠정 기준), 보호자 홈 **목표 위험 넛지**(목~일, 목표 미달 시) + **스마트 재예약 칩**(예정 없음+완료 이력 시 '지난번처럼 다시 예약할까요?')
- P3(다음 리빌드 동승): lucide 아이콘, 인증샷 view-shot, 햅틱, 스켈레톤 로더

## 2026-07-23 세션 19: 미학 오버홀 (4레버)

- ✅ **타이포 시스템** — theme.ts에 `type` 스케일(display/numeric/title/heading/body/label/caption) + 규칙 명문화: **900은 숫자와 화면 제목만**. colors에 forest/coralText/border/green 토큰 추가(산재 리터럴 수렴 목적지). 전면 교체는 화면 보며 점진 적용
- ✅ **모션 = 의미** — 링이 0→pct로 차오름(750ms), 리포트 목표 바 채움 애니, 슬롯 확인 바 스프링 등장
- ✅ **사진의 구조화** — 리포트 히어로에 러닝 첫 사진 배경(0.28), 러너 프로필 히어로에 갤러리 첫 사진 배경(0.25), 홈 인사·위젯 아바타 = 강아지 실사진 (fetchFitness.dogPhotoUrl)
- ✅ **다크 토글 은퇴** — 반쪽 다크는 깨져 보임; '나이트 러너' 테마로 전 화면 완성 후 복귀 (테마 코드는 보존)
- ✅ **햅틱 (지연 로드)** — 결제·슬롯 확정·수락·인계 확인·러닝 이벤트에 haptic(). 구 빌드에선 무시, **다음 리빌드부터 진동**
- ✅ **다음 리빌드 동승 준비** — expo-haptics 57.0.1 + react-native-svg + lucide-react-native 설치·package.json 반영 (import는 리빌드 후 아이콘 스왑 세션에서). **⏳ Sean: `npx expo run:ios` 시 함께 컴파일됨**
- 보류: 마스코트 vs 사진-온리 (브랜드 인격 결정 — 숙고 대상), 나이트 러너 테마 완성

## 2026-07-23 세션 20: GPS/지도 실화 (마지막 대형 목업)

- ✅ **실GPS 거리** — 러너 러닝 화면: expo-location watchPosition(5m/2s), 하버사인 누적(노이즈 필터 2–120m), 실시간 초. GPS 불가(권한 거부·구 빌드)면 데모 타이머 폴백 + '데모 거리' 표기. **정산이 드디어 실거리 기반**
- ✅ **실트레이스** — 정산 후 runs.trace 저장 (리포트 지도의 원천)
- ✅ **km 마일스톤 알림** — 실거리에서만: 'N km 돌파 🏃' 보호자 알림 + 러너 햅틱
- ✅ **위치 브로드캐스트** — Realtime 채널(DB 무기록)로 러너 위치 발행, 보호자 라이브 화면 구독
- ✅ **보호자 라이브 실모드** — 실지도(react-native-maps, 새 빌드) + 러너 마커 + 경로 폴리라인, 위치 수신 대기 상태 정직 표기, 실통계(가짜 BPM/KCAL/SPM·가짜 REC 숨김), 완료 감지 → 리포트로 (데모의 '/owner/pay' 라우팅은 데모만), 강제 종료 → 채팅 조율
- ✅ expo-location 57.0.6 + react-native-maps 1.29.0 + 위치 권한 plugin. **⏳ Sean: `npx expo run:ios` (이번 리빌드에 haptics·svg·lucide·maps·location 전부 동승)**
- 남은 것: 리포트에 실트레이스 지도 렌더(다음 폴리시 세션), 픽업 실좌표(주소 실화)

## 2026-07-23 세션 21–22: 폴리시 배치 + 리뷰 실화

**세션 21 (폴리시):**
- ✅ **Icon 컴포넌트** — lucide 지연 로드 + 글리프 폴백 (리빌드하면 자동 실아이콘). 바텀 내비 5탭 적용 (House/CalendarDays/Users/ShoppingBag/CircleUserRound/Inbox/Wallet)
- ✅ **리포트 실경로 지도** — runs.trace → MapView 폴리라인(새 빌드) / HeatTrace 정규화 폴백(구 빌드, '지도 배경은 새 빌드에서' 표기)
- ✅ **📸 인증샷 만들기** — 브랜디드 카드(사진 배경 + 코랄 km + 기록 배지 + 댕런 워드마크) → view-shot 캡처 → 네이티브 공유. 구 빌드는 텍스트 공유 폴백. react-native-view-shot 5.1.1 설치(리빌드 동승)
- ✅ **스켈레톤 로더** — Skeleton 컴포넌트, 리포트 로딩 적용

**세션 22 (리뷰):**
- ✅ **보호자 → 러너 후기 실화** — /owner/review (별점·태그 6종·노트·플랫폼 전용 플래그), reviews 실저장, 중복 방지(23505), 리포트 '★ 러너 후기 남기기' 진입. 러너→강아지 리뷰는 기존 실저장 확인
- ✅ **0011 스토어프런트 리뷰 정책** — public 러너 리뷰 전체 공개 읽기 (기존엔 당사자만 → 프로필 후기가 타인에게 항상 빈 표시). **⏳ Sean: `npx supabase db push`**

## 2026-07-23 세션 23: 인센티브 경제 (통합 원장)

- ✅ **댕마일 = 단일 화폐** — settle-run이 miles_ledger에 실적립: 완주 양측 +50, **응가 도장 보너스 양측 +30**(러닝당 1회 — '응가 쿠폰' 실현). 상태머신이 중복 적립 차단. **⏳ Sean: `npx supabase functions deploy settle-run`**
- ✅ **주간 리더보드** — 0012: SECURITY DEFINER 집계 2종 (강아지 주간 km TOP10 / 러너 주간 러닝 수 TOP10, KST 월요일 리셋, 이름·사진·합계만 노출). **⏳ Sean: `npx supabase db push`**
- ✅ **/leaderboard 화면** — 내 댕마일 카드(잔액+최근 적립+적립 규칙), 탭 2종, 메달 TOP3 강조, 빈 주 카피('첫 러닝이 1위가 되는 주'), 당겨서 새로고침. 진입: 보호자 홈 '🏆 동네 랭킹', 러너 홈 '🏆 랭킹'
- 다음 경제 확장: TOP3 주간 보상 크론, 드랍 contents.miles → miles_ledger 연결, 샵 마일 사용(shop_spend), 시즌제

## 다음 실화(實化) 순서 (제안)

1. Supabase: auth + bookings + 상태 머신 → 수락 알림이 진짜가 되는 지점
2. react-native-maps + expo-location → 지도·트래킹 실화
3. Expo Notifications → 알림 센터 실화
4. Realtime 채팅
5. 결제 (파일럿은 계좌이체/수기로도 가능)
