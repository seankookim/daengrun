# Gap sweep — FINAL (2026-09-25 16:3x–17:0x KST; resume of the 03:44 run)

Counts: raw 127 → deduped 112 → **kept 91 / refuted 21** (one adversarial verifier per lens, plus v1's 20 confirmed / 2 refuted). Plan: 10 slices · 16 letters · 18 dropped. File conflicts named by the planner: ['app/src/lib/api.ts — fix/runner-live-run (runner-jobs filters :1544-1583/:1958-2050, fetchBookingSync :1491, one string :7033) and fix/notification-landings-ops-inbox (fetchNotifications opts + markNotificationsRead beside :6288/:6432). Serialise: land runner-live-run first, ops-inbox rebases. Also touched by HELD be/0211-owner-proximity and possibly in-flight fix/chat-read-cursor — union-merge at their landings.', 'app/scripts/check-a11y-roles-baseline.txt — fix/runner-live-run (calendar 3→2), fix/owner-schedule-report (schedule 16→15 or lower), ui/safety-settings-chrome (safety 7→≤3) each shrink a line while in-flight fix/client-review-2 rewrites the ledger to per-element fingerprints. Per §A step 3 the merge commit edits the ledger; builders run the gate, edit minimally, and report the line.', "app/app/owner/matching.tsx — fix/owner-request-funnel (pick mode) vs HELD be/0211-owner-proximity (distance bands in the roster). 0211's landing must re-merge matching.tsx by hand; the funnel builder confines edits to mode/roster-load/nominate and names the functions touched.", "app/app/owner/home.tsx and app/app/owner/radar.tsx — HELD be/0211 owns them; no wave slice edits them (owner-journey-1's home.tsx/lateness.ts arms were deliberately dropped from the hero slice).", "supabase/migrations/REGISTRY.md and supabase/tests/harness.sh — be/custody-strand-sweep vs in-flight be/0221-offered-slots-merge and be/0222-evidence-preserve-fix; union-resolve one row/line per number, numeric order, and grep -c '^<<<<<<<\\|^>>>>>>>\\|^=======' = 0 before every commit.", 'supabase/tests/241_ops_category_and_payout_stuck_suite.sql — be/custody-strand-sweep adds two titles to the 0210-O2 array; if 0221/0222 also touch suite 241, union-merge the array.', 'fix/alert-fold-copy sweep targets in files owned by slices 1–9 (safety, request, schedule, run, home, requests, my, done, meetup) — the copy slice runs LAST, rebases after each landing, and carries a shrinking per-file ledger for anything it could not reach; it never edits a file another builder still holds.', "app/src/lib/push.ts comment at :264-268 (the primer's button is the only caller of requestPushPermission) — ui/safety-settings-chrome adds a second ENTRY POINT (the settings screen mounts NotificationPrimer, not a new caller) and must not edit push.ts; the orchestrator amends the comment on landing if wanted.", 'app/src/theme.ts secTitle — added by ui/my-fitness-floor; ui/safety-settings-chrome and fix/runner-home-requests inline the same values with a `// → theme.secTitle` note; a one-line repoint follows next wave.'].

## Slices (priority order)

| P | branch | kind | mig | findings | files owned |
|---|---|---|---|---|---|
| 1 | `fix/owner-hero-late-doors` | client | no | owner-journey-1, owner-journey-2 | app/src/components/home-hero.tsx, app/src/lib/home-hero-route.ts, app/test/home-hero-route.test.cjs |
| 2 | `fix/runner-live-run` | client | no | runner-journey-2, runner-journey-9, runner-journey-11, runner-journey-12, runner-journey-10, runner-journey-3, runner-journey-8, onboarding-first-run-8, copy-hierarchy-13, copy-hierarchy-6 | app/app/runner/run.tsx, app/app/runner/meetup.tsx, app/app/runner/calendar.tsx, app/app/runner/return-seal.tsx, app/app/runner/base-pin.tsx, app/app/runner/done.tsx, app/app/runner/review.tsx, app/src/lib/api.ts, app/src/lib/runner-job-route.ts, app/src/lib/receipt-phase.ts, app/test/runner-job-route.test.cjs, app/test/receipt-phase.test.cjs, app/test/runner-jobs-filters.test.cjs |
| 3 | `fix/runner-home-requests` | client | no | runner-journey-1, runner-journey-4, runner-journey-6, runner-journey-8, onboarding-first-run-2, onboarding-first-run-3, onboarding-first-run-6, runner-journey-15, ui-consistency-16, ui-consistency-7, ui-consistency-4, ui-consistency-2, less-is-more-19 | app/app/runner/home.tsx, app/app/runner/requests.tsx, app/app/runner/apply.tsx, app/app/onboard/runner.tsx, app/app/runner/earnings.tsx, app/src/lib/runner-application-copy.ts, app/test/runner-application-copy.test.cjs |
| 4 | `fix/notification-landings-ops-inbox` | client | no | contract-gaps-1, ops-notifications-3, ops-notifications-2, ops-notifications-4, contract-gaps-2, ops-notifications-12, ops-notifications-5, ops-notifications-7, ops-notifications-8, ops-notifications-6, ops-notifications-9 | app/src/lib/notification-route.ts, app/src/lib/push.ts, app/test/notification-route.test.cjs, app/app/ops/index.tsx, app/app/alerts.tsx, app/app/ops/handoffs.tsx, app/src/lib/ops-roster.ts, app/app/ops/roster.tsx, app/test/ops-roster.test.cjs, app/test/ops-system-titles.test.cjs |
| 5 | `fix/owner-request-funnel` | client | no | owner-journey-3, owner-journey-4, owner-journey-8, onboarding-first-run-1, copy-hierarchy-7 | app/app/owner/request.tsx, app/app/owner/matching.tsx, app/app/index.tsx, app/src/lib/request-gate.ts, app/test/request-gate.test.cjs |
| 6 | `fix/owner-schedule-report` | client | no | owner-journey-6, owner-journey-7, less-is-more-18, onboarding-first-run-12, contract-gaps-7, ops-notifications-3, ops-notifications-4, ui-consistency-2 | app/app/owner/schedule.tsx, app/app/owner/report.tsx, app/src/lib/booking-state-copy.ts, app/test/booking-state-copy.test.cjs |
| 7 | `be/custody-strand-sweep` | server | yes | backend-logic-1, backend-logic-2, backend-logic-4, backend-logic-6 | supabase/migrations/<next-free>_custody_strand_sweep.sql, supabase/migrations/REGISTRY.md, supabase/tests/<next-free>_custody_strand_suite.sql, supabase/tests/harness.sh, supabase/tests/241_ops_category_and_payout_stuck_suite.sql, supabase/tests/230_resolution_visibility_suite.sql |
| 8 | `ui/safety-settings-chrome` | client | no | ui-consistency-12, ui-consistency-13, ui-consistency-15, onboarding-first-run-4, ui-consistency-1, ui-consistency-2, ui-consistency-7, ui-consistency-10, ui-consistency-4, ui-consistency-5, less-is-more-16, ops-notifications-10 | app/app/safety.tsx, app/app/settings.tsx, app/app/notification-settings.tsx, app/app/runner/bank-account.tsx, app/app/leaderboard.tsx, app/src/components/ui.tsx, DESIGN.md |
| 9 | `ui/my-fitness-floor` | client | no | ui-consistency-8, ui-consistency-14, ui-consistency-4, ui-consistency-5, less-is-more-13, less-is-more-15, less-is-more-7, less-is-more-12 | app/app/my.tsx, app/app/owner/fitness.tsx, app/app/payments.tsx, app/app/cards.tsx, app/app/shop.tsx, app/src/theme.ts, app/test/font-floor-my-fitness.test.cjs |
| 10 | `fix/alert-fold-copy` | client | no | copy-hierarchy-1, copy-hierarchy-3, copy-hierarchy-7, copy-hierarchy-8, copy-hierarchy-10, ops-notifications-10 | app/src/lib/alert-fail.ts, app/test/alert-fail-sweep.test.cjs, app/src/lib/rpc-error.ts, app/test/rpc-error-fold.test.cjs, app/test/copy-forms.test.cjs, app/src/lib/late-copy.ts, app/src/lib/checkin-copy.ts, app/src/components/checkin-answer.tsx, app/src/lib/gear-claim-form.ts, app/src/components/course-detail.tsx, app/src/lib/notification-prefs.ts, app/test/notification-prefs.test.cjs, app/app/compose.tsx, app/app/course/[id].tsx, app/app/owner/card-link.tsx, app/app/owner/dog.tsx, app/app/owner/review.tsx, app/app/owner/live.tsx, app/app/owner/address-pin.tsx, app/app/runner/availability.tsx, app/app/runner/rewards.tsx, app/app/runner-profile/[id].tsx, app/app/incident/[bid].tsx, app/src/components/card-link-panel.tsx |

## Letters for Sean

### The handoff moment: what the owner's hero button says, and whether opening the runner's meetup screen means 출발

Two sides of the same minute. Owner: STATUS_MAP maps picked_up → 'handoff' and the hero draws the coral 「인계하기 · 아이를 넘기고 봉인해요」 AFTER both stamps landed (home-hero.tsx:200/245/356), while the real your-turn moment (runner_enroute + arrived_at) shows a gold 「티켓 보기」 (home-hero.tsx:470). The build slice ships the post-seal reword; the open question (your Q4) is whether the ARRIVED state gets coral. Runner: runner/meetup.tsx:232-238 fires runnerEnroute(jobId) on every mount — tapping tomorrow's ticket to read the address tells the owner 「러너가 픽업 장소로 출발했어요」 and flips the booking to runner_enroute (transition-booking:362-371 allows it within 24h). The file's own comment (:222-231) records this as an open door awaiting you.

- ⓐ Owner: arrived+resumable becomes the coral 「내 차례 · 인계하기」 frame (GO law: coral = your turn) · Runner: delete the mount-time enroute call; an explicit 「출발했어요」 door in the existing enroute action block calls runnerEnroute, with `departed` read from server truth
- ⓑ Owner: keep the gold ticket on arrival (calm), coral only after the seal as today · Runner: keep open-is-depart but print 「이 화면을 열면 보호자에게 출발 알림이 가요」 on the ticket and the screen
- ⓒ Leave both as shipped
- **Recommendation:** ⓐ for both — the arrived minute is the one the GO law exists for, and a tap that notifies the other party must say so or be explicit; the meetup stage machine is untouched either way.

### Runner-side marketplace rules: withdrawing from a confirmed booking, unanswered nominations, and applicant-tier nominated work

(1) No runner-side cancel exists anywhere: transition-booking's action list has none (index.ts:3), the state table permits confirmed → cancelled_runner (0001:205), meetup's pastCeiling hides every CTA, and a dead confirmed booking stays home's one next action forever (api.ts:1310 — no expiry cron past confirmed). The runner_decline 409 copy even tells the runner to 'cancel' via a path that does not exist. (2) A nomination the runner never answers runs until scheduled_at and expires (0080:935); the owner is never warned and has no owner-side door back to the open pool (only runner_decline writes runner_pending → matching; generate_recurring_bookings re-nominates the last runner every week, 0180:150-166). A reminder push and an owner 「공개 요청으로 전환」 edge action are build-only and queued for the next wave; auto-releasing to the pool on a clock changes what a nomination promises. (3) The applicant refusal (index.ts:265) sits only in the open-pool branch; the nominated CAS (:271-276), request_runner (:292-293) and the recurring generator carry no tier gate — reachable only by a demoted-after-verified runner or an out-of-band uuid.

- ⓐ Runner may withdraw from confirmed/runner_enroute before the appointment: `cancel_runner` action (runner-gated CAS → cancelled_runner, cancel_reason 'runner_withdraw', booking_faults row, owner told 「러너가 예약을 취소했어요」 with a re-request door, no owner fee — 0220 maps cancelled_runner → no_charge) · unanswered nomination auto-releases to the pool N hours before start (owner told) · applicants refused on nominated and recurring work with 「인증 전 러너는 지명할 수 없어요」
- ⓑ No runner withdrawal (fix the 409 copy to 운영팀 문의) · nomination never auto-releases; only the reminder + owner withdraw door · applicant gate as ⓐ
- ⓒ Withdrawal allowed only until N hours before (name N) · auto-release at the same N · applicant gate as ⓐ
- **Recommendation:** ⓒ with N = 12h — a sick runner needs an exit and the owner needs the pool; the applicant gate has no downside in any option.

### Deleting a pickup address that a live booking still references

addresses.tsx:116 remove() deletes with no default/only-address guard; bookings.address_id has no ON DELETE (0001:170), so deleting an address tied to a held/confirmed booking fails with a raw Postgres FK message shown as 「삭제 실패」, and deleting the only address leaves the next request with no pickup (the request-screen gate ships in the build slice). Two honest shapes exist and both change what the owner is allowed to do.

- ⓐ Refuse deleting an address referenced by a live (held/confirmed/runner_enroute) booking, with the sentence 「진행 중인 예약이 이 주소를 써요 — 예약이 끝나면 지울 수 있어요」 (one client read before delete; or a server-side check in a small migration)
- ⓑ Allow it after a confirm Alert stating the consequence, and null the booking's address (needs ON DELETE SET NULL migration + runner copy already handles null)
- ⓒ Leave as is (FK error surfaces)
- **Recommendation:** ⓐ — the runner must be able to navigate to a booked pickup; a client-side refusal is one read and no migration.

### Runner ranking bars: two of three axes are synthetic

matching.tsx:57-87 is the whole ranking: exp = min(97, 62 + totalRuns×5), paceFit = max(58, 100 − |Δpace|/4), weights 0.35/0.3/0.35. The 「AI」 wording is removed in the build slice (honesty law). What remains is that the 러닝 경험 and 페이스 적합 Bar tracks read as measured percentages while only 응답 신뢰도 (respond_rate_pct) is real.

- ⓐ Keep the Bar only for 응답 신뢰도; show 러닝 경험 as the plain number 「완주 N회」 and 페이스 적합 as 「Δ M:SS/km」 (real fields, no synthetic %)
- ⓑ Keep all three bars but caption 「앱이 계산한 추천 점수」 under the rail
- ⓒ Drop the two synthetic axes entirely; rank by respond rate + runs
- **Recommendation:** ⓐ — binds real fields, keeps the ranking useful, no fabricated percentages on the screen where the owner picks who takes the dog.

### 안심 센터 content: the insurance sentence and the 안전 체계 block

owner/meetup.tsx:770 and runner/meetup.tsx:762 advertise 「펫보험 파트너십 협의 중 — 사고 시 안심 센터에서 바로 도와드려요」 on every stage of the seal screen; safety.tsx:206 carries the same 협의 중 row. The fact-pack (docs/investor/fact-pack.md:133) flags 「[Sean 확인 필요 — 보험사 접촉 여부]」. Separately, safety.tsx:203-222 spends three non-tappable InfoRows and a ~40-word paragraph between 긴급 연락처 and 사고 신고 (honesty-repaired copy from 08-05/08-08). What users are told about safety is yours (CLAUDE.md §Operations).

- ⓐ No active insurer negotiation today → drop the insurance clause: meetup feet read 「사고가 나면 안심 센터에서 바로 도와드려요 ›」 and the safety row is omitted; the 안전 체계 rows move behind one row 「안전 체계 안내 ›」 with 사고 신고 directly under contacts (owner/home.tsx:1007 sub follows)
- ⓑ Negotiation is real → keep the insurance sentence; still fold the 안전 체계 rows behind a tap
- ⓒ Keep everything as shipped
- **Recommendation:** ⓐ unless an insurer contact is live — a promise about a partnership that does not exist yet has no place at the handoff moment.

### Legal documents in-app: which versions, and hosted or bundled

login.tsx:113 says 「로그인하면 이용약관과 개인정보 처리방침에 동의하게 돼요」 as static text; no screen or URL renders either document (grep: login.tsx is the only file mentioning 이용약관; no app/app/legal route; settings has no 약관 row). docs/legal/terms-of-service.md and privacy-policy.md exist but counsel's 제2조/제3조 item and the 0-untricies 0123 coordinate line are open. Linking the drafts in-app IS publishing them; App Store review requires a reachable privacy policy.

- ⓐ Publish the repo drafts as they stand: new app/app/legal/[doc].tsx (bundled plain Text), tappable 「이용약관 · 개인정보 처리방침」 under the Kakao button, one settings row
- ⓑ Host them at a URL you control (Notion/GitHub Pages) and link out with Linking.openURL from the same two entry points; no route built
- ⓒ Hold until counsel closes 제2조/제3조 and the 0123 sentence, then ⓐ
- **Recommendation:** ⓑ now (a hosted page can be corrected without a client build), then bundle once counsel signs off.

### Runner home quiet-day word budget (your A① pick from 2026-08-24)

On a quiet day runner/home.tsx prints the online state twice (toggle :842-859 and the empty-inbox summary row :1406-1409) and explains it twice (toggle sub + emptyInboxNote :1428-1430); 러닝 가능 시간 has two homes with two identically-named 「시간 조정 ›」 doors (:1410-1422 and the §3b section :1638). These rows are your A① pick (docs/labs/enh-runner-home-lab.html:312-318, approved verbatim 08-24); §7a-bis (08-26) postdates it. Note: while ONLINE the note is the only place saying open requests arrive regardless; the section's 「기본 06–22시 · 보호자 예약 화면에 즉시 반영」 line states an immediate server write and must stay.

- ⓐ Keep A① as picked
- ⓑ Drop only the two summary rows (온라인 켜짐/꺼짐 and 러닝 가능 시간 + its 시간 조정 door) and keep the note; the §3b section is the single home for hours
- ⓒ Drop rows and note
- **Recommendation:** ⓑ — one fact once; the note survives because it is the only carrier of the open-pool rule while online.

### Rows to delete: owner-home 주간 목표 nudge, 마이 예약 관리 row, passport chrome

(1) owner/home.tsx:970-986 주간 목표 row (Thu–Sun when 0<weekKm<goalKm) is functionally identical to the hero's 미리 예약 (clears nomination, pushes /owner/request) and the 체력 row already prints 「이번 주 Xkm / Ykm」 — a fifth booking door at most. (2) my.tsx:198 「예약 관리」 duplicates the second dock tab; kept only as 「기존 경로 유지」 by a session, and the MENU is marked frozen (:19, :433). The 러닝 기록 row (:203) must STAY (it is the only /cards door when the stamp read fails). (3) Passport chrome: masthead 「DOGS HIGH · MEMBER … KOR」 + PASSPORT tag (my.tsx:226-233), TYPE/NO. grid (:286-298), MILESTONE strap (cards.tsx:206) — a deliberately kept ceremony artifact (my.tsx:19-24, DESIGN.md §8); the § section rows are converted in the build slice regardless.

- ⓐ Delete the 주간 목표 row and the 예약 관리 row; keep the passport chrome
- ⓑ Delete all three (passport becomes a plain paper card with role + serial only)
- ⓒ Keep everything
- **Recommendation:** ⓐ — two plain duplicates go; the passport is identity you confirmed and costs no confusion.

### Chrome rulings: repaint alerts.tsx to paper, and the dock on 마이's children

alerts.tsx is still the lilac world (lilacRadius/lilacShadow imports :14; s.board/evt/empty/failStrip/retryBtn rounded + shadowed; latin MY / ALERTS, ARRIVALS, TIME/TYPE, EMPTY STATE, DOGS HIGH postmark, NEW seal) — your 2026-08-01 arrivals-board pick; DESIGN.md:21-22/:241 sanction the dark board itself; ARRIVALS/NEW are the awaiting-sean item 19 question (a kicker on an artifact). Tab bar: tab-parent.ts keeps the dock on exactly alerts/safety/cards (announcer ruling 09-22) while settings/payments/notification-settings/profile-edit/addresses/dog/fitness/rewards/leaderboard drop it — from 마이, 알림 keeps the bar and 설정 removes it; every dock-less screen still exits via goBackOrHome.

- ⓐ alerts: radius 0 / no shadow on cells, empty, failStrip, retryBtn and mast; keep the dark board as the one artifact; drop MY / ALERTS, TIME/TYPE, EMPTY STATE, the DOGS HIGH postmark AND ARRIVALS/NEW · dock: add the nine 마이 children to TAB_PARENTS and mount BottomNav (alerts.tsx pattern)
- ⓑ alerts: same repaint but keep ARRIVALS and NEW on the board/seal only (item 19 = 'a kicker may label an artifact') · dock: drop BottomNav and the TAB_PARENTS rows from alerts/safety/cards instead (reverses the 09-22 ruling)
- ⓒ Leave both
- **Recommendation:** ⓑ for alerts (the board keeps its two artifact labels), ⓐ for the dock (predictable bar on every 마이 child; tab-parent.test.cjs updated in the same commit).

### Returning users: auto-route into the remembered role on cold launch

index.tsx:137-175 renders the two role halves on every cold launch; the only redirect is !auth → /login. auth-context.tsx:33 hydrateRole() already reads profiles.role but nothing routes on it. 역할 전환 exists on 마이 (:451) and settings (:148), so the fork is not the only switch. Direct-their-attention says the first frame should be the hero.

- ⓐ Auto-start into profiles.role once per cold launch when resolved and non-null (fail-open on error, ~3s cap, fork stays for null)
- ⓑ Keep the fork as a deliberate choice screen
- ⓒ Remember only the last pick on-device (no server read)
- **Recommendation:** ⓐ — every returning customer skips a screen they already answered; the fork survives for new accounts.

### 주간 랭킹 reward sentence: state the shipped rule (needs a production cron read)

leaderboard.tsx:176 says 「주간 TOP 3 시즌 보상은 곧 공개돼요」 but grant_weekly_rewards() (0158:325-364) already pays 200/100/50 하이 포인트 to last week's TOP3 dogs and runners, scheduled as cron 'weekly-rewards' (0014:70); api.ts:6119 labels those ledger reasons. The sentence calls a shipped reward 'coming soon'. Whether 'weekly-rewards' is registered on PRODUCTION cron is unverified (0014 installed it inside the swallowing do-block form 0172 condemned) — a production read is yours.

- ⓐ Read `select jobname, active from cron.job` on production; if active, one-line copy 「주간 TOP 3에게 하이 포인트가 지급돼요」 (amounts stay off the sentence)
- ⓑ If not registered, re-register in a small migration on the next db push and then ⓐ
- ⓒ Remove the promise sentence entirely
- **Recommendation:** ⓐ, falling to ⓑ if the row is missing — a built reward should be named, not teased.

### Voice: the word for the customer's dog (아이 / 강아지 / 반려견)

Three nouns in customer copy (반려견 69, 강아지 44, 아이-forms 36). The role rule already holds where it matters: delete-account-sheet.tsx uses 강아지 to the runner (:77) and 우리 아이 to the owner (:101); runner-side category nouns use 강아지. The genuine deviation is 반려견 in OWNER-facing prose: course-detail.tsx:146, request.tsx:443/1256. No voice ruling exists in DESIGN.md or docs/decisions.

- ⓐ 아이 for the owner's own dog, 강아지 as the runner-side/category noun, 반려견 retired to legal text — a grep sweep over owner-facing strings (skipping club/community/shot and legal docs)
- ⓑ Keep 반려견 as the neutral formal word everywhere it stands today
- ⓒ No ruling; leave as is
- **Recommendation:** ⓐ — the product already speaks this way in its newest screens; three sites move.

### Runner application form: drop the two unread fields (needs a migration)

apply.tsx:612-628 requires 감당 가능한 최대 체중 and 활동 반경; 0062:377-378 writes them to runners at approval; nothing reads either column anywhere (app, functions, migrations beyond 0001/0062). runners_available_for and the request cards never filter by them; the runner sees the dog's weight on the card (requests.tsx:494). CORRECTION to the finder: dropping is NOT client-only — 0062:68 declares runner_applications.max_dog_weight_kg NOT NULL with no default, so a migration must relax it (or set a default); any client sentinel would be fabricated data.

- ⓐ Drop both fields: migration relaxes the NOT NULL, client removes the two inputs/validators and api.ts form keys
- ⓑ Keep them and make runners_available_for / marketplace_open_requests honour them (server work, new predicate — availability definitions are frozen as three distinct predicates, so this is a fourth)
- ⓒ Keep collecting (status quo)
- **Recommendation:** ⓐ for the pilot — collected-but-unused data is the pattern profile-gaps.tsx names as collection, not a nudge.

### Club host: reverse a dog rejection in-product (Codex lane)

session_reconsider_dog (0043:362-383) is granted to authenticated, gated on approval='rejected' + host identity, creates a new attempt row — and has zero client callers; club/console/[sid].tsx has no 'rejected' branch. The owner can re-apply (0043:201-206), so nobody is stuck; this is a missing host promise. Club client work is your Codex W4 lane, not Claude's wave.

- ⓐ Yes, hosts may reverse: api.ts reconsiderDog wrapper (clubRpc list +1, check-rpc-contracts) and a 「거절 번복」 door on rejected rows using the doReview Alert pattern (:256-275), mapping not_rejected / session_closed to Korean — hand to Codex W4
- ⓑ No; leave the RPC unused (optionally revoke from authenticated in a later hygiene migration)
- ⓒ Defer until club v2 is measured in the pilot
- **Recommendation:** ⓐ into the Codex batch — the server already models it and the console screen already has the review grammar.

### 인계 멈춤 desk: give the operator a contact means, or keep it a watch list

ops/handoffs.tsx:79 instructs 「양측에 직접 연락해 확인해주세요」 while OpsStalledHandoff (api.ts:7274-7287) carries first names, stamps and minutes only — 0206's header (:73-77) names 'no phone, no address, no memo' as a deliberate privacy floor and the desk is read-only pending your ruling on what a stuck handoff becomes. The build slice rewrites the lead so it no longer instructs an impossible action.

- ⓐ ops_stalled_handoffs() returns owner/runner phone to holders of the handoff_unanswered class (privacy widening: migration + a journal row in the ops_bank_account idiom)
- ⓑ Stay a watch list; the desk explains that both parties were re-asked and no rule exists yet (the build-only lead)
- ⓒ Add an ops → party chat door instead of phone (new server surface: an ops-authored message on the booking thread)
- **Recommendation:** ⓑ now; revisit with the strand rules in the runner-withdrawal letter — a phone number is a disclosure the desk cannot yet justify by an action it can take.

### Custody strand thresholds and whether ops may force-end a run the runner never stopped

The be/custody-strand-sweep slice ships arm g (picked_up with no start_run; active with no end_run) behind two ops_flags minute columns that ship NULL (= off), exactly like return_strand_minutes (0201:503-512), and widens the work gate. What stays yours: the two minute values, and whether an operator may end/settle a run the runner never stopped (force_return_tx 0205:256-257 and ops_resolve_return_tx 0201:177-178 both raise run_not_ended; ending a run freezes money).

- ⓐ Start strand 30 min after both handoff stamps, end strand 60 min past km×8+25; ops may force-end with a reason token (new arm in force_return_tx, journaled, settles at recorded distance)
- ⓑ Same thresholds; no ops force-end — the bell + gate only, the runner or owner must act
- ⓒ Different minute values (name them); force-end as ⓐ
- **Recommendation:** ⓐ — a dog with a runner who never taps is the one state nobody else can close; 30/60 minutes match the existing late grace.

## Dropped by the planner

- **backend-logic-3** — Next wave: the owner 「공개 요청으로 전환」 door needs schedule.tsx (owned by fix/owner-schedule-report), an api.ts wrapper (contended by two slices already), a transition-booking action and a second parallel migration; the auto-release half is in the runner-rules letter. Build after the letter and after this wave's api.ts/schedule.tsx land.
- **backend-logic-11** — Low, edge-only dog-clash check in request/accept_reschedule; groups naturally with backend-logic-3 and -12 into one transition-booking slice next wave rather than a third builder on index.ts now.
- **backend-logic-12** — Low; same transition-booking slice as backend-logic-3/11 next wave (enroute CAS clears the proposal + one owner notice).
- **owner-journey-14** — chat.tsx is owned by the in-flight fix/chat-read-cursor builder; the one-line retention sentence (「모든 대화는 보낸 날부터 30일간 보관돼요」) should ride that branch's landing or the next wave — never two builders in one file.
- **ui-consistency-1** — ScreenHead is CREATED and applied to five screens in ui/safety-settings-chrome; converting the remaining ~14 inline headers (payments, addresses, review, rewards, compose, course, incident, report, chat, profile/edit, runner-profile, course-map, radar, pay, card-link, onboard/*) touches files owned by other slices and by held/in-flight branches — next wave, one screen per commit.
- **ui-consistency-3** — Secondary-button conversions (live chatBtn, calendar availBtn/emptyBtn, runner-profile editBtn, course-map clearBtn) sit in files owned by other slices or nobody; medium polish, next wave after the DESIGN.md table fix lands.
- **ui-consistency-6** — PaperSheet conversions need a new `ground` prop and touch owner/live.tsx, runner/run.tsx (owned by fix/runner-live-run this wave), shot/[bid].tsx (Codex lane); next wave once run.tsx is free.
- **ui-consistency-7** — Owned-file parts are folded into ui/safety-settings-chrome (settings/notification-settings/bank-account/safety/leaderboard) and fix/runner-home-requests (runner/home '#EEEEEE'); runner-profile and course-map remain — next wave with ui-consistency-1's second batch.
- **ui-consistency-10** — Gutter literals in owned files are fixed inside slices 8/9; the rest (cards, compose, addresses, review, chat, return-seal, course-map) ride the next chrome batch; low.
- **ui-consistency-5** — Sub-floor failure styles and the my/cards empty split are built in ui/my-fitness-floor and the leaderboard retry in ui/safety-settings-chrome; the finder's own conclusion was 'do not build StateLine' — nothing else to build.
- **less-is-more-10** — community.tsx is Sean's Codex sheets-lists lane (CODEX_BATCH, §B W3); the 「동네 신문」 kicker deletion and colophon trim are noted for that batch.
- **copy-hierarchy-2** — Low; the de-DE/fr-FR divergence is unreachable for a Banpo pilot on ko-KR/en-US, and the 29 sites sit in files owned by five other slices this wave. Next wave as one mechanical commit after those land.
- **runner-journey-13** — Letter (alerts.tsx repaint retires your 2026-08-01 arrivals-board pick; ARRIVALS/NEW is awaiting-sean item 19).
- **ui-consistency-11** — Letter (option B reverses the 09-22 announcer ruling; both options change a visible feature).
- **ops-notifications-6 (charge.ts emitter)** — The DORMANT_CLASSES labelling ships in fix/notification-landings-ops-inbox; the charge.ts notifyOps('charge_ladder_exhausted') emitter is a money-path edge change (0059 doctrine: autoplan + codex + functions deploy) and inert while payments_live_since is null — rides the slice that turns charging on.
- **backend-logic-1 (ops console client row)** — The api.ts wrapper + ops/index.tsx row for the new stranded-custody read wait for the RPC to land (be/custody-strand-sweep); the ops bell already lands as a card via ops-notifications-5. Next wave.
- **copy-hierarchy-1 (sites in files owned by other slices, club, shot, community, chat, dev)** — Converted where fix/alert-fold-copy owns the file; every other site is carried on the new shrinking ledger with its owning branch named, so the pin cannot lie about coverage. Club/shot/community sites go to Sean's Codex batch as a note.
- **less-is-more-9 / onboarding-first-run-9 / onboarding-first-run-10 / owner-journey-11 / owner-journey-12 / contract-gaps-3 / contract-gaps-4 / onboarding-first-run-14 / less-is-more-1 / less-is-more-17 / ops-notifications-1** — Merged into their parent findings by the dedup (see merged_from); handled through the parent's slice or letter.

## Refuted or unverified

- **owner-journey-9** — Retired palette worlds (swamp green, volt, beige) still ship on four owner core screens — The headline — that the schedule greens and volt are a retired palette — is contradicted by DESIGN.md:74-76 (Sean 2026-08-10, every screen's SEMANTIC colour system survives the paper migration and names schedule).
- **owner-journey-10** — Latin kickers and English status chips (retired app-wide by DESIGN §3b) remain on alerts, pay and me — Three of the finding's four load-bearing claims fail on the code and the law book (DESIGN §3b:189 retires the latin kicker as a section eyebrow while §2:90 keeps it as a type-world element); the residue is a polish item already queued as an open Sean decision.
- **contract-gaps-6** — `notifications` is in the realtime publication but nothing subscribes, and push.ts has no foreground — The mechanism is real (no postgres_changes subscription on `notifications` — api.ts:4310/4387/5764 are chat_messages, bookings, club_chat_messages; alerts.tsx:79-81 refreshes on focus and pull only), but the user-facing claim is false on the code: push.ts:292-296 arms `setNotificationHandler` with s
- **contract-gaps-8** — `debugReleasePayouts` wrapper calls an RPC the server revoked from authenticated in 0057 — the dev-l — The facts hold — `debugReleasePayouts` (api.ts:5614-5615, not :5607) wraps `club_debug_release_payouts`, which 0045:419 granted to authenticated and 0057:357 revoked from public/anon/authenticated with no later re-grant, and club-lab.tsx:163 is its only caller — but the screen is dev-only by constru
- **ui-consistency-9** — (i) Colours: retired forest/beige palette and sub-4.5:1 '#BBBBBB' text still painted on paper screen — The finding's unique claim is inverted and its proposed sweep would create the defect it describes. Every '#BBBBBB' text hit checked sits on a DARK ground: run.tsx:1361/1364/1417/1430/1446 are inside s.panel whose ground is paper.ink (run.tsx:1830) or the '#141414' sheet (1897); rewards.tsx:204-248 
- **ui-consistency-17** — Ops console (7 files) is internally split: ink vs coral primary, circle backs, gutter 11, rounded ca — The drift is real (ops/_layout.tsx:182-186, returns:294, gear:256, payout:457 ink primaries vs roster.tsx:434 coral; circle backs at ops/index:467, handoffs:141, gear:245, payout:425, returns/[bid]:277, returns/index:148, roster:404; gutter 11 at index:190; radius-16 cards at index:474-486), but the
- **ui-consistency-18** — Club client: five custom sheets on club/session and the LoadGate/ClubCta kit are consistent within t — Scope: club client work is assigned away from this wave. docs/decisions/awaiting-sean.md (2026-09-25 02:34 KST, Sean verbatim 'a, keep going') routes 'optional W4 club UI consistency' to Sean's Codex sessions and states 'Claude's finish-line wave stays out of the club world'. The observations are ac
- **less-is-more-4** — Runner-home 리워드 card stacks three progress meters and a pulsing ring — a celebration block competing — Duplicates a decision Sean already made, with no new fact. The file header records it: 「2026-08-11 Ⓑ① … Sean pick 'b1 + keep the rewards layout' … 리워드 카드는 Sean 지시로 현행 레이아웃 동결 (3중 진행계 포함 — 랩의 통합안 미적용)」 (home.tsx:47-48, repeated :1502). The finding re-proposes the lab's 통합안 he rejected. Two further er
- **less-is-more-6** — Owner home stacks five tinted DrawButtons (coral · paper · volt · lilac · blue) — the emphasis budge — Scope: the coda-tier question is already queued for Sean — awaiting-sean.md:1436 lists 「the draw-button §8 question (is a DrawButton title a display-font use? — blocks A③'s coda tier and font demotion, enh-owner-home-lab open question 4)」 — and the three coda buttons are his explicit 2026-08-20 pick
- **less-is-more-14** — Requests tab: a 새로고침 chip beside pull-to-refresh, and a footer that restates the confirm dialog — The colophon (requests.tsx:900-911) is Sean's verbatim instruction — 「I like b1 but also show them what's next」 (2026-08-24-sean-ui-club-commentary.md:40) — recorded in the code comment :901; the accept Alert (:387) shows NEXT_STEPS only after the runner has already pressed 수락, which is not 「show th
- **backend-logic-5** — Both late_booking_sweep and sweep_cancel_money_gaps are dark while ops_flags.late_protocol_live_sinc — Staleness. The finding's premise ('if production still reads NULL') was answered on 2026-08-25: REGISTRY.md row 0126 records the flip package EXECUTED via db push with the flag set at 05:34:06Z, a hand drain (tick1=1, tick2=0 — the 2026-08-04 booking resolved ceiling_backfill → incident_review) and 
- **backend-logic-7** — F3 remains: a payout_due operator can seat anyone in any class, including themselves in return_stran — Duplicate of a letter already on Sean's queue. docs/decisions/awaiting-sean.md item 0 carries it verbatim: '⚠ F3 is YOURS: a payout_due operator can seat anyone (incl. a non-party) into any class and self-seat return_strand (a settling door) — by construction of 0208; ⓐ accept (documented root) · ⓑ 
- **backend-logic-8** — Three party RPCs read (and two lock) the booking before the party gate: open_incident_tx, fetch_chec — Scope. The code shape is as described — open_incident_tx (0114:321-327), fetch_checkin (0117:1046-1050) and answer_checkin (0117:837-843) select by id, raise not_found, then not_party — but this is not a customer-facing gap: the oracle needs a v4 uuid to test, is available only to signed-in accounts
- **backend-logic-9** — expire_unmatched_bookings promises '전액 환불 처리돼요' when a confirmed payment exists, and nothing in the  — Unreachable state, by the finding's own evidence and re-verified: the only writer of payments.status='confirmed' is confirm-payment (handler.ts:186,286), the widget-prepay flow whose client wrapper (api.ts:764) has zero callers in app/ (grep for confirmPayment( finds only the definition); create-boo
- **backend-logic-13** — settle-run is still deployed and runner-callable with zero client callers — a second settlement door — The premise is false: settle-run has a live client caller. api.ts:1680-1687 settleRun() invokes 'settle-run', and app/app/club/run/[sid].tsx:385 calls settleRun() on the club host's run screen (plus dev/club-lab.tsx:290). The finding's grep saw only comments because it searched for the string 'settl
- **ops-notifications-11** — notifyOps still falls back to the OPS_PROFILE_ID env uuid — a recipient the roster screen cannot see — The code is as described (ops.ts:149-158 falls back to OPS_PROFILE_ID on zero roster rows, and recipientsFor:184-189 folds an RPC error into the same fallback), but the fallback is a DELIBERATE RULE recorded in docs/decisions/ops-profile-id-vs-admin-role.md — Sean's 2026-08-13 ruling: 'The env var s
- **onboarding-first-run-11** — The 샵 tab shows every owner, from day 1, a hard-coded catalogue with invented brand collaborations a — Facts hold (store.ts:227-233 six literal products with 「도그스하이 × 바잇미 / 페스룸 / 페티즌」 collab strings and prices; shop.tsx:228-232 the only disclosure is 「· 오픈 준비 중」; :246-261 renders the grid with 「N원 예정」; bottomnav.tsx:42 lists 샵 as OWNER_TABS[3]). But this is already queued for Sean: docs/decisions/awa
- **copy-hierarchy-5** — Runner money vocabulary: 수익 (tab/title) · 정산 · 지급 used as three names in one sentence — The three nouns are three different stages used by their meaning, and the cited line already reads as the finder's own recommended ladder. done.tsx:423 — '정산 기록이 저장됐어요 (settlement record saved) — 수익 화면에서 누적을 볼 수 있어요 (see the cumulative on the earnings screen) · 지급 일정은 결제 연동 후 안내드려요 (payout schedule 
- **copy-hierarchy-9** — 26 ASCII `...` in customer strings against 181 `…`; two loading/placeholder families in one screen — Every one of the 26 sites is already owned elsewhere, and a gate already encodes that ownership. test/copy-forms.test.cjs (on trunk, 30 pass / 0 fail via run-copy-forms-tests.sh) walks app/+src/ for ASCII `...` in Korean copy; its header (:1-30) records the 2026-09-23 normalisation of 53 screens, it
- **copy-hierarchy-11** — Date arithmetic retyped in four places outside kst.ts and the weekday array duplicated six times (ks — Not a customer-facing gap, by the finder's own sentence ('no customer sees a defect'), which is a stated refutation ground for this sweep. The facts are accurate — kst.ts:49 WD_KO is module-private, :53 kstMonthDay / :57 kstDateLabel are the house forms, and request.tsx:353, shot/[bid].tsx:56+377, a
- **copy-hierarchy-12** — Two server tokens rendered in parentheses inside Korean sentences on the host console — The strings exist exactly as cited (console/[sid].tsx:418-419, inside the doFinish refusal mapper fed to `run()` at :217-226, which shows the mapped Korean or else the raw message), but the file is app/app/club/console/[sid].tsx — inside `app/app/club/**`, which is Sean's Codex batch lane: finish-li

## Confirmed findings (91) — one line each

| id | sev | kind | title | product? |
|---|---|---|---|---|
| owner-journey-1 | high | logic-gap | A late or overrun run strands the owner from the handoff/return seal: the hero routes every late state to 내 일정, which has no door to /owner/ |  |
| owner-journey-2 | high | logic-gap | The hero's coral 「내 차례 · 인계하기」 frame fires AFTER the handoff is sealed (picked_up), while the real handoff moment gets a quiet gold 「티켓 보기」 | YES |
| owner-journey-3 | high | feature-gap | 「러너 직접 고르기」 on the request screen opens the leaderboard, which has no way to pick a runner — a dead end in the booking funnel |  |
| owner-journey-4 | high | logic-gap | A booking can be held with no pickup address (server accepts NULL), and 주소 관리 lets the owner delete the default/only address with no guard | YES |
| runner-journey-1 | high | logic-gap | Runner home swallows a failed jobs read — a runner holding a dog sees no ticket, no error |  |
| runner-journey-2 | high | logic-gap | Calendar sends a run-ended (귀가) job to the live run screen, and the run screen has no 'already ended' guard |  |
| runner-journey-3 | high | logic-gap | Receipt (done.tsx) asserts the wrong phase: '인계해 주세요' on settled runs, '러닝 화면에서 다시 정산' during the return ceremony, coral 다음 요청 while gated |  |
| runner-journey-5 | high | feature-gap | A runner cannot withdraw from a confirmed booking anywhere, and a dead confirmed booking occupies home forever | YES |
| runner-journey-7 | high | logic-gap | Opening the meetup screen IS the 출발 event — any tap on a confirmed job within 24h tells the owner '러너 이동 중' | YES |
| runner-journey-9 | high | logic-gap | start_run failure is swallowed twice — the runner runs for 30 minutes on a picked_up booking and only learns at 종료 |  |
| contract-gaps-1 | high | logic-gap | SOS push tap lands the owner on the run REPORT and the runner on the CALENDAR — not on any contact surface |  |
| ui-consistency-8 | high | ui-consistency | (h) Typography: 16 Korean styles at 14pt on 마이, 13 on fitness, failure text at 14 on payments — with comments still citing a '14pt floor' |  |
| ui-consistency-12 | high | ui-consistency | Screen: safety.tsx breaks 7 patterns — the 안심 센터 is the most off-system customer screen |  |
| backend-logic-1 | high | logic-gap | Custody states with no runner action have no exit: picked_up without start_run and active without end_run are swept by nothing, listed nowhe |  |
| ops-notifications-3 | high | logic-gap | Live-run owner pushes (러닝 시작 · N km 돌파 · 응가 완료 …) land on the post-run report, which renders an in-progress run as an unmeasured FINISHED ru |  |
| owner-journey-6 | medium | feature-gap | After a cancel, expiry or runner no-show the schedule sheet dead-ends at 「더 진행할 작업이 없어요」 — no re-book door, unlike the completed sheet |  |
| owner-journey-7 | medium | copy | Two permanent 「준비 중」 placeholders in the schedule booking sheet promise things the app does not ship |  |
| owner-journey-8 | medium | copy | Runner ranking is labelled 「AI 추천 순 / AI 1순위」 but is a fixed client formula (62 + runs×5, pace distance/4) | YES |
| runner-journey-4 | medium | feature-gap | A directed (지명) request can only be declined from home's front ticket — 요청함 has no 거절 door |  |
| runner-journey-6 | medium | logic-gap | 요청함 draws live 수락하기 doors while the work gate is shut; home already draws the blocked sentence |  |
| runner-journey-8 | medium | logic-gap | A booking in incident_review disappears from the home ticket and calendar while the dog may still be with the runner |  |
| runner-journey-10 | medium | logic-gap | Bare router.back() on return-seal and base-pin — dead ‹ and silent no-op on deep-linked single-entry stacks |  |
| runner-journey-11 | medium | logic-gap | Live run screen has no exit and, on re-entry, labels a server-active run '러닝 준비' |  |
| runner-journey-12 | medium | copy | Meetup 'waiting for owner' stage has no time anchor and no instruction |  |
| runner-journey-13 | medium | ui-consistency | alerts.tsx is still the lilac world: rounded cards, drop shadows, glass mast, five latin kickers | YES |
| contract-gaps-2 | medium | contract-gap | Six runner-bound booking titles have no RUNNER_ROUTES entry and all fall to /runner/calendar — money and case decisions land on a screen tha |  |
| ui-consistency-1 | medium | ui-consistency | (a) Screen header + back affordance: 1 majority header inlined on ~14 screens, 6 competing back-button shapes, 7 title sizes |  |
| ui-consistency-2 | medium | ui-consistency | (b) Primary CTA: PaperBtn on 30 screens vs 6 ink-face primaries, 9 hand-rolled coral lips, 3 lip-less/opacity/scale variants, 1 pale fifth c |  |
| ui-consistency-3 | medium | ui-consistency | (c) Secondary/tertiary actions: PaperBtn secondary (30) / quiet (4) vs 9 hand-rolled outline buttons in 4 border colours and 4 fills |  |
| ui-consistency-4 | medium | ui-consistency | (d) Section headers: 20/800 ink majority vs 17/900, 15.5/900, 16/800, 20/900 — and the §3b-retired '§ KICKER 한글' grammar still shipping on m |  |
| ui-consistency-6 | medium | ui-consistency | (f) Sheets: PaperSheet (pageSheet) on 3 screens vs 12 hand-drawn transparent slide Modals with the fake grabber PaperSheet's header names as |  |
| ui-consistency-7 | medium | ui-consistency | (g) Cards & rows: the retired rounded-card grammar (radius 16, '#fff', beige '#DCD6C4' border) survives on 12 screens; hairlines split paper |  |
| ui-consistency-11 | medium | ui-consistency | (k) Tab bar: 마이's children split — alerts/safety/cards keep the dock, settings/payments/notification-settings/profile-edit/addresses/dog/fit | YES |
| ui-consistency-13 | medium | ui-consistency | Screen trio: settings.tsx, notification-settings.tsx, runner/bank-account.tsx share one legacy grammar that breaks 5 patterns each |  |
| ui-consistency-14 | medium | ui-consistency | Screen: my.tsx (마이) breaks 5 patterns — sub-floor type, retired kicker grammar, dead sheet styles, 35 hex literals, opacity presses |  |
| ui-consistency-15 | medium | ui-consistency | Screen: leaderboard.tsx breaks 6 patterns — the retired forest/beige world intact |  |
| ui-consistency-16 | medium | ui-consistency | Screen: runner/home.tsx breaks 4 patterns on the runner's highest-traffic surface |  |
| less-is-more-7 | medium | ui-consistency | Shop shows five category chips that are not pressable but styled as an active filter |  |
| less-is-more-10 | medium | copy | Community masthead still says 「동네 신문」 and closes with an explanatory colophon the reader cannot act on |  |
| backend-logic-2 | medium | logic-gap | Sealed-but-unsettled rows tell both parties '담당자가 확인하고 있어요' while no operator is told and no console lists them |  |
| backend-logic-3 | medium | feature-gap | A nomination that is never answered runs until the start time and expires; the owner is never warned and has no door back to the open pool |  |
| backend-logic-4 | medium | logic-gap | Recurring generator re-notifies a debt-blocked owner on every hourly tick for up to 72h per occurrence, with copy that claims the series is  |  |
| ops-notifications-2 | medium | logic-gap | 「예약 시간이 지났어요 — 앱에서 응답해 주세요」 routes both parties to screens that have no check-in answer |  |
| ops-notifications-4 | medium | logic-gap | Pre-run owner titles (러너 매칭 완료 · 러너 재탐색 중 · 매칭 만료 · 일정 변경 수락/거절/만료) fall to the post-run report placeholder |  |
| ops-notifications-5 | medium | feature-gap | 7 of the 11 ops-addressed titles have no console door and are filtered OUT of the console's 운영 알림 list |  |
| ops-notifications-7 | medium | ui-consistency | Opening a notification never marks it read — only 「모두 읽음」 does, so NEW seals and the home bell count outlive the tap |  |
| ops-notifications-8 | medium | logic-gap | The console's 운영 알림 reads the newest 20 rows of EVERY kind, so an operator's own customer traffic pushes ops bells out of the list |  |
| onboarding-first-run-1 | medium | logic-gap | Owner display name is never collected; every Kakao owner is seeded as '사용자' and that word reaches runners, the community and club boards |  |
| onboarding-first-run-2 | medium | logic-gap | Runner home and 요청 tab tell an applicant who has ALREADY submitted an application to '지원하기' — the runner cannot see their status from either |  |
| onboarding-first-run-3 | medium | ui-consistency | Applicant runners get a working 온라인/오프라인 switch on home that changes nothing for them (requests.tsx deliberately omits it for exactly this r |  |
| onboarding-first-run-4 | medium | feature-gap | A refused push permission has no way back: nothing in the app detects OS-level 'denied', and 설정 › 알림's four switches look live while iOS del |  |
| onboarding-first-run-5 | medium | contract-gap | Login says 'by logging in you agree to 이용약관 and 개인정보 처리방침' but neither document is reachable anywhere in the app | YES |
| onboarding-first-run-6 | medium | onboarding | Runner types the same 동네 twice: onboarding asks 홈 베이스, the application form asks 활동 지역 with no prefill |  |
| onboarding-first-run-7 | medium | feature-gap | Application collects 감당 가능한 최대 체중 and 활동 반경, approval writes them to runners, and nothing ever reads them | YES |
| copy-hierarchy-1 | medium | logic-gap | 81 Alert.alert sites and 3 inline fail strips print raw (e as Error).message — PostgREST/Supabase English reaches the customer |  |
| owner-journey-13 | low | onboarding | A returning owner re-picks their role on every cold launch even though the server already knows it | YES |
| owner-journey-14 | low | copy | Chat footer promises retention 「러닝 종료 후 30일간」 but the purge is 30 days from message creation |  |
| owner-journey-15 | low | copy | Handoff screen foot advertises pet insurance under negotiation as a safety assurance | YES |
| runner-journey-15 | low | copy | Certification centre and onboarding overspend the word budget: latin kickers, colophon, footnotes, a '1 / 1' step counter |  |
| contract-gaps-5 | low | club-client | `session_reconsider_dog` is granted to authenticated with ZERO client callers — a host cannot reverse a dog rejection though the server supp | YES |
| contract-gaps-7 | low | ui-consistency | Two owner screens carry two different word tables for the same raw booking states |  |
| ui-consistency-5 | low | ui-consistency | (e) Loading / failure / empty faces: one sentence in 6 greys, '다시 시도' in 24+ private styles, empty copy at 14–17pt |  |
| ui-consistency-10 | low | ui-consistency | (j) Spacing: layout.gutter (15) on 14 screens vs 11 on 10, 16 on 7, 18 on 4, 12/14/20 elsewhere |  |
| less-is-more-2 | low | ui-consistency | Runner home prints the online state and its explanation twice within one viewport | YES |
| less-is-more-3 | low | ui-consistency | 러닝 가능 시간 has two homes and two 시간 조정 doors on the runner home | YES |
| less-is-more-5 | low | ui-consistency | Owner home (no booking) has five doors into the request screen; the 주간 목표 nudge is a plain duplicate of 미리 예약 | YES |
| less-is-more-11 | low | ui-consistency | 마이 gives owners two doors to /cards and a 예약 관리 row that duplicates the 내 일정 tab | YES |
| less-is-more-12 | low | ui-consistency | Passport chrome (마이 · 컬렉션) prints latin kickers and pseudo-data cells the customer cannot use | YES |
| less-is-more-13 | low | copy | 컬렉션 and the 마이 stamp page carry explanatory paragraphs over the word budget |  |
| less-is-more-15 | low | ui-consistency | 결제 관리 return state shows two equal-weight buttons; the return door should be the directed one |  |
| less-is-more-16 | low | ui-consistency | 안심 센터 spends a third of the screen on an informational 안전 체계 block the customer cannot act on | YES |
| less-is-more-18 | low | ui-consistency | 내 일정 renders 「＋ 새 러닝 예약하기」 under every agenda, duplicating the header ＋ |  |
| less-is-more-19 | low | copy | 수익 says 「지급 일정 미정」 twice and 주간 랭킹 promises an unbuilt 시즌 보상 | YES |
| backend-logic-6 | low | logic-gap | F6 remains: the operator's profile id still reaches both parties through bookings.return_force_evidence |  |
| backend-logic-10 | low | logic-gap | The nominated-accept path skips the applicant tier gate the open-pool path enforces; request_runner checks only that a runners row exists | YES |
| backend-logic-11 | low | logic-gap | accept_reschedule re-checks the runner's slot but never the dog's clash, so a reschedule can double-book the same dog |  |
| backend-logic-12 | low | logic-gap | A pending reschedule proposal survives the runner departing: nothing clears it and nobody is told it expired |  |
| ops-notifications-6 | low | logic-gap | The roster offers four alert classes that nothing emits — a chip an operator can turn on and never receive |  |
| ops-notifications-9 | low | feature-gap | 인계 멈춤 desk tells the operator to 「양측에 직접 연락해」 but carries no phone, no chat door, nothing to contact with | YES |
| ops-notifications-10 | low | copy | 운영 알림 toggle names 4 titles while silencing 11; the settings hint for the console names 2 of its 5 desks |  |
| ops-notifications-12 | low | logic-gap | Payment-failure pushes that say 「설정 > 결제 관리에서 …」 land on the run report instead of /payments |  |
| onboarding-first-run-8 | low | copy | Runner calendar's empty state sends an applicant to the 요청 tab, which then sends them to apply — a two-hop empty for the one tab that could  |  |
| onboarding-first-run-12 | low | copy | Owner 내 일정 empty state tells the customer to go back to home while its own 「＋ 새 러닝 예약하기」 button sits directly beneath |  |
| copy-hierarchy-2 | low | ui-consistency | Money numbers: 29 bare `toLocaleString()` price sites beside 12 `'ko-KR'` sites and two unused-by-most helpers (fmtWon, won) |  |
| copy-hierarchy-3 | low | ui-consistency | Alert titles use two grammars for the same event: noun `X 실패` (96) vs sentence `…지 못했어요` (16) — and the same failure is titled both ways on  |  |
| copy-hierarchy-4 | low | copy | The dog is named three ways in customer copy — 아이 (59), 반려견 (68), 강아지 (32) — sometimes inside one component | YES |
| copy-hierarchy-6 | low | copy | Owner writes a 후기, runner writes a 리뷰 — one feature, two names by role |  |
| copy-hierarchy-7 | low | copy | Ten 합니다체 sentence tails inside an otherwise uniform 해요체 product (2,042 요-endings, 0 반말) |  |
| copy-hierarchy-8 | low | copy | `해주세요` (181) vs `해 주세요` (44) spacing split — both forms in rpc-error.ts four lines apart |  |
| copy-hierarchy-10 | low | ui-consistency | Same primary action labelled 저장 on four screens and 저장하기 on two |  |
| copy-hierarchy-13 | low | copy | Question-form screen titles disagree on particle and person: 「어디서 만날까요?」 vs 「어디에서 출발하세요?」 |  |

## Slice briefs (paste-ready, verbatim from the planner)

### P1 · `fix/owner-hero-late-doors` · client

WHAT IS WRONG. app/src/components/home-hero.tsx:183-197 openNext runs `if (isLate) { router.push('/owner/schedule'); return; }` BEFORE the state switch, so once lateness() fires (31 min after scheduled_at for runner_enroute, or after km*8+25+30 min for active — app/src/lib/lateness.ts:120-156) the hero's only coral action lands on 내 일정, whose runner_enroute/handoff sheets offer only cancel (owner/schedule.tsx:1003, :1060). /owner/meetup is reachable only via home-hero:195 and notification-route:446, so a late-arrived runner strands the owner from the seal. Also :301 lateStrip prints 「러닝이 예정보다 N분 길어지고 있어요」 in the returning frame (:370-387) after the server recorded run_ended_at. Separately (owner-journey-2 PART 1 only): STATUS_MAP maps picked_up → 'handoff' (api.ts:1155) and picked_up means BOTH handoff stamps landed (transition-booking:426-427), yet the 'handoff' frame (:200 chip 내 차례 coral, :244 phrase 「지금 만나요 / {dog} 인계할 시간」, :354 sub 「러너가 도착했어요 · 만나서 인계해주세요」, :356 coral 「인계하기 · 아이를 넘기고 봉인해요」) tells the owner to hand over a dog they already handed over.

BUILD. (1) Extract the destination decision into a PURE module app/src/lib/home-hero-route.ts: `heroDestination(input: { state: HeroState; isLate: boolean; arrivedWaiting: boolean; resumable: boolean; bid: string | null }): Destination` where Destination is a string or {pathname, params}. Order of arms: state==='returning' → {pathname:'/owner/report', params:{bid}} FIRST; then state==='confirmed' && arrivedWaiting && resumable → '/owner/meetup'; then isLate → '/owner/schedule'; then the existing switch (handoff → '/owner/meetup', searching/directed → radar etc. — copy the current arms verbatim). openNext (:183) becomes `router.push(heroDestination({...}))`. Hoist `const arrivedWaiting = next?.rawStatus === 'runner_enroute' && !!next.arrivedAt` (currently :325) above openNext; `resumable = late?.resumable !== false`. (2) Confirmed button (:490-497 area, ground at :472): `const lateDoorClosed = isLate && !(arrivedWaiting && resumable)`; gate title/sub/ground on lateDoorClosed so the arrived-and-resumable case shows the same gold 「티켓 보기」 → meetup. Do NOT add coral here (that is Sean's Q4 letter). (3) Remove `{lateStrip}` from the returning branch only (:370-387); leave it in every other frame. (4) owner-journey-2 PART 1: in the chip ternary (:200) give 'handoff' its own row { c: GO_SAGE, t: '인계 완료' } (leave 'returning' on paper.action 내 차례); phrase ternary (:245) for 'handoff': { top: '인계 끝났어요', bottom: `${name} 곧 출발해요` }; sub (:354) → 「러너가 곧 러닝을 시작해요 · 시작하면 실시간 보기가 열려요」; the :356 DrawButton becomes non-coral (ground='gold') with label 「티켓 보기 · 인계 기록을 확인해요」 still routing to /owner/meetup (the SEALED block at meetup.tsx:762 is the honest landing). Keep all hero routing to meetup for arrived/handoff states; do not touch lateness.ts, owner/home.tsx, schedule.tsx, meetup.tsx (frozen stage machine).

DO NOT TOUCH: app/app/owner/home.tsx (held be/0211 owns it), app/src/lib/lateness.ts, app/app/owner/schedule.tsx (slice fix/owner-schedule-report), any meetup file.

ACCEPTANCE (observable): app/test/home-hero-route.test.cjs (register in app/package.json test chain ONLY if the chain is a file list — read it first; otherwise the runner globs) pins: returning+isLate → /owner/report with bid; confirmed+arrivedWaiting+resumable+isLate → /owner/meetup; confirmed+arrivedWaiting+resumable=false+isLate → /owner/schedule; confirmed+!arrivedWaiting+isLate → /owner/schedule; handoff+!isLate → /owner/meetup; searching → unchanged current destination. tsc 0; check-a11y-roles unchanged count for home-hero (no new bare Pressable); npm PASS grows by exactly the pins you add, FAIL 0. Grep: `grep -c lateStrip app/src/components/home-hero.tsx` shows the returning branch no longer references it (name the line numbers in your report). Sim: UNVERIFIED unless screenshotted.

PLANTS (each must redden a named pin, `&&`-chained, plant asserted): (a) move the `isLate → schedule` arm above the returning arm → returning+isLate pin reddens; (b) delete the arrivedWaiting arm → confirmed+arrived+isLate pin reddens; (c) control: a comment quoting the old `if (isLate)` line reddens nothing.

**Acceptance:** home-hero-route.test.cjs pins the five destination arms listed and passes; npm PASS delta equals pins added, FAIL 0, exit 0 · tsc 0; check-a11y-roles count for home-hero.tsx does not grow · returning frame no longer renders lateStrip (line cited in report); handoff frame chip reads 인계 완료 in sage, phrase 인계 끝났어요, button non-coral → /owner/meetup · owner/home.tsx, lateness.ts, schedule.tsx, meetup files untouched (git diff --stat names only the three owned files)
**Plants:** swap the isLate→schedule arm above the returning arm in heroDestination → returning+isLate pin reddens · delete the arrivedWaiting→meetup arm → confirmed+arrived+resumable+isLate pin reddens · control: a comment quoting `if (isLate) { router.push('/owner/schedule')` reddens nothing

### P2 · `fix/runner-live-run` · client

Ten runner-side defects that share run.tsx / calendar.tsx / meetup.tsx / done.tsx. Read each cited line before editing; api.ts is SHARED with fix/notification-landings-ops-inbox (they add two functions beside fetchNotifications ~:6288 / markAllNotificationsRead :6432) — touch only the runner-jobs region (:1544-1583, :1958-2050) and fetchBookingSync (:1491-1506).

1. runner-journey-2 + onboarding-first-run-8 (calendar.tsx). :129 openJob routes on rawStatus==='active' alone; :203-205 captions every in_progress job 「탭하여 러닝 화면 ›」; a run-ended job lands on the live run screen. Build app/src/lib/runner-job-route.ts (PURE): `runnerJobDestination(j: { bookingId: string; rawStatus: string; runEndedAt: string | null; ... })` = homewardReturnOpen(j) (import from custody-ping-policy.ts:44) ? {pathname:'/runner/return-seal', params:{bid}} : rawStatus==='active' ? '/runner/run' : '/runner/meetup'; plus `runnerJobCaption(j)` returning 「탭하여 반환 봉인 ›」 / 「탭하여 러닝 화면 ›」 / 「탭하여 인계 화면 ›」. calendar.tsx:126-130 and :203-205 use them (home.tsx:672-677 keeps its own copy this wave — another slice owns it). Empty state :371-380: load fetchMyRunnerStatus() beside jobs with the same tri-state as home.tsx:691; when loaded && tier null/'applicant' render 「인증이 끝나면 확정된 러닝이 여기에 떠요」 + 「인증 센터 ›」 → /runner/apply; unresolved/failed status keeps today's copy. Give the :376 Pressable accessibilityRole="button"; the a11y baseline line `app/runner/calendar.tsx 3` must drop to 2 — see CONFLICT note: fix/client-review-2 is rewriting that ledger; run `node scripts/check-a11y-roles.mjs`, edit the baseline minimally, and say so in the report.

2. runner-journey-9 (run.tsx :1096-1100, meetup.tsx :746-750). startRun does `startRunServer(bid).catch(() => {})` then setRunning(true) unconditionally; meetup swallows the pre-call. Change startRun to `await startRunServer(bid)` BEFORE setRunning(true); on failure `await h.stop(); setGps(false); handle.current = null;` keep running=false and surface through blockStrip (:1117) 「러닝 시작을 서버에 기록하지 못했어요 · 다시 시도」 (the :1626 CTA already re-runs beginRun). No 'already active' client special-case: the server returns {unchanged:true}. meetup.tsx:746-750: delete the swallowed pre-call, just router.replace('/runner/run'). Do not touch meetup's stage machine, polling or confirmHandoff.

3. runner-journey-2 (run.tsx hydrate :297-327): if the hydrated booking already carries run_ended_at, router.replace({pathname:'/runner/return-seal', params:{bid}}) instead of mounting the live run.

4. runner-journey-11 (run.tsx): in the strip lane (:1167) render a 40×40 ‹ Pressable (accessibilityRole="button", accessibilityLabel="뒤로", goBackOrHome from nav.ts:22) gated on `!running` so back never stops a live recording. Badge (:1354-1357) and CTA label (:1631): when `!running && (bookingWatch?.status === 'active' || startedAtMs.current != null)` say 「{dog}와 러닝 기록 이어가기」 / 「기록 이어가기」 instead of 러닝 준비 / 러닝 시작; beginRun unchanged.

5. runner-journey-12 (api.ts fetchBookingSync :1491-1506 + meetup.tsx :696-713). Add `runnerConfirmedAt: data.runner_confirmed_handoff_at ?? null` to BookingSync (keep the boolean). Waiting statusSub (:712): `${kstClock(kstCal(ms))}에 확인 요청을 보냈어요 · 보호자가 확인하면 러닝을 시작할 수 있어요` when the timestamp parses, else today's sentence; never print an invalid time (kst.ts only). Do not add a second chat door (chip at :525 exists).

6. runner-journey-10: return-seal.tsx:312 `router.back()` → goBackOrHome (import from '../../src/lib/nav'); base-pin.tsx:140 and :166 → goBackOrHome() (already imported :13).

7. runner-journey-3 (done.tsx). Add `runnerConfirmedAt: string | null` to Receipt (:95-109) from seal.runnerConfirmedAt (fetchReturnSeal api.ts:1737 returns it), null on the store fallback. Build app/src/lib/receipt-phase.ts (PURE): `receiptPhase({ paramBid, settled, rawStatus })` → 'estimate' | 'settled' | 'custody' | 'pending' exactly as: !paramBid ? 'estimate' : settled ? 'settled' : inCustodyPhase(rawStatus) ? 'custody' : 'pending'. Render per phase: settled → 「정산이 끝났어요」 + 수익 door (/runner/earnings); custody → the existing 인계 sentence (:371) + 반환 봉인 door → {pathname:'/runner/return-seal', params:{bid}}; pending → 「운영팀 확인 중이에요」, no stamp door; estimate → current store-path sentence. The :430 「러닝 화면에서 다시 정산」 strip renders ONLY in 'estimate'. The coral 「다음 요청 보기 ›」 (:565) renders only in 'settled' and 'estimate'; in custody/pending it becomes a quiet 홈으로 door.

8. runner-journey-8 (api.ts only for this slice — the home.tsx half belongs to fix/runner-home-requests): do NOT widen the shared IN_FLIGHT const (:1544 feeds owner reads, SERIES_UPCOMING, chat resolver). Add 'incident_review' to the runner-side filters only: fetchRunnerJobs (:1961 .in list), fetchInFlightRunnerJobs (:2039), fetchCurrentRunnerJobId (:1577) — map incident_review to status 'in_progress', keep rawStatus. runnerJobDestination: rawStatus==='incident_review' → runEndedAt ? return-seal : '/runner/run' (run.tsx:1186 already draws the incident banner).

9. copy-hierarchy-13: base-pin.tsx:182 '어디에서 출발하세요?' → '어디서 출발하세요?'. copy-hierarchy-6: runner/review.tsx:118 '후기 등록 완료', :214 '러너의 후기가 다음 러너를 지켜요', :321 '후기 남기기'; calendar.tsx:281/283 '후기 남기기'; done.tsx:559 '후기 남기기 ›'; api.ts:7033 '후기 기록을 확인하지 못했어요'. Comments and the reviews table name stay.

DO NOT TOUCH: runner/home.tsx, runner/requests.tsx (other slice), notification-route.ts/push.ts, IN_FLIGHT const, meetup stage machine/polling/confirmHandoff, availability predicates.

ACCEPTANCE: runner-job-route.test.cjs pins destination × caption for {confirmed, runner_enroute, picked_up, active+null, active+runEndedAt, incident_review+null, incident_review+ended}; receipt-phase.test.cjs pins the four phases incl. settled beating custody; runner-jobs-filters.test.cjs reads api.ts comment-stripped and asserts each of the three runner filters names 'incident_review' AND that IN_FLIGHT does not (control that the widening did not leak). tsc 0, all check scripts 0, npm FAIL 0 with PASS delta = pins added. Report the a11y baseline edit.

**Acceptance:** runner-job-route.test.cjs: seven status arms pin destination and caption; active+runEndedAt → return-seal with bid · receipt-phase.test.cjs: estimate/settled/custody/pending pinned; done.tsx renders the 러닝 화면에서 다시 정산 strip only in 'estimate' (line cited) · runner-jobs-filters.test.cjs: three runner filters contain 'incident_review', IN_FLIGHT const does not (comment-stripped source read) · run.tsx: startRun awaits startRunServer before setRunning(true); failure stops GPS and renders blockStrip text; hydrate with run_ended_at replaces to return-seal; ‹ door present and gated on !running · return-seal.tsx and base-pin.tsx contain zero bare router.back() (grep count 0) · tsc 0 · check-a11y-roles exit 0 with calendar line shrunk by 1 · npm FAIL 0, PASS delta explained
**Plants:** delete the homewardReturnOpen arm from runnerJobDestination → active+runEndedAt pin reddens · delete the 'incident_review' entry from fetchRunnerJobs' .in list → runner-jobs-filters pin reddens; add it to IN_FLIGHT → the control arm reddens · swap the settled/custody order in receiptPhase → settled-beats-custody pin reddens · control: a comment quoting `startRunServer(bid).catch(() => {})` reddens nothing

### P3 · `fix/runner-home-requests` · client

Runner home + 요청함 + 인증 센터. Read the cited lines first.

1. runner-journey-1 (home.tsx). loadJobs (:405-415) is Promise.all([fetchRunnerJobs(), fetchInFlightRunnerJobs()]).catch(console.warn) — jobs stays [] and a runner holding a dog sees a quiet day. Add `const [jobsErr, setJobsErr] = useState(false)` beside jobs (:400); setJobsErr(false) at the top of loadJobs and after setJobs; setJobsErr(true) in the catch (keep the warn). Render ONE strip in the 진행 중 slot before the 요청 대기열 SectionHead (:1152), guarded `{jobsErr && jobs.length === 0 && (…)}`: paper.criticalWash View, 15pt 700 paper.critical 「진행 중인 일을 불러오지 못했어요」 + Pressable 다시 시도 (accessibilityRole="button") → loadJobs.

2. runner-journey-8 home half (the api.ts filters are fix/runner-live-run's; assume fetchRunnerJobs now returns incident_review rows as status 'in_progress' with rawStatus 'incident_review' — code defensively so home is correct whether or not that slice has landed): STAGE.incident_review { label '담당자 확인 중', action '진행 화면으로 ›' }; include 'incident_review' in the `current` find list (:626); openJob (:668-677) routes incident_review by runEndedAt (set → return-seal with bid, null → /runner/run). Gate strip (:1132): say 「지난 러닝이 담당자 확인 중이에요」 only when gate.runEndedAt is set (RunnerWorkGate carries it, api.ts:1875), else 「진행 중인 러닝을 담당자가 확인하고 있어요」.

3. onboarding-first-run-2 + -3. Build app/src/lib/runner-application-copy.ts (PURE): `applicationLine(app: { state: string; canReapply: boolean } | null, tier: string | null): { lead: string; link: string }` mirroring my.tsx:160-166 (submitted/under_review → 심사 중 lead 「지원서 접수됨 · 운영자 확인 중」 link 「진행 상황 보기 ›」; approved; canReapply → 「다시 지원하기 ›」; null row → 「인증 센터에서 지원하기 ›」; else 「지원 결과 확인 ›」). home.tsx: load fetchMyRunnerApplication() (api.ts:2913) beside fetchMyRunnerStatus in the same focus-load, loud-fail preserved (a FAILED application read must not print 'go apply' — render the neutral 「인증 상태를 불러오지 못했어요 · 다시 시도」 line instead). Use it at :1378-1386 (preCert inbox block) and :1526-1527 (ladder line). Add a `preCert` arm to the :821 ternary so the 온라인 switch is NOT drawn for an applicant; render the application line in that slot. Leave api.ts:1186 `online: true` untouched (mint default is Sean's). requests.tsx:804-819 uses the same helper. my.tsx is another slice's — do not repoint it this wave; note it as a follow-up in the report.

4. runner-journey-4 + -6 (requests.tsx). Import declineBooking (api.ts:1583), fetchRunnerWorkGate + RunnerWorkGate (:1875). Add `declining: string | null` beside `accepting` (:144); busyAny = accepting ?? declining; the acting door is identified by (bookingId × action) so the non-acting door renders disabledFill with accessibilityState={{disabled:true}}, never opacity. In doorRow (:589) for `req.directed` only, render a ghost ink-outline 거절 door (never coral) through the same confirm Alert grammar home.tsx:1304-1315 uses, then load(). Gate: load gate/gateKnown in load() as an independent side load (mirror home.tsx:532-537); directly above the list, when gateKnown && gate && gate.gated, render the blocked sentence + the exit door home.tsx:1263 already draws; every 수락하기 door becomes disabledFill + accessibilityState disabled while gated, and accept() must not open the Alert while gated.

5. onboarding-first-run-6 (apply.tsx). On mount, only when no application row is shown and district === '' , call fetchMyDistrict() (api.ts:339) and setDistrict when non-null; field stays editable; a failed read leaves it empty and prints nothing; hint (:588-590) reads 「홈 베이스에서 가져왔어요 · 달라면 고쳐주세요」 only when the prefill landed.

6. runner-journey-15 + ui-consistency-4 (apply.tsx, onboard/runner.tsx): remove the masthead kicker 'RUNNER · CERTIFICATION' (:308) and its rule, the SERVER RECORD/RUNNERS strap (:344-345), the colophon (:767-770), SecRule's `en` slot (:780-792), StateStrap 'APPLICATION' (:803), the § secNo/secT pair beside secKo (:919-922; secKo stays 20/800); cut the lede (:311-312) to one line; delete recFoot (:380-383), stepLede (:390), stepFoot (:400) keeping the personal-data notice. onboard/runner.tsx:100 delete the '1 / 1' Text + its style. Copy/style only.

7. ui-consistency-16 + -7 (home.tsx): jobCta (:2034-2035) borderBottomColor → paper.actionPressed and pressed → translateY(3) + borderBottomWidth 1 (PaperBtn arithmetic), remove pressed96 from that one Pressable (:1070); replace the 24 '#EEEEEE' literals with lilac.hair (theme.ts:76, same value). LEAVE jobChat as the documented ink outline (:1078-1081 quotes Sean) and the lilac.accent state fills.

8. less-is-more-19 earnings half + ui-consistency-2 (earnings.tsx): reduce the tail note (:594-603) to 「지급되면 위 지급 내역에 남아요」; bankRegister (:661) ink face → <PaperBtn variant='primary'>. Leaderboard half is a letter — leave leaderboard.tsx alone.

DO NOT TOUCH: api.ts, my.tsx, calendar.tsx, availability predicates, the online mint default, jobChat.

ACCEPTANCE: runner-application-copy.test.cjs pins every state → sentence incl. null row and the failed-read guard (helper returns a neutral line for a sentinel 'error' input or the screen never calls it on error — pin whichever you build). grep '#EEEEEE' app/app/runner/home.tsx → 0. grep -c 'RUNNER · CERTIFICATION\|SERVER RECORD\|APPLICATION' apply.tsx executable lines → 0. check-a11y-roles: no bare Pressable added (decline door and retry door carry roles); baseline lines for home/requests may only shrink. tsc 0, npm FAIL 0, PASS delta = pins added.

**Acceptance:** runner-application-copy.test.cjs pins submitted/under_review/approved/rejected+canReapply/null → lead+link; a failed application read renders the neutral retry line, never 「지원하기」 · home.tsx: jobsErr strip renders with 다시 시도 when both reads fail; 온라인 switch absent for tier null/'applicant'; STAGE.incident_review present and in the `current` find list; gate strip copy keyed on gate.runEndedAt · requests.tsx: directed cards carry an ink-outline 거절 door; gated state shows one blocked sentence above the list and all 수락하기 doors disabledFill + accessibilityState disabled; accept() never opens the Alert while gated · apply.tsx: district prefilled from fetchMyDistrict when non-null; latin kickers/colophon/footnotes removed (grep counts 0); onboard/runner.tsx has no '1 / 1' · runner/home.tsx: grep '#EEEEEE' → 0; jobCta lip = paper.actionPressed, no pressed96 on it · tsc 0 · check-a11y-roles exit 0 (baseline only shrinks) · npm FAIL 0, PASS delta explained
**Plants:** delete the submitted/under_review arm in applicationLine → its pin reddens · make applicationLine return the null-row copy for the 'error' sentinel → the failed-read pin reddens · control: a comment quoting `.catch((e) => console.warn(` reddens nothing

### P4 · `fix/notification-landings-ops-inbox` · client

Every push tap must open a screen that can do what the body asks. Router is app/src/lib/notification-route.ts (RUNNER_ROUTES :255-278, owner default :448 `/owner/report?bid=`, RUN_STOP arm :407-411, club branch :412-414, CLUB_PROBE_TITLES :105, REFLESS_BOOKING_DESTINATIONS :234) and app/src/lib/push.ts (owner fast path :172-176, titles arg :80). Pin every new string against its WRITER file with the test's comment-stripped idiom (notification-route.test.cjs:267-270) — never against the router.

ROUTING (both roles unless stated):
1. contract-gaps-1 SOS. Extend destinationForBookingRef's `titles` to { incident: string; sos: string }; add `if (title === titles.sos) return { pathname: '/chat', params: { bid: refId } }` for BOTH roles, placed AFTER the club branch, and add 'SOS' to CLUB_PROBE_TITLES so a club delegation's SOS lands on /club/session/{sid}. push.ts: import SOS_TITLE from './api' (leave it defined there — notification-prefs.test.cjs:208 pins its source), pass { incident: INCIDENT_NOTI_TITLE, sos: SOS_TITLE } at :80, add `title === SOS_TITLE` to the owner fast path at :173. Pins: owner+runner 1:1 'SOS' → {'/chat', bid}; club 'SOS' → /club/session/{sid}; the string read from api.ts:sendSOS.
2. ops-notifications-3 live-run owner titles: OWNER_LIVE_RUN_TITLES = ['러닝 시작','응가 완료','간식 타임','수분 보충','새 사진 도착'] plus /^\d+km 돌파$/; needsCurrentBookingProbe true for them; in the owner section return '/owner/live' when isCurrentOwnerBooking === true else the report (mirror of the meetup arm :440-446; live.tsx self-resolves, no store write). push.ts:174 fast path must admit the family. Pin against start_run.ts:59 and api.ts EVENT_NOTI (:3131-3149). (report.tsx's in-progress arm is fix/owner-schedule-report's — do not edit report.tsx.)
3. ops-notifications-2: export CHECKIN_TITLE = '예약 시간이 지났어요'; RUNNER_ROUTES[CHECKIN_TITLE] = '/runner/home'; owner arm → '/owner/schedule' (after the runner branch, before OWNER_MEETUP). Pin against 0117:433-437.
4. ops-notifications-4 OWNER_PRERUN_ROUTES exact-title map consulted after the runner branch and before the meetup arm: '러너 재탐색 중' → {pathname:'/owner/radar', params:{bid}}; '매칭 만료' · '러너 매칭 완료' · '일정 변경 수락 ✓' · '일정 변경 거절' · '일정 변경 요청 만료' → '/owner/schedule' (NOT radar for 매칭 만료 — radar alerts-and-bounces on expired). Pin against transition-booking/index.ts:279/:359/:579/:587, 0080:941, 0021:18. The shared string 일정 변경 요청 만료 keeps the runner on the calendar.
5. contract-gaps-2 RUNNER_ROUTES: '정산을 확인하고 있어요' → '/runner/earnings'; '케이스 정산 결정' → '/runner/earnings'; '확인이 필요해요' and '지연 예약이 정리됐어요' → {pathname:'/chat', params:{bid}} via the RETURN_TITLES wrapping idiom (:420-431) AFTER verifying chat.tsx:44-47 does not refuse a no_show/incident_review thread (if it does, route to /runner/home instead and say so); '반환 완료' → add to CLUB_PROBE_TITLES (club writer 0070:378, no ledger row — NOT earnings); '예약 취소됨' → '/runner/calendar' explicitly. Pin each against 0201:547, 0152:230, 0117:790-803, 0070:378, cancel_owner.ts:229.
6. ops-notifications-12 OWNER_PAYMENT_TITLES = ['결제가 완료되지 않았어요','결제 처리 안내'] → { pathname: '/payments', params: { returnTo: `/owner/report?bid=${refId}`, returnLabel: '러닝 리포트로' } } (the params report.tsx:1213 already passes). Pin against charge.ts:483-493 and 0080:832-834.

OPS INBOX:
7. ops-notifications-8: api.ts is OWNED by fix/runner-live-run this wave — CONFLICT declared; add `fetchNotifications(opts?: { kind?: string })` with `.eq('kind', opts.kind)` when given, and `markNotificationsRead(ids: string[])` (update read_at = now() where id in ids and read_at is null; RLS 0002:139 permits) immediately beside fetchNotifications :6288 / markAllNotificationsRead :6432 and NOTHING else in api.ts; the orchestrator serialises the two branches. alerts.tsx call shape unchanged.
8. ops/index.tsx loadAlerts: fetchNotifications({ kind: 'system' }); drop the OPS_SYSTEM_TITLES title filter (:139); for each row `dest = destinationForSystemRef(...)` (notification-route.ts:146) — Pressable when non-null (as today), otherwise a plain View card showing title, BODY and time (the body is the operator's instruction); section hint 「누를 수 있는 알림은 해당 화면으로 가요」; empty-state sub 「최근 담당자 알림 20건 안에 없어요」. Row tap marks that row read via markNotificationsRead on success only (rowUnread cleared after the write resolves; on failure console.warn and leave it).
9. ops-notifications-7 alerts.tsx openNoti (:84): collect the tapped card's row ids (ALL ids of a collapsed handoff cycle — groupByHandoffCycle :106), fire markNotificationsRead, on success set those rows unread=false locally, on failure warn and leave the seal; then route. Keep 모두 읽음.
10. ops-notifications-6: ops-roster.ts export DORMANT_CLASSES = new Set(['charge_ladder_exhausted','charge_dispatch_stale','settled_without_payment','incident_waive_pending']) with a comment naming the measured emitter list; SELECTABLE_CLASSES = CLASS_ORDER stays (0208 contract, set pin untouched). roster.tsx renders those chips (rows + 운영자 추가 sheet) with a 「· 아직 발신 없음」 suffix. ops-roster.test.cjs: DORMANT_CLASSES ⊂ CLASS_LABELS, and a comment-stripped scan of supabase/functions + supabase/migrations asserting no `notifyOps(db, "<class>"` and no `ops_recipients_for('<class>'` literal exists for a dormant class (so the set must shrink when an emitter lands). Do NOT touch charge.ts.
11. ops-notifications-9 build-only: handoffs.tsx:77-80 lead → 「한쪽만 인계를 확인한 채 멈춰 있는 예약이에요. 이 화면은 보기 전용이에요 — 양측에는 확인 요청이 다시 보내졌고, 멈춘 인계를 어떻게 처리할지는 아직 정해진 규칙이 없어요.」 (drop the 직접 연락 sentence).
12. New app/test/ops-system-titles.test.cjs: OPS_SYSTEM_TITLES ⊆ the `_noti_ops_titles()` array read comment-stripped from 0214:156-175, so a console title cannot drift from the ledger.

DO NOT TOUCH: report.tsx, chat.tsx (fix/chat-read-cursor in flight), charge.ts, any migration, SOS_TITLE's home, any ops screen chrome (Sean-only styling).

ACCEPTANCE: notification-route.test.cjs grows by one pin per routed title per role listed above, each reading its string from the writer; hasNotificationRoute stays the synchronous twin (pin that every newly routed title returns true); deno is NOT required (no server change) but `cd supabase/functions && deno test --allow-all --node-modules-dir=auto _test` must stay 383/0 if you touch nothing there — state N/A. ops/index.tsx draws the 7 unlisted ops titles as non-pressable cards with body. tsc 0, npm FAIL 0.

**Acceptance:** notification-route.test.cjs: SOS (owner, runner, club), five live-run titles + Nkm 돌파, CHECKIN_TITLE both roles, six pre-run owner titles, six runner titles, two payment titles — each pinned against its writer file comment-stripped, each destination as specified · ops-system-titles.test.cjs: OPS_SYSTEM_TITLES ⊆ 0214 _noti_ops_titles() array (comment-stripped) · ops/index.tsx: kind='system' server-side filter; unlisted titles render as View cards with title+body+time; listed ones stay Pressable; row tap marks read on success · alerts.tsx: opening a card marks every row of its cycle read on success; NEW seal cleared only after the write resolves · ops-roster.test.cjs: DORMANT_CLASSES ⊂ CLASS_LABELS and the emitter scan finds no emitter for any dormant class; roster chips carry 「· 아직 발신 없음」 · tsc 0 · check scripts 0 · npm FAIL 0, PASS delta = pins added
**Plants:** delete the SOS arm → owner and runner SOS pins redden (owner falls to report, runner to calendar) · move the SOS arm ABOVE the club branch → club SOS pin reddens · change '매칭 만료' to route to radar → its pin reddens · add a fake title to OPS_SYSTEM_TITLES → ops-system-titles pin reddens · plant `ops_recipients_for('charge_dispatch_stale'` in a scratch copy of a migration under MIGRATIONS_DIR → emitter-scan pin reddens (never edit a landed migration; point the scan at a copy)

### P5 · `fix/owner-request-funnel` · client

The booking funnel's dead end, its missing address gate, and two honesty items. ⚠ owner/matching.tsx is also edited on the HELD branch be/0211-owner-proximity (distance bands); keep your edits to the mode/roster/nominate area so 0211's landing merges cleanly, and say in the report which functions you touched.

1. owner-journey-3. request.tsx:983 pushes '/leaderboard' from the 러너 row (직접 고르기 › / 다시 고르기 ›); leaderboard has nothing to tap and BoardRow carries no profile id — leave app/app/leaderboard.tsx untouched. matching.tsx: add `mode === 'pick'` (pre-booking) beside rebook (:181-182). In pick mode the roster loads fetchCertifiedRunners() (api.ts:1403, same LiveRunner shape the home shelf renders) instead of fetchAvailableRunnersFor(draft.bookingId) (:210) — do NOT unify with runners_available_for (the three availability predicates are frozen). nominate() in pick mode writes draft.preferredRunnerId / preferredRunnerName (the same fields runner-profile/[id].tsx:249 writes) and router.back()s to /owner/request (use goBackOrHome if the stack can be empty). Hide the rebook-only chrome in pick mode; the 「1순위」 ranking may still render. request.tsx:983 → router.push('/owner/matching?mode=pick').

2. owner-journey-4 (A) request.tsx only — (B) the address-delete guard is Sean's letter; do not edit addresses.tsx. pay() (:513-567) gates chargeLocked → dogs → scheduledAtIso → card and never pickupAddr; :590 sends `address_id: pickupAddr?.id`; the CTA ladder (:786-791) has no address arm while the 어디서 row says 「주소를 불러오지 못했어요」. Extract the ladder into a PURE app/src/lib/request-gate.ts: `requestBlocker(s: { chargeLocked: boolean; dogsState: 'loading'|'error'|'ready'; hasDog: boolean; addrState: 'loading'|'error'|'ready'; hasAddr: boolean; hasSlot: boolean; cardGate: ... }): { key: string; label: string } | null` reproducing today's order exactly, then adding two arms after the 반려견 arms and before 「시간부터」: addrState==='error' → { key:'addr-retry', label:'주소 확인 다시' } (pay() re-runs fetchAddresses; haptic('error') on failure and it stays error — a failed read is NEVER 「없음」); addrState==='ready' && !hasAddr → { key:'addr-first', label:'주소부터' } (pay() pushes /owner/addresses). The docked CTA and pay() both read requestBlocker. Add a useFocusEffect re-read of addresses in the shape of loadDogs (:280), keeping 'ready' while refreshing. Change the :1115 note to drop 주소: 「코스·러너는 지금 안 정해도 돼요 — 위에 보이는 그대로 접수돼요」.

3. owner-journey-8 (A) copy only (B is Sean's letter — keep the three bars): matching.tsx:358 「러너 {n}명 · AI 추천 순」 → 「러너 {n}명 · 추천 순」; :393 delete the clause 「— 1순위가 AI 추천이에요」; :493 'AI 1순위' → '추천 1순위'. No other matching copy.

4. onboarding-first-run-1 (index.tsx:74). Seed name as the first non-empty TRIMMED string among auth.user.user_metadata?.name, user_metadata?.full_name, user_metadata?.preferred_username, then auth.user.email?.split('@')[0], then '사용자' (keep `||` semantics for empty strings per the :70-73 comment). Add ONE `console.warn('[seed-name] keys', Object.keys(auth.user.user_metadata ?? {}))` in the INSERT branch only so the Kakao key gets measured on the first real session; say in the report that the chosen key is unmeasured. No onboarding field; profile/edit stays the correction path.

5. copy-hierarchy-7: request.tsx:443 and :1256 「…점검이 됩니다」 → 「…점검이 돼요」 (course-detail.tsx's copy is the copy slice's).

DO NOT TOUCH: leaderboard.tsx, addresses.tsx, api.ts, home.tsx, runner-profile, the slot picker PaperSheet, the card gate (:548-557), TOSS flags.

ACCEPTANCE: request-gate.test.cjs pins the ladder order incl. the two address arms and that addr error beats addr empty and slot; the pay() path for 'addr-retry' re-reads and never submits; the 러너 row pushes /owner/matching?mode=pick (grep); matching pick mode nominates without a bookingId and returns to request with draft.preferredRunnerId set (describe the manual sim check as UNVERIFIED if not screenshotted); grep -c 'AI 추천\|AI 1순위' matching.tsx executable lines → 0. check-a11y-roles baseline for request/matching only shrinks; tsc 0; npm FAIL 0.

**Acceptance:** request-gate.test.cjs pins the full ladder order; addr-error → '주소 확인 다시', addr-empty → '주소부터', both after dogs and before slot; a ready+hasAddr state yields the existing labels unchanged · request.tsx:983 pushes '/owner/matching?mode=pick'; matching.tsx has a 'pick' mode that loads fetchCertifiedRunners and writes draft.preferredRunnerId/preferredRunnerName then returns; no runners_available_for change · pay() never sends address_id undefined when addrState is 'error' or the list is empty (line cited); focus re-read of addresses present · matching.tsx executable lines contain no 'AI 추천' / 'AI 1순위'; the three bars untouched · index.tsx seed reads user_metadata name/full_name/preferred_username before the email prefix; one measured-keys warn in the INSERT branch · tsc 0 · check scripts 0 · npm FAIL 0, PASS delta = pins added
**Plants:** delete the addr-error arm from requestBlocker → its pin reddens (state falls through to '주소부터' or '시간부터') · swap the addr arms above the dog arms → order pin reddens · control: a comment quoting `router.push('/leaderboard')` in request.tsx reddens nothing and the grep for the executable push is 0

### P6 · `fix/owner-schedule-report` · client

내 일정 and 러닝 리포트. Read the current line numbers first (schedule.tsx moved: terminal branches ~1064-1078, footer :714-716, header ＋ :587-589, empty text :687-688; report.tsx STATUS_LABEL :92-95, placeholder :690-701, record card :850, 「기록 없음」 :934-936/:1325).

1. owner-journey-6 (schedule.tsx). Hoist the completed branch's rebook onPress (prefills draft.km/pace/runner) into ONE `rebook()` closure over `selected`; render ONE rebook Pressable (label 「⟳ 이대로 다시 예약」, accessibilityRole="button") in three places: completed (unchanged), `selected.status === 'cancelled'` (covers cancelled_owner · cancelled_runner · expired · refund_pending via STATUS_MAP), and the `rawStatus === 'no_show'` half of the no_show/incident_review branch under the existing state sentence. NOT in incident_review. Use PaperBtn variant='secondary' if it fits the sheet; otherwise the existing Pressable grammar.

2. owner-journey-7 map plate (schedule.tsx:777 area). The 110pt 「코스 지도 준비 중」 box renders on every sheet unconditionally. Replace with request.tsx's three-way split: per-sheet effect keyed on selected.routeId → fetchRouteById (api.ts:311) with an alive guard into {loading|ready|error}; render nothing when routeId is null (name line already says 코스 미지정), 「불러오는 중」 while loading, a loud failure with 다시 시도 on error, HeatTrace when ready. DO NOT touch the 바디캠 line (:1019-1021, Sean D3=B).

3. less-is-more-18 + onboarding-first-run-12: delete the unconditional footer Pressable (:714-716) and s.emptyCta (:1395-1399); the header ＋ (:587-589) stays the shortcut. Empty-state first branch (:687-688) → the single fact 「예정된 러닝이 없어요」 (the 「홈의 지금 찾기…」 clause goes; the header ＋ is the direction). If you keep any footer for the empty state it must carry accessibilityRole="button" and must not repeat the 홈의 instruction. The a11y baseline line `app/owner/schedule.tsx 16` shrinks accordingly — ⚠ fix/client-review-2 is rewriting that ledger format; run the gate, edit minimally, report it.

4. contract-gaps-7 + ops-notifications-4 label: new PURE app/src/lib/booking-state-copy.ts `bookingStateLabel(rawStatus: string): string | null` — ONE table keyed by RAW status, schedule.tsx's words as canon (:61-84 incl. no_show '불발', incident_review '확인 중', expired '매칭 만료'), plus every state report.tsx:92-95 names, plus cancelled_owner/cancelled_runner/refund_pending. report.tsx:693 reads it instead of STATUS_LABEL (delete STATUS_LABEL); schedule.tsx STATUS_STYLE/stFor keep their colours and take `.label` from it by b.rawStatus. A stale 매칭 만료 inbox row must caption 「매칭 만료」, not 「진행 상황 확인 중」.

5. ops-notifications-3 report half (report.tsx): when `run && run.endReason == null && report.status === 'active' && !seal?.runEndedAt`, render the `report && !run` placeholder box (:693) with label 「러닝 진행 중」 and a <PaperBtn variant='primary' label="실시간 보기 ›"> → router.push('/owner/live') INSTEAD of the record card — never print 「기록 없음」/「달성률을 계산할 수 없어요」 for a run in progress. Extract the predicate as `reportShowsInProgress(...)` into booking-state-copy.ts so it is pinnable.

6. ui-consistency-2 in owned files: report.tsx rebook/rebookLip/rebookDown (:1806-1811, used :1099/:1131) → <PaperBtn variant='primary'> (delete the orphan styles); schedule.tsx primaryLip/primaryDown (:1437-1440, used :1034/:1094) → PaperBtn primary. LEAVE 「실시간 보기」's pale face (:1015-1018, documented in-code as a status colour) and flag it in the report.

DO NOT TOUCH: 바디캠 line, CheckinAnswer mount (:782), api.ts, live.tsx, home.tsx.

ACCEPTANCE: booking-state-copy.test.cjs pins every raw status → label (incl. expired → 매칭 만료, no_show → 불발, incident_review → 확인 중, picked_up → 인계 완료 · 시작 대기) and reportShowsInProgress for {active+runs row+null endReason+null runEndedAt} = true and {completed} / {active+runEndedAt} = false. grep -c '준비 중' schedule.tsx executable lines = 1 (the 바디캠 line only). grep -c "홈의 지금 찾기" schedule.tsx → 0. STATUS_LABEL no longer exists in report.tsx. check-a11y-roles exit 0 with the schedule line shrunk; tsc 0; npm FAIL 0.

**Acceptance:** booking-state-copy.test.cjs pins the raw-status table (report and schedule read the same words) and reportShowsInProgress true/false cases · schedule.tsx: rebook door present on completed, cancelled (all four raw states) and no_show; absent on incident_review; footer ＋ 새 러닝 예약하기 gone; empty copy is the single fact · schedule.tsx: route card is {loading|ready|error|null} bound to fetchRouteById; 「코스 지도 준비 중」 no longer exists; 바디캠 line untouched · report.tsx: an active run with a runs row and no run_ended_at renders 러닝 진행 중 + 실시간 보기 PaperBtn, not the record card; 매칭 만료 row captions 매칭 만료 · hand-rolled rebook/primary lips replaced by PaperBtn; orphan styles deleted; 실시간 보기 pale face left and flagged · tsc 0 · check-a11y-roles exit 0 (schedule line shrinks) · npm FAIL 0, PASS delta = pins added
**Plants:** delete the `expired` row from bookingStateLabel → its pin reddens · invert the runEndedAt conjunct in reportShowsInProgress → the active+runEndedAt=false pin reddens · control: a comment quoting `코스 지도 준비 중` reddens nothing and the executable grep stays at 1 (bodycam)

### P7 · `be/custody-strand-sweep` · server · migration

Server-only, no status moves, no client (the ops console list row for the new class is next wave; the ops bell lands in the console as a card once fix/notification-landings-ops-inbox lands). Claim numbers four-sided at WRITE and COMMIT time (0196/0197 reserved, 0204/0211 held, 0221/0222 in flight on be/0221-*, be/0222-*; suites 247+ are Claude's). Redeclare from the LATEST catalog body (`select prosrc from pg_proc` on the harness after apply, or the highest declaring file) — never edit a landed migration. Every definer: `set search_path = public, pg_temp` in-body and revoke/grant restated in this file. You may split into two consecutive migrations (sweep+gate+list vs recurring+F6) if review size warrants; say which.

A. backend-logic-1 — picked_up without start_run and active without end_run are swept by nothing (arms key on run_ended_at: 0201:531/633/739, 0206:172; c_dead names them 0201:485-487/821; late_booking_sweep only confirmed/runner_enroute 0126:78/105; work gate 0092:112-117 fires only on run_ended_at or incident_review; force_return_tx 0205:256-257 and ops_resolve_return_tx 0201:177-178 raise run_not_ended). Build: (1) new arm g in sweep_run_end_recovery (redeclare 0201's body): status='picked_up', greatest(handoff stamps) older than `ops_flags.custody_start_strand_minutes`, no runs row → runner once 「러닝을 시작해주세요」 (kind booking, ref booking), owner once; and status='active' with run_ended_at null and runs.started_at + (km*8+25 min) older than `ops_flags.custody_end_strand_minutes` → runner once 「러닝을 종료해주세요」, owner once; then bell ops_recipients_for('return_strand') once by title with a NEW ledgered title (e.g. '러닝 진행 좌초 — 확인 필요', kind system, ref booking, no amounts) using arm f's not-exists idiom. Both threshold columns ship NULL = arm off, exactly as return_strand_minutes does (0201:503-512) — the values are Sean's letter. Add the title to _noti_ops_titles() (redeclare 0214's) and to 0210-O2's array in suite 241. (2) Widen _runner_work_gate_blocking (redeclare 0092's, ACL restated) to also match status='picked_up' or (status='active' and run_ended_at is null) — Sean's 2026-08-13 ruling applied literally; check every caller of the gate (0092, index.ts:221-236 accept path via runner_work_gate) and confirm the client copy in runner/home.tsx:1132 and requests still reads truthfully (report, do not edit client). (3) Add both shapes to ops_stranded_returns or a sibling `ops_stranded_custody()` with the same party/roster gate order as 0206 (party/roster gate before the read) and the same flat whitelisted return. Leave 「may ops force-end a run the runner never stopped」 as the letter.

B. backend-logic-2 — arm a (0201:527-552) tells both parties 「담당자가 확인하고 있어요」 while no operator is told. Inside arm a's one-shot block insert one 'system' row per ops_recipients_for('payout_due') with a new ledgered title ('정산 미완료 — 확인 필요', ref booking, no amount per 0084 §E), one-shot by title; add to _noti_ops_titles() and 0210-O2's array. Add a sealed_unsettled arm (settlement_ready_at not null, status active, no ledger row) to the stranded list read so the console can show it; document the pricing re-drive path (0083 §0f) in the migration header — no settle button.

C. backend-logic-4 — generate_recurring_bookings (0180 body, ACL restated) re-notifies '반복 예약 일시 중지' every hourly tick for 72h. Guard the insert (0180:177) with `not exists (select 1 from notifications where profile_id = s.owner_id and title = '반복 예약 일시 중지' and created_at > now() - interval '24 hours')` (ref_id is null on this row, match by title+profile); keep v_notified for the intra-tick guard. NO copy change (the body already says it resumes when the debt clears).

D. backend-logic-6 F6 — ops_resolve_return_tx (0201 body) writes 'resolved_by' into party-readable bookings.return_force_evidence (0201:202-209). Redeclare dropping 'resolved_by' from the jsonb_build_object (the journal insert :212 keeps it; 0218's CHECK requires it there), ACL restated; one-shot idempotent scrub `update bookings set return_force_evidence = return_force_evidence - 'resolved_by' where return_force_evidence ? 'resolved_by'`. Pin in suite 230's family: after a resolve the blob has no resolved_by key, the journal row does; scrub idempotent.

DO NOT TOUCH: any landed migration, IN_FLIGHT client consts, edge functions (run deno anyway: sweep_run_end_recovery and the gate are called from edges? verify; deno baseline 383/0).

ACCEPTANCE: harness pass delta = pins added, 0 fail; `grep -E 'pass / '` from the full log (never tail alone); pg_ctl stopped. Pins pin PROPERTIES: (A1) a picked_up row with stamps older than the threshold and no runs row yields exactly one runner row + one owner row + one ops row per recipient across TWO ticks (delta counted, not state found); threshold NULL → zero rows; (A2) active+run_ended_at null past duration threshold → same; (A3) work gate returns blocking for a picked_up row and for active+null run_ended_at, and NOT for active+run_ended_at set (that is the existing arm — keep its pin green), NOT for completed; (A4) ops_stranded_custody lists both shapes, refuses a non-roster caller (party gate before read: a stranger gets not_found/empty, never a row); (B1) after SEAL_ALARM_AFTER the payout_due recipients hold exactly one system row per booking across two ticks; (B2) the title is in _noti_ops_titles() and 0210-O2's array (fixture starts where production starts: the roster row must exist BEFORE the tick — absence pin over an empty roster proves nothing); (C1) two ticks in one hour → one row, a tick 25h later → a second row (advance the clock by updating created_at, delta measured); (D1) blob has no resolved_by, journal has it, scrub twice is a no-op. Comment-strip prosrc in every source pin; assert exact booleans (`is distinct from true`), never a bare IF.

**Acceptance:** harness: 0 fail, pass delta equals the pins added; full-log grep, pg_ctl stopped; deno 383/0 or N/A stated with reason · A1/A2: strand arm emits exactly one row per party and one per ops recipient across two ticks; NULL threshold emits zero (delta measured against a control tick) · A3: _runner_work_gate_blocking true for picked_up and for active+run_ended_at null; existing arms' pins stay green; completed false · A4: stranded-custody read refuses a non-roster caller before reading the row; lists both shapes for a roster member · B1/B2: sealed-unsettled bells payout_due recipients once; new titles present in _noti_ops_titles() and suite 241's 0210-O2 array; fixture seeds the roster before the tick · C1: recurring block notification once per 24h per owner (two ticks → 1, +25h → 2) · D1: return_force_evidence has no resolved_by after resolve; journal row carries it; scrub idempotent; suite 230 family pin · every definer sets search_path in-body and restates ACL in this file; check-definer-acl exit 0; REGISTRY row pushed in the same commit as the file
**Plants:** remove the `not exists` one-shot from arm g → A1's two-tick delta pin reddens (2 rows) · delete the picked_up conjunct from _runner_work_gate_blocking → A3 reddens; delete the run_ended_at-is-null conjunct → the active arm of A3 reddens alone · remove the roster gate from ops_stranded_custody → A4's stranger pin reddens · delete the 24h not-exists guard in generate_recurring_bookings → C1 reddens (2 rows in one hour) · re-add 'resolved_by' to the jsonb_build_object → D1 reddens · control: set custody_start_strand_minutes NULL → A1 emits zero and its NULL-off pin stays green; control: a comment quoting `'resolved_by', p_actor` reddens nothing (prosrc comment-stripped)

### P8 · `ui/safety-settings-chrome` · client

Five off-system customer screens onto the paper grammar, plus the shared header component and the stale DESIGN.md button table that keeps producing ink primaries. Copy is unchanged except where named. Chrome only; no data/route changes except the notification-denied strip.

0. app/src/components/ui.tsx: add `ScreenHead({ title, onBack, right? })` encoding the majority header (payments.tsx:143-148): Row space-between · 40×40 square back, paper.canvas fill, 1px paper.line border, '‹' 20.5 paper.ink, accessibilityRole="button" accessibilityLabel="뒤로", onBack defaults to goBackOrHome · title 23/900 paper.ink with explicit lineHeight 28 · 40-wide trailing slot (or `right`). Apply it ONLY to the five owned screens this wave (the other 14 inline copies are next wave). DESIGN.md: under 'Screen title' add one paragraph: pushed sub-screens use the 23/900 chrome header (ScreenHead); the 30/900 display title stays for tab/ceremony screens. Fix DESIGN.md:120 and :203 to paper.action / actionPressed (the shipped matrix, paper-btn.tsx:1-9) — one sentence noting the 2026-08-11 retirement.

1. safety.tsx (ui-12, high — the emergency screen): ScreenHead (replace the s.bell circle :293) · sections 20/800 paper.ink with coral rules, inline `{fontSize:20,lineHeight:25,fontWeight:'800',color:paper.ink}` (ui/my-fitness-floor adds theme.secTitle; repoint later) · radius 0 everywhere, no card fills: card (:313), callBtn (:311), miniBtn (:315), addRow (:316), input (:317), sosCard radius 0 (:300) — keep sosCard's hand-rolled lip (already paper.actionPressed, :305-309) · saveBtn (:318, used :187) → <PaperBtn variant='primary' busyLabel='저장 중…'> · the 112/119 callBtns (:130,:135) → <PaperBtn variant='secondary'> · the tel/remove miniBtns (:173,:176) gain accessibilityRole="button" · replace '#DCD6C4'/'#cfd8c2'/'#faf9f3'/'#f4f2ea'/colors.dim with paper tokens; shieldChip volt → paper.wash + ink glyph; root colors.cream → paper.canvas; padding 16 → layout.gutter; loading line colors.dim → paper.dim. less-is-more-16 build-only part: delete the subtitle :111 (the 안전 체계 block stays — Sean's letter). Baseline line `app/safety.tsx 7` must shrink to the count you leave (⚠ fix/client-review-2 is rewriting the ledger format — run the gate, edit minimally, report).

2. settings.tsx / notification-settings.tsx / runner/bank-account.tsx (ui-13): ScreenHead · layout.gutter (11 → gutter) · paper.canvas root (colors.cream) · radius 0 with coral-rule sections instead of cards (card :300/:182/:268; failStrip radius 0 keeping the critical strip grammar) · section 17/900 → 20/800 inline values as above · bank-account primary (:284-298, cites the stale DESIGN table) → <PaperBtn variant='primary'>, 계좌 삭제 → <PaperBtn variant='destructive'>, bankChip/input radius 0 · leave the __DEV__ club-lab card (settings.tsx:263-264) alone. ops-notifications-10: settings.tsx:209 console hint → 「운영 대기열과 담당자 알림을 여기서 처리해요」.

3. notification-settings.tsx (onboarding-first-run-4): read readPushPermission() (push.ts:251) on focus and on AppState 'active' (idiom onboard/runner.tsx:43-49); when 'denied' render a loud strip ABOVE the switches 「기기 설정에서 알림이 꺼져 있어요 — 설정 열기 ›」 → Linking.openSettings(). For 'undetermined' do NOT call requestPushPermission from this screen (push.ts:264-268: the primer's button is the only caller) — present the existing NotificationPrimer component instead, and amend the push.ts comment ONLY IF you own it — you do not; note it in the report for the orchestrator.

4. leaderboard.tsx (ui-15, ui-5): ScreenHead · layout.gutter · radius 0 on milesCard (keep its ink plate and '#8fa093' dim-on-dark), tabWrap, emptyBox, failStrip, row, bib · '#DCD6C4'/'#EDE8DA'/'#d8d2c0' → paper.line or '#EEEEEE' by role, emptyBox '#f4f2ea' → canvas + '#EEEEEE' border, colors.dim → paper.dim on white · retryBtn → the underlined critical retry the failStrip family uses. Keep podium art. Do NOT change the :176 시즌 보상 sentence (Sean's letter).

DO NOT TOUCH: ops/*, club/*, my.tsx, payments.tsx, push.ts, tab-parent.ts, alerts.tsx, any data or route.

ACCEPTANCE (measurable): per owned file, `grep -cE "borderRadius: *(1[0-9]|2[0-9]|99)"` → 0 except the documented milesCard/podium exceptions you list; `grep -c '#DCD6C4'` → 0 in all five; safety.tsx has no volt fill; check-a11y-roles exit 0 with safety line shrunk by ≥4; tsc 0; npm FAIL 0 (no pins expected — say so; a ScreenHead source pin in an existing ui test is welcome if cheap). Sim screenshots or UNVERIFIED.

**Acceptance:** ScreenHead exported from ui.tsx and used on safety, settings, notification-settings, bank-account, leaderboard; DESIGN.md :120/:203 read paper.action/actionPressed and a 'chrome header' paragraph exists · grep '#DCD6C4' across the five screens → 0; borderRadius ≥10 count → 0 except listed dark-artifact exceptions; roots paper.canvas; gutters layout.gutter · safety.tsx: saveBtn and 112/119 are PaperBtn; miniBtns carry accessibilityRole; subtitle :111 gone; 안전 체계 block and sosCard lip unchanged; a11y baseline line for safety shrinks · notification-settings.tsx: OS 'denied' renders the 설정 열기 strip via Linking.openSettings; no requestPushPermission call added (grep 0) · bank-account primary is PaperBtn primary, delete is PaperBtn destructive; settings.tsx:209 hint reworded · tsc 0 · check-a11y-roles exit 0 (only shrinks) · npm FAIL 0
**Plants:** add a bare <Pressable> to safety.tsx → check-a11y-roles exit 1 naming the line (gate control, then revert) · control: no pin is claimed for chrome; state UNVERIFIED for every visual claim without a screenshot

### P9 · `ui/my-fitness-floor` · client

Sean's 2026-08-25 ruling: Korean prose ≥15pt (DESIGN.md:145-150; Korean never rides the kicker exemption). The 마이 hub and 체력 page are below it almost everywhere, including their failure text. Type/style/copy only; no data or routing.

1. theme.ts: export `secTitle = { fontSize: 20, lineHeight: 25, fontWeight: '800' as const, color: paper.ink }`.

2. my.tsx (ui-8, ui-14, ui-4, ui-5, less-is-more-12 build part): raise every Korean style at 14 to 15 with lineHeight ≥19 — microK 530, roleTagTxt 532, fldK 543, fldVSmall 545, fldV2 546, idEditTxt 552, recU/recL 576-577, recGo 579, recFailTxt/recFailRetry 587-588 (→ 15/700 and 16/800 underline, the failStrip family), secKo 598, visaFootG 615, emptT/emptD 617-618 (→ 16/700 and 15, matching cards.tsx:406-407), drowTitle/drowDesc 629-630, btnRoleSwTxt 653, signoutTitle/signoutSub 660-661; delete the stale '14pt floor' comments (528-529, 532, 543, 552, 653). microK/fldK latin halves ('IDENTITY', 'NAME'): drop the latin word rather than enlarging it where the Korean label stands alone. Delete secNo/secT (596-597) and the '§ STAMPS'/'§ DOCUMENTS' rows' latin parts (396-399, 433-436); render secKo via theme.secTitle (§3b: Korean 20/800 + coral rule). Delete the four dead styles handle/fieldLabel/input/saveBtn (670-677, zero JSX uses — confirm with grep before deleting). Replace raw hexes that equal tokens: '#C6472C' → paper.action (539), '#586055' → colors.dim, '#EFF1EC' → colors.clay (206). LEAVE the scale(0.96) presses (they are the sanctioned unfilled press, DESIGN.md:216), the passport masthead/PASSPORT tag/TYPE/NO. grid/MRZ (Sean's letter), and stamp/passport artwork sizes ruled as glyphs (DESIGN.md:255-257). Empty stamp paragraph (411-415) → headline + one ≤12-word line 「첫 러닝을 완주하면 왼쪽 위 칸부터 찍혀요」 — must not reintroduce a permanence claim (comment :412).

3. owner/fitness.tsx (ui-8, ui-5): raise the 14pt Korean styles to 15 / lineHeight 20 — fitFailTxt/fitFailRetry 508-509, mastSub 522, filmNote 526, filmRetryTxt 528, emptyCopy 551, bandLabel 554, bandCap 560, punchLabel 570, statLine 572, cardSub 580, ageGate 583, goalHint 592, barVal/barX 600/603; sect (607) → theme.secTitle. ⚠ FROZEN hero architecture (CLAUDE.md DO-NOT-REFACTOR): fitness.tsx:40-43 derives HEAD_H from mastSub/bandLabel/punchLabel lineHeights — move those constants by EXACTLY the lineHeight delta in the same edit; no layout animation, no backgroundColor animation, no ring-layer change. Show the arithmetic in the report.

4. payments.tsx (ui-8, less-is-more-15): note (308) 14.5 → 15, failTxt (310) → 15/700; the return door (:221-226, gated on returnTo) → variant='primary'; 「카드 바꾸기」 (:217-219) stays secondary.

5. cards.tsx (ui-4, less-is-more-13): delete secNo/secT and the '§ STAMPS'/'§ PATCHES' latin parts (168-173 ×3, 230), secKo → theme.secTitle (lilac.head allowed on cards per finding); delete subNote (123); empty state (213-216) → headline + the same ≤12-word line; KEEP wellNote (286-288) — it is the only legend for the colour worlds and the grade ladder for locked patches. MILESTONE strap (206) stays (letter).

6. shop.tsx (less-is-more-7, ui-4): delete the CATS constant (:33), the chip ScrollView (:236-242) and s.cat; section (280) 17/900 → theme.secTitle. Grid and 「예정」 labels unchanged; the '#8fa093' loading line inside the ink hero stays (dim-on-dark).

SKIP: community.tsx and club/receipt (Sean's Codex lane — list them in the report as owed there).

ACCEPTANCE: app/test/font-floor-my-fitness.test.cjs reads my.tsx, fitness.tsx, payments.tsx, cards.tsx, shop.tsx comment-stripped and asserts no `fontSize: 14` / `fontSize: 14.5` on a style that also sets a Korean-bearing text (pragmatic form: assert 0 occurrences of `fontSize: 14(\.5)?,` in these files, with an explicit allowlist for latin kicker styles you keep, each named); grep '14pt floor' my.tsx → 0; grep -c 'secNo\|secT\b' my.tsx cards.tsx → 0; grep 'CATS' shop.tsx → 0; HEAD_H arithmetic shown; tsc 0; check-a11y-roles unchanged or shrunk; npm FAIL 0 with PASS delta = pins added.

**Acceptance:** font-floor-my-fitness.test.cjs pins zero 14/14.5pt Korean styles across the five files with a named allowlist; passes · my.tsx: stale '14pt floor' comments gone; § kicker rows gone; dead handle/fieldLabel/input/saveBtn gone; token hexes replaced; passport artifact and scale presses untouched · fitness.tsx: HEAD_H and siblings moved by the exact lineHeight delta (arithmetic in report); no animation or ring change · payments.tsx: note 15, failTxt 15/700, return door PaperBtn primary; shop.tsx: no CATS chips; cards.tsx: subNote gone, wellNote kept, § rows converted · tsc 0 · check-a11y-roles exit 0 · npm FAIL 0, PASS delta = pins added
**Plants:** re-plant `fontSize: 14` on my.tsx recFailTxt → font-floor pin reddens naming the file · add a stray `fontSize: 14` to shop.tsx → the pin reddens (proves the sweep covers every owned file) · control: a comment containing `fontSize: 14` reddens nothing (comment-stripped)

### P10 · `fix/alert-fold-copy` · client

⚠ RUNS LAST. Rebase on trunk after slices 1–9 have landed, then sweep. 81 executable `Alert.alert(…(e as Error).message…)` sites print PostgREST English inside a Korean product; rpc-error.ts's foldRpcError (:114) exists and is bypassed. This slice builds the helper, converts the files it owns, and leaves a SHRINKING LEDGER for files owned by other branches (the orchestrator serialises: convert those too once your rebase shows them landed — report which you reached).

1. New app/src/lib/alert-fail.ts: `export const alertFail = (title: string, e: unknown, tail?: string) => { console.warn('[alert-fail]', rpcRaw(e)); const m = foldRpcError(e).message; Alert.alert(title, tail ? `${m}\n${tail}` : m); }` — imports Alert from react-native and foldRpcError/rpcRaw from rpc-error.ts. rpc-error.ts itself stays PURE (test/run-rpc-error-fold-tests.sh esbuild-bundles it; importing react-native there breaks the bundle).

2. Convert every non-club, non-dev `Alert.alert(…\.message` site in your owned files to alertFail; safety.tsx (owned by ui/safety-settings-chrome — convert after it lands) keeps its 112/119 line as `tail`. compose.tsx:85 and course/[id].tsx:209: route through foldRpcError(e).message (their `??` fallbacks are dead on an Error). LEAVE login.tsx:66 (deliberate, :92-93). LEAVE api.ts's `if (error) throw error` lines. SKIP app/club/**, app/shot/**, app/community.tsx (Sean's Codex lane), app/dev/** (__DEV__), chat.tsx (fix/chat-read-cursor in flight), src/components/clubcard.tsx (Codex W4).

3. New app/test/alert-fail-sweep.test.cjs: walks app/ + src/ comment-stripped, EXCLUDES the CODEX_BATCH prefixes and app/dev/ and app/club/ exactly as copy-forms.test.cjs:44-50 does, and asserts the count of executable `Alert.alert(…\.message` hits equals a per-file KNOWN ledger that may only shrink (a file at 0 must be removed from the ledger; a count above its line fails; a stale line fails) — the check-a11y-roles ledger shape. Control arm: a comment quoting the retired form does not count. Seed the ledger from your measured post-sweep tree and name every file left in it and WHY (owned by which branch).

4. copy-hierarchy-3 Alert TITLE grammar — house form: short noun `<대상> 실패` / `<대상> 완료`, body carries one 해요체 sentence. Convert the sentence-form titles in owned files: compose.tsx:116 → '업로드 실패'; card-link.tsx:67 → '카드 연결 완료' with the last4 body; dog.tsx, review.tsx, live.tsx, availability.tsx, runner-profile, rewards, incident as found (grep -rhoE "Alert\.alert\(\s*'[^']*(못했어요|됐어요|했어요)'"). owner/meetup.tsx:267 and runner/meetup.tsx:274 ('인계 확인이 전송되지 않았어요' → '인계 확인 실패' / body '다시 시도해주세요') are frozen-zone files owned by other slices this wave — put them on the ledger for the orchestrator, do not edit.

5. copy-hierarchy-7: course-detail.tsx:146 '…점검이 됩니다' → '…돼요' (request.tsx's two copies are fix/owner-request-funnel's); notification-prefs.ts:143 쌓입니다 → 쌓여요. runner/requests.tsx:763 and runner/done.tsx:445 belong to other slices — ledger them. Add a `(습니다|ㅂ니다|됩니다|합니다|입니다)` arm to copy-forms.test.cjs over comment-stripped strings, EXCLUDING CODEX_BATCH and legal-text modules, with a KNOWN ledger (run.tsx:187/193 parody captions stay; requests/done lines until their slices land) that only shrinks.

6. copy-hierarchy-8: house form closed `해주세요`. rpc-error.ts:33 PENDING_DEPLOY_KO → '…다시 시도해주세요' AND rpc-error-fold.test.cjs:63 in the same commit (comment: spacing normalised, one retry sentence). Sweep late-copy.ts, checkin-copy.ts, checkin-answer.tsx, gear-claim-form.ts, rewards.tsx; add `해 주세요` as a 0-count arm in copy-forms.test.cjs with the CODEX_BATCH exclusion and a KNOWN ledger for files you could not reach.

7. copy-hierarchy-10: dog.tsx:452 label '저장'; runner/availability.tsx:803 `dirty ? '저장' : '저장됨 ✓'`.

8. ops-notifications-10: notification-prefs.ts:98 desc → '담당자에게 오는 운영 알림 전체 — 지급 대기·인계·반환·굿즈, 결제·카드 처리 실패까지 · 운영 콘솔은 그대로예요' (keeps '지급 대기' / '굿즈' / '콘솔' so notification-prefs.test.cjs:85-87 stay green); add a pin that the desc mentions '결제'.

DO NOT TOUCH: login.tsx, api.ts, chat.tsx, meetup files, club/shot/community, settings.tsx.

ACCEPTANCE: alert-fail-sweep ledger seeded and every remaining line explained; rpc-error-fold suite green with the moved literal; copy-forms arms green with ledgers; tsc 0; npm FAIL 0; `grep -rn 'from .react-native' app/src/lib/rpc-error.ts` → 0.

**Acceptance:** alert-fail.ts exists; rpc-error.ts imports nothing from react-native; run-rpc-error-fold-tests.sh still bundles and passes · alert-fail-sweep.test.cjs: per-file shrinking ledger seeded from the measured tree; every remaining file named with its owning branch; control arm proves a quoted comment does not count · copy-forms.test.cjs: new 합니다-tail arm and 해 주세요 arm with CODEX_BATCH + legal exclusions and shrinking ledgers; rpc-error-fold.test.cjs:63 moved with PENDING_DEPLOY_KO · owned files converted: compose/course fold through foldRpcError; sentence-form Alert titles → noun form; dog/availability '저장'; prefs desc mentions 결제 and keeps 지급 대기·굿즈·콘솔 · tsc 0 · check scripts 0 · npm FAIL 0, PASS delta = pins added
**Plants:** re-plant `Alert.alert('삭제 실패', (e as Error).message)` in compose.tsx → alert-fail-sweep reddens naming the file · raise a ledger line above the measured count → the stale-line arm reddens · control: a comment `// Alert.alert('x', (e as Error).message)` reddens nothing · revert rpc-error.ts:33 to the spaced form → rpc-error-fold pin and the 해 주세요 arm both redden

