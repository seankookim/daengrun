You are reviewing daengrun (도그스하이), an Expo + Supabase dog-running marketplace, before ONE production deploy. You are in a frozen git export of trunk. Read CLAUDE.md §「Migrations & security」 bullets only (skip incident narratives). SCOPE — exactly these files and nothing else (ELEVEN migrations pending on production, deployed end state = latest definition wins): supabase/migrations/0157_billing_cron_and_key_hardening.sql, 0158_settled_distance_is_actual.sql, 0159_pack_map_channel.sql, 0160_pack_publish_hardening.sql, 0161_billing_keys_client_grants.sql, 0162_chat_client_key.sql, 0166_revocation_dispatch_findings.sql, 0167_phone_visibility_gate.sql, 0168_run_end_two_phase_stop.sql, 0169_settle_run_stopping_belt.sql, 0170_billing_intent_row.sql; their suites supabase/tests/188_ 189_ 190_ 191_ 192_ 193_ 196_ 197_ 198_ 199_ 200_; the edge handlers supabase/functions/{collect-charges,register-billing-key,revoke-billing-keys,settle-run}/handler.ts + _shared/cron-auth.ts; and the client half in app/src/lib/pack.ts, app/src/lib/use-pack-share.ts, app/app/club/map/[sid].tsx, app/app/club/run/[sid].tsx, app/app/club/console/[sid].tsx, app/src/lib/api.ts (only sendChatMessage/sendChatPhoto and the runStopping/ended[] types), app/app/chat.tsx. Contracts to check against: docs/contracts/pack-publish-hardening-contract.md (0160), docs/contracts/run-end-two-phase-stop-contract.md (0168/0169), docs/research/2026-08-31-toss-provider-memo.md §4 (0170). Context: 0159 was previously REJECT/11 (docs/decisions/2026-08-28-codex-verdicts.md §0159) and 0160 is the fix pass — verify each of the 11 is closed or say which is not. 0157/0158 close billing findings 5/6/7 and run-end findings 8/9 in docs/reviews/2026-08-28-codex-billing-chain.md and -runend-money.md. For the deployed END STATE (latest definition wins): (1) every SECURITY DEFINER touched has in-body search_path and an explicit same-file ACL; (2) party gate before state gate; (3) no bare <> / IF on a nullable predicate; (4) club_pack_publish cannot be made to publish another user's identity or publish outside the session window, and no realtime policy admits a client socket INSERT; (5) billing_keys grants: over-revoke would break register/revoke edge paths — confirm service_role keeps what those handlers need; (6) 0158's coalesce removals cannot rank a NULL-km run first or print 0 for an unmeasured run; (7) anything that arms the moment card_registration, phone_collection or payments flags flip; (8) 0168/0169: can a run be settled at client numbers through ANY door while `stopping`, can the drain window be abused to inflate km, does the sweep's explicit cutoff preserve 0156's bound, and does any caller of runEnded/ended[] break because a field's meaning widened; (9) 0170: can an intent be opened twice for one attempt, can a client role read or write the intents table, does a thrown Toss call leave a credential unnamed; (10) 0166/0167: does gating visibility on phone_collection_live() break the roster for a HOST when the flag is on (positive control), and can a NULL-token report still rewrite a terminal revocation row. Mark every claim [measured] (you read it) or [inferred]. Cite file:line. Rank by severity. End with exactly two lines: FINDINGS: <n> and VERDICT: <one of APPROVE, APPROVE-WITH-FIXES, REJECT>.
