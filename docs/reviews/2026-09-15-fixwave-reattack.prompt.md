You are re-attacking a FIX WAVE in daengrun (Expo + Supabase dog-running marketplace), inside a frozen git export of trunk. Your earlier review (docs/reviews/2026-09-15-deploy-gate-verdict.md — read it first) returned REJECT with 10 findings; findings 4, 5, 6, 7, 8, 9 were fixed and 1, 2, 3, 10 were dispositioned (1/2 standing behind money flags, 3 provenance class, 10 recorded). SCOPE — only these files, read each fully: app/app/club/run/[sid].tsx (findings 4 + 7), app/src/lib/api.ts sendChatPhoto only (~3140-3170, finding 8), app/src/lib/pack.ts packRetainRoster + app/app/club/map/[sid].tsx roster handling + app/test/pack.test.cjs (finding 9), supabase/migrations/0171_phone_visible_null_safe.sql + supabase/tests/201_phone_visible_null_safe_suite.sql (finding 6 — NOTE the fixer measured your premise false: session_dogs.custody_phase is NOT NULL since 0040:47 with a trigger that always writes it; say whether you agree and whether 0171 is still worth deploying as hardening), supabase/functions/register-billing-key/handler.ts + supabase/functions/_test/register_billing_key_test.ts (finding 5, AND the HIGH #2 half-fix: the blind Toss DELETE on a failed/unknown swap is gone — definite SQL refusal orders a revocation through the outbox, unknown outcome does nothing but the named intent row; check that no path can now leave a LIVE key both stored-at-Toss and unnamed, and that the outbox order cannot revoke a key the swap actually stored). Also read supabase/migrations/0172_settle_charge_reconcile.sql + supabase/tests/202_settle_charge_reconcile_suite.sql: the fixer REFUTED your HIGH #1 by measuring that sweep_settled_without_payments() (0080:569, 0116:60, cron sweep-settled-charges) already re-mints a settled-but-uncharged booking; 0172 only pins that both recovery cron jobs are registered. Say whether you accept the refutation, and whether the named residual (a booking the sweep cannot price is skipped invisibly — payments_reconciliation() has no settled_without_payment arm) deserves a finding. For each of the eight (six findings + the HIGH #2 half-fix + the HIGH #1 refutation): CLOSED (cite the line that closes it) · PARTIAL (what remains) · WRONG (the fix introduces a new defect — say what). Then look for regressions the fixes could have introduced: a frozen/stopping run whose settle CTA is now enabled when it must not be; the trace_future_fix arm swallowing an error that must retry; the upsert path letting one user overwrite another's object (check the storage policy the fixer cites, 0064_private_media.sql); roster retention showing stale people after the window reopens; 0171's ACL/search_path/VERIFY house form; the reordered handler leaking flag state or profile existence to an unauthenticated caller. Mark [measured]/[inferred], cite file:line, rank by severity, keep it under 120 lines. End with exactly two lines: FINDINGS: <n> and VERDICT: <one of APPROVE, APPROVE-WITH-FIXES, REJECT> for the fix wave alone.
