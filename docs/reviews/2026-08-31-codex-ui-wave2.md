# codex round 2 — UI wave (U3 + U4c + chat rescue + round-1 fix round)

**Genuine verdict** (gpt-5.6-sol, xhigh, frozen export, digit-detector `FINDINGS: 13` on stdout,
no quota wall): **REJECT, 13 findings.** Round 1 (U5+U2) was REJECT/5, answered in `bf0d387`.
This file is the round-2 disposition ledger — written BEFORE the fixes so a context compaction
cannot lose the mapping. Status column updated as fixes land.

| # | file:line (at review) | sev | finding (compressed) | disposition |
|---|---|---|---|---|
| F1 | owner/pay.tsx:250 | HIGH | doors discard the payment row's amount; screen prints planned `total_price` labeled 결제 금액 — a waived 0원 row opens a non-zero 「결제 금액」 | **FIXED**: total label becomes phase-aware (결제 금액 only when `paid`; 청구 금액 for collect_*; 예상 요금 otherwise). Full Ⓒ1 per-payment receipt (actual amount rows) stays future work, noted in-file |
| F2 | payphase.ts:187 | HIGH | collection consulted only for completed/refund_pending — cancelled bookings with minted cancel-fee rows render quiet 「취소된 예약이에요」 | **FIXED**: `collectionMatters` widened to cancelled_owner/cancelled_runner/expired; shared fold; `disputed` deliberately excluded (incident surface owns its money). payphase tests extended in-slice |
| F3 | club/run/[sid].tsx:125 | HIGH | in-flight board response race across sid change: A resolving after B overwrites B's board (publishing is guarded by boardFor, but A's dogs drive actions under B's sid) | **FIXED**: drop stale responses — capture request sid, ignore resolution when it no longer matches |
| F4 | club/console/[sid].tsx:145 | HIGH | 0144 authorizes host OR backup, but the console bounces non-hosts and the shell hides its door — a backup can never invoke 러닝 종료 | **FIXED**: console admits `iAmBackup` in a reduced backup mode (board read + 러닝 종료 + result only — every other console action is strict-host server-side and would be a dead button); shell console door widens to backup with a labeled variant |
| F5 | chat.tsx:67 | MED | bid change on a mounted route: old ctx/msgs union into the new thread | **FIXED**: reset ctx/msgs/link/pollErr/state at the top of the thread-prep effect |
| F6 | club/console/[sid].tsx:286 | MED | 러닝 종료 preview names only active&unfrozen; the RPC loops every delegated booking — the confirm under-names what the tap reports | **FIXED**: preview enumerates the RPC population (bookingId != null); confirm copy states that non-running delegations are reported too |
| F7 | club/console/[sid].tsx:94 | MED | endResult not sid-scoped and never invalidated — session A's report can render under B | **FIXED**: store {sid, result}; render only on matching sid; timestamp label from result.at via kst |
| F8 | club/console/[sid].tsx:73 | MED | `error` reason copy promises a per-dog retry that doesn't exist | **FIXED**: copy → 다시 시도해주세요 (pack-wide truth) |
| F9 | charge-states.tsx:87 | MED | PaymentRow a11y label replaces content but omits amount/status/refund/decline | **FIXED**: label composed from title+amount+status(+환불) |
| F10 | chat.tsx:174 (+ :158 photo) | MED | send/refetch share one catch — committed send + failed refetch → 「전송 실패」 + restored input → duplicate on retry | **FIXED**: split catches; refetch failure sets pollErr, never claims send failure |
| F11 | club/run/[sid].tsx:123 | LOW | retry after board failure renders 'denied' while actually loading (boardLoaded stays true) — pre-existing, surfaced by this slice | **FIXED**: reset boardLoaded at load() start |
| F12 | club/console/[sid].tsx:373 | LOW | `not_signed_in` raise untranslated | **FIXED**: map arm added |
| F13 | owner/pay.tsx:95 | LOW | whenLabel's catch falls back to device-timezone `toLocaleString` | **FIXED**: converge on kst.ts (kstDateLabel + kstAmPm), Intl dropped entirely |

Verified-clean by the same review (kept for the record): pay's two-coral-rule count holds in every
state; map import stays lazy; map-less deep links render an honest terminal; merge dedup stable;
all SQL reason tokens rendered; the widened RPC-contracts regex alters no existing captures;
frozen lockfile carries transitive @types/node.

---

# Round 3 — re-attack of the r2 fixes + first review of U4b + 예약 규칙 editor

**Genuine verdict** (same invocation shape, frozen wave-3 export, digit-detector `FINDINGS: 19`
on stdout, err.log clean of quota/trust refusals): **REJECT, 19 findings.** Ledger written before
the fixes, per standing practice. Verification fan-out ran per finding cluster (a codex finding is
a claim; the disposition column records what the artifact said).

Codex's own verified-clean list, kept for the record: committed-runner picker vs server gates,
no-unset behavior, reduced backup console composition, zero-row free-cancel terminal, independent
rule-save state vs the frozen D② grid, the two-column reader claim, and `rest_after_min = 0`
range arithmetic all check out. It could not rerun tests (export omits node_modules) — expected.

| # | file:line (at review) | sev | finding (compressed) | disposition |
|---|---|---|---|---|
| R3-1 | 0047_assignment_loop.sql:308 | HIGH | session_set_backup party gate is bare `<>` → NULL-collapses for anon; "never revoked from PUBLIC, grant is additive" → anon can steal backup slot once flag opens | **REFUTED on reachability, measured on production 2026-08-31**: `anon_exec=false`, ACL explicit `postgres/authenticated/service_role` — no PUBLIC arm. Mechanism: 0058 §4's owner-filter-free sweep revoked public/anon from every pre-0058 definer (0047 < 0058), and no migration after 0047 recreates the function (grep: only 0047 defines it). The `<>` vs `is distinct from` asymmetry vs its sibling club_assume_host is real but unreachable (authenticated callers always have non-NULL auth.uid()); relayed to backend as recreation-hazard hygiene, not a hole |
| R3-2 | payphase.ts:147 | HIGH | canceled/partial_canceled payment statuses fold into "captured" → refunded booking renders 결제 완료, hiding the refund | **CONFIRMED → FIXED.** Verifier grounded reachability: 'canceled' has a deployed writer TODAY (confirm-payment autoCancel, handler.ts:244-250, writes canceled+refunded_amount; its booking lands 'expired' via 0060's sweep → derivePayPhase said 결제 완료 for fully-refunded money); partial_canceled is latent (CHECK admits, no writer — widened-meaning law applies). Fix: Collection gains `refunded: none/partial/full` (word-primary — server may leave refunded_amount at default) + two new phases `refunded`/`refund_partial` pushed through consumers by the literal union; priority kept (captured still outranks outstanding, refund splits within). Pins: P9 block (16), incl. P9b never-without-evidence sweep |
| R3-3 | owner/pay.tsx:252 | HIGH | totals always bind planned bookings.total_price; captured-fee/partial-refund amounts can differ from what's printed under 결제 금액/청구 금액 | **CONFIRMED → FIXED.** Verifier's truth table showed 3 lying cells + 3 framing lies; the fee divergence is production-real (0118:571-598 mints amount=cancel_fee ≠ total_price, called by transition-booking cancel_owner.ts:259). Fix: Collection carries server-committed sums (capturedGross/refundedTotal/capturedNet/outstandingAmount, null = not measured, never 0); PayView gains `collection` prop; total cell binds real sums under 결제/청구/환불 금액 with '—' on null (fail-closed — planned fare never sits under a payment-fact label), and terminal no-further-charge states get neutral 예약 요금 instead of the false 예상 요금. pay-lab renders per-phase fold fixtures (fee 8,400 vs planned 27,900 shows the divergence). Pins: P10 (2) |
| R3-4 | owner/pay.tsx:314 | HIGH | phase bodies (러닝이 끝나고 / 러닝 정산은 끝났어요) false for cancelled bookings widened into collect_*/paid | **CONFIRMED → FIXED** — verifier found a 4th cell codex missed: the loud strip's 「러너 정산은 이미 완료됐어요」 (unverified claim for cancel fees). All four sentences now branch on rawStatus cancelFamily (cancelled_owner/cancelled_runner/expired — same family STATUS_MAP already folds); 취소 수수료 wording grounded in 0118:600 raw.kind='cancel_fee'. Korean copy is draft — Sean's review welcome |
| R3-5 | club/run/[sid].tsx:126 | HIGH | sid reset omits endTarget — A's end modal survives into B; confirm settles A under B's chrome | **CONFIRMED → FIXED**: endTarget/endStep/conditionNote join the sid reset; belt-and-braces guard in doSettle refuses any target outside the current board's active set. Verifier aggravations recorded: A-modal confirm also saved trace under B's sid and mis-computed the 'all done' alert |
| R3-6 | club/run/[sid].tsx:225 | HIGH | resetTrace() only on unmount — B inherits A's trace/distance/tracking; saveClubRunTrace(B) can persist A's GPS into B's money record | **CONFIRMED → FIXED**: resetTrace() + the five local mirrors (trace/kmRef/km/pathLen/lastPos) cleared in the sid effect. The r2-F3 comment's 「geo singleton is the pack session's job」 deferral was half-wrong — resetTrace is this file's own call. Ordering verified: saveTrace's cleanup snapshots trace.current synchronously before the reset body runs, so A's final flush still saves A's points to A's sid. Residual named, not closed: two simultaneously-mounted run screens sharing the singleton (SOS → case → second run push) needs a buffer-ownership contract — backend/geo lane |
| R3-7 | club/run/[sid].tsx:176 | MED | in-flight fetchRunStartedAt unscoped — A's timestamp lands after B's reset; fetchingStart wedges B's clock in an absorbing state | **CONFIRMED → FIXED**: requestSid stale-drop (load()'s landed idiom); fetchingStart deliberately NOT reset on sid change (a stale finally would license a duplicate; the 1s ticker re-requests once A settles). Verifier radius: same class in hydrateFromServer — guarded too (F6's reset would have re-opened its empty-buffer seed gate to stale responses) |
| R3-8 | club/console/[sid].tsx:117 | HIGH | console board load has no sid guard/reset — stale A board renders A's report/actions under B (endResult.sid check compares two stale A values) | **PARTIAL → FIXED anyway.** Mechanics confirmed; reachability refuted today (all console entries are router.push = fresh instance; no navigate/setParams/deep-link targets it). Fixed per house precedent — the run screen measured the identical class real, and one navigation refactor arms it: liveSid + sid reset (incl. endResult/forceTarget — also closes a stale force-resolve sheet) + requestSid drops on all three fetches |
| R3-9 | club/console/[sid].tsx:160 | HIGH | real backup bounced as unauthorized while backupFacts pending, permanently after fetch failure — backupErr renders below the early return, unreachable to its own audience | **CONFIRMED → FIXED**: three-state gate for non-hosts before the bounce — backupFacts null + !backupErr → LoadGate loading; null + backupErr → LoadGate error with retry (load() re-clears backupErr); the bounce now fires only on a measured non-backup. Defeated exactly the r2-F4 grant it sat in front of |
| R3-10 | api.ts:4297 | MED | fetchSessionBackup swallows getUser() errors; null row invented as "no backup" (iAmBackup:false) | **PARTIAL → FIXED.** Auth arm confirmed (contrast: fetchMessages throws); hidden-row arm REFUTED — 0030:133 「sessions public read」 using(true) stands unreplaced, so a null row is genuine absence, not RLS hiding. Fix: throw authError; throw 'session not found' on missing row (safe precisely because of the RLS fact); signed-out-without-error still honestly false |
| R3-11 | club/session/[sid].tsx:227 | MED | backupFacts not sid-scoped/reset; failed reread silently preserves stale facts (server-rejected takeover button post-takeover) | **CONFIRMED → FIXED**: rosterOf idiom — `backupOf {sid, data}` with a derived read that drops mismatches (reset + stale-drop in one move); doAssumeHost success clears backupOf before load() so a failed reread shows nothing rather than a dead button |
| R3-12 | club/session/[sid].tsx:1454 | MED | T−30 gate reads `now` state that only ticks for proposals/holds/joinedWaiting — a checked-in or assignment-only backup never sees the takeover card appear | **CONFIRMED → FIXED**: 30s ticker gains a backupWaiting arm (iAmBackup ∧ open/full) — one interval for the single designated backup while the session is open, same budget as the check-in ring. Verifier sharpened codex: rerender/refocus does NOT unfreeze it (gate reads state, not Date.now()), so the freeze was indefinite |
| R3-13 | chat.tsx:60 | MED | bid-change reset omits `input` — A's draft sendable into B; failure-restore replants A's draft into B | **CONFIRMED → FIXED**: separate bid-keyed effect clears input+sending (NOT added to the shared line-60 list — that also runs on loadAttempt and would eat same-thread drafts on retry); the restore path is thread-guarded via R3-14's identity |
| R3-14 | chat.tsx:197 | MED | send/photo post-await writes guarded by mount only — A's snapshot unions into B, A's pollErr/Alert land on B, A's `sending` blocks B | **CONFIRMED → FIXED**: ctxRef identity captured at operation start; every post-await write (setMsgs, setPollErr, Alert+restore, scroll, finally's setSending) drops when the thread changed. The three effects already had per-run alive closures — only the two handlers were mount-guarded, which is what made it a defect |
| R3-15 | api.ts:3095 | MED | text sends have no idempotency key — commit-with-lost-response + retry duplicates | **CONFIRMED → client half FIXED, schema half RELAYED.** chat_messages has no client-key column and no unique constraint (grep: 0001 DDL only), so a client key alone would be decorative. Client now says uncertainty as uncertainty: a codeless (network) insert failure alerts 「전송이 확인되지 않았어요 — 목록에 보이면 다시 보내지 마세요」 + refetches so a committed message becomes visible; a coded (server-refused) failure keeps the definite 「전송 실패」. Complete fix (client_key uuid + partial unique index + 23505→success) → backend queue |
| R3-16 | charge-states.tsx:89 | LOW | a11y label omits the visible 청구 date — and title prefers scheduledAt, so in the normal case createdAt is entirely unspoken | **CONFIRMED → FIXED**: `청구 <date>` unconditional in the label array, mirroring the visible line |
| R3-17 | 0001_init.sql:108 | MED | runner_booking_rules has no CHECK constraints — direct write of negative rest_after_min breaks tstzrange at 0003:53 | **ALREADY-RELAYED** (backend queue, this round independently re-confirms it): server CHECK is theirs; UI clamps bound this client's writes |
| R3-18 | api.ts:948 | MED | registration's initial rules INSERT error ignored → permanent absent row (early return blocks retry; save is UPDATE-only = silent 0-row match); editor's absent copy false for a registered runner | **CONFIRMED → FIXED** three-sided: saveMyBookingRules → upsert onConflict runner_id (legal under 0002:79 FOR ALL policy — USING applies to inserts absent WITH CHECK); the absent state becomes an editable seeded editor (「지금은 기본값(30분·4회)으로 판정되고 있어요」 — grounded in 0003:42-46's coalesce — with 규칙 만들기 save that heals the row); ensureRunner's two insert errors now throw (stops manufacturing the hole) |
| R3-19 | availability.tsx:443 | LOW | rules steppers enabled at bounds — announced button that silently does nothing (dead button + a11y) | **CONFIRMED → FIXED**: optional minusDisabled/plusDisabled on Stepper (default false — the frozen D② grid call sites stay byte-identical), disabled + accessibilityState + explicit paper.dim ink (no opacity tricks). ⚠ Director's flag for Sean: the two stepper families on this screen now diverge at bounds (grid = clamp-inside-always-enabled per its freeze; rules = disable-at-bound). Unifying means thawing D② — Sean's call, not taken unilaterally |
| R3-V | (verifier bonus) run/[sid].tsx full state enumeration | MED | stale `roster` across a sid change made emergencyOf miss B's dogs and render 「비상 연락처 미등록」 — an invented claim on a safety datum; elapsed/saveLag also crossed | **FIXED** with R3-5/6: roster/saveLag/elapsed join the sid reset. Found by enumerating ALL useState/useRef against the reset per the finding-sentence-over-cited-site law |

**Round-3 landing evidence**: gates tsc 0 · rpc 0 · route 0 · acl 0 · clock 0; payphase standalone 83/0 (65 + 16 P9 + 2 P10, three shape-grown pins re-pinned in place with WHY comments); full chain 822 PASS / 0 FAIL exit 0 (804 + 18, delta reconciled). Verification method: 8-cluster parallel fan-out, every finding CONFIRMED/PARTIAL/REFUTED against source before any fix; R3-1 refuted on production measurement (anon_exec=false). Round 4 (queued): re-attack of these fixes.
