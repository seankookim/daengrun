You are re-attacking FIX WAVE 2 in daengrun (Expo + Supabase), inside a frozen git export of trunk. Your previous review (docs/reviews/2026-09-15-fixwave-reattack-verdict.md — read it first) returned REJECT/2: R1 the cross-session roster contamination introduced by packRetainRoster, R2 settled-but-unpriceable bookings invisible to ops. SCOPE — only these files, read fully: app/src/lib/pack.ts packRetainRoster, app/app/club/map/[sid].tsx (the new sid-change reset effect and its ordering relative to the subscription effect), app/test/pack.test.cjs packRetainRoster block (R1); supabase/migrations/0173_settled_without_payment_arm.sql + supabase/tests/203_settled_without_payment_arm_suite.sql + supabase/functions/_shared/ops.ts RECONCILIATION_ARM + supabase/functions/_test/ops_routing_test.ts (R2), with 0116_flip_blockers.sql:47-130 and 0118_club_cancel_fee_collection.sql:1372-1400 as the base definitions. For R1: is the retention now scoped to one session, does the reset run before the new session's subscription can deliver, is any sid-owned state still carried (list what the fixer reset and what they deliberately kept — offsetMs/now, loadGen/rosterGen — and say whether keeping them is right)? For R2: is the anchor runs.settled_at with the sweep's existence predicate verbatim, does the one-hour grace reuse the function's existing constant, is the arm flag-gated by construction (NULL payments_live_since ⇒ no rows), does the NULL payment_id collide with arm seven under the group-by 0118:1372-1376 warned about, and does the ops.ts paging entry route correctly? For each: CLOSED (cite the line) · PARTIAL · WRONG (new defect). Mark [measured]/[inferred], cite file:line, under 80 lines. End with exactly two lines: FINDINGS: <n> and VERDICT: <one of APPROVE, APPROVE-WITH-FIXES, REJECT> for wave 2 alone.
