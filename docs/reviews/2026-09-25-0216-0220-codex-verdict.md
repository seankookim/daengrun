# Codex adversarial verdict — 0216–0220 (the correct-forwards for the 02:30 REJECT/6)

Run: 2026-09-25 16:34–16:36 KST · plugin `adversarial-review --wait --base b46adb9` from a detached export at `b12c68f` with `app/`
reverted to the base (server-only: 16 files, +5,829). Streams split (`scratchpad/codex-review/mig17-srv.{out,err}`).
Detection: `FINDINGS: [0-9]+` → 1 · verdict VALUE → 1 · `usage limit` → 0 · exit 0. Harness blocked by the sandbox → both findings READ.

**VERDICT: APPROVE-WITH-FIXES · FINDINGS: 2** — every one of the six original findings is accepted as closed; two mediums remain.

## Routing (announcer, 16:41 KST)

| # | sev | finding | goes to |
|---|---|---|---|
| 1 | med | 0219:171-176 — the merged judge admits a slot spanning two ADJACENT windows (09:00–12:00 rule + 12:00–14:00 extra → 11:00–12:05), but `runner_offered_slots` (0215:180-196) still returns separate windows and `app/src/lib/offered-slots.ts:115` requires the whole duration inside ONE window — owners cannot select some server-approved availability; suite 250 W9 (:458-464) requires the divergence | **0221** `be/0221-offered-slots-merge`: the candidate list uses the same merged intervals (per-segment source metadata kept), the client accepts a merged window, W9 → equality both directions incl. cross-midnight |
| 2 | med | 0218:430-439 — preservation skips a historical booking payload when it EQUALS `_force_evidence_public(...)`; a pre-0218 shell-force row could legally carry those five fields, so its journal evidence stays NULL and the CHECK at 0218:492-494 aborts the apply (constructible, not observed) | **0222** `be/0222-evidence-preserve-fix`: preserve every non-NULL historical payload when the force-kind journal evidence is NULL regardless of equality; fixture with that exact payload; second repair = zero change |
| — | next step | `248_claim_deletion_race.sh` uses fixed sleeps — can pass without exercising the lock | **0222 slice** (same builder): observed lock/wait handshake via `pg_stat_activity` |

## Codex output (verbatim)

```
# Codex Adversarial Review

Target: branch diff against b46adb9
Verdict: needs-attention

Fix two remaining gaps before shipping: availability enumeration diverges from the judge, and a valid historical evidence shape can abort migration 0218. MEASURED: the SQL harness was blocked by read-only directory permissions before startup; shutdown confirmed no test database existed. Both findings are READ-based.

Findings:
- [medium] [READ] Merged availability admits slots the booking calendar cannot offer (supabase/migrations/0219_window_intervals.sql:171-176)
  With a 09:00–12:00 rule and 12:00–14:00 extra window, this merged predicate accepts 11:00–12:05. However, runner_offered_slots still returns separate windows (0215:180–196), and app/src/lib/offered-slots.ts:115 requires the entire duration inside one window. Owners therefore cannot select some server-approved availability; adjacent windows across midnight have the same mismatch. Suite 250:458–464 explicitly requires the divergence instead of enforcing equivalence in both directions.
  Recommendation: Make candidate generation use the same merged timestamp intervals, retaining source metadata separately. Replace W9's expected divergence with equality checks for adjacent same-day and cross-midnight windows.
- [medium] [READ] Evidence equality can leave a historical journal row unfillable (supabase/migrations/0218_force_evidence_seal.sql:430-439)
  For a 0205 force row whose journal evidence is NULL, preservation skips the booking payload when it equals _force_evidence_public(...). That payload was legal under 0205's unrestricted nonempty-object contract: a shell caller could supply these five fields, including the transaction's now() timestamp. Equality does not establish server provenance. The journal remains NULL, then the evidence CHECK added at 0218:492–494 rejects it and aborts the migration. This is a constructible historical input, not an observed production row.
  Recommendation: Preserve every non-NULL historical booking payload when force-kind journal evidence is NULL, regardless of equality with the public composer. Add a pre-0218 fixture with that exact payload and verify migration completion, evidence preservation, and a zero-change second repair.

Next steps:
- Fix both cases and rerun the complete PostgreSQL harness in a writable environment.
- Strengthen 248_claim_deletion_race.sh with an observed lock/wait handshake: fixed sleeps can miss overlap and pass without exercising the lock.
- FINDINGS: 2
VERDICT: APPROVE-WITH-FIXES
exit=0
```
