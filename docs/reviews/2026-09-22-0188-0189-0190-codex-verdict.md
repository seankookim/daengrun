# Codex adversarial verdict — 0188 (run-end ceremony) · 0189 (urgent titles + tombstone) · 0190 (payout sweep lock + retention end)

Run: 2026-09-22 02:46 KST · plugin `adversarial-review --wait --base e21dfd7` from a detached worktree at trunk `92518b0` · diff scoped to supabase/ + app/ (31 files, +5043/-222).
Detection (strings only Codex can produce): `FINDINGS: [0-9]+` → 1 hit · verdict VALUE → 1 hit · `usage limit` → 0 on both streams · exit 0.

## Routing (announcer, 2026-09-22 02:46 KST)

| # | finding | side | goes to |
|---|---|---|---|
| A1 | one-stamp strand blocks earnings forever; no caller for `force_return_tx` | SQL + edge | **0193** correct-forward: reachable ops resolution action; strand arm reads `ops_flags.return_strand_minutes` (NULL = off; the NUMBER is queue item 23, Sean) |
| A2 | zero-stamp timeout → `incident_review` is unpayable | SQL | **0193**: adjudicated settlement from `incident_review` via the ops action; pin stop → timeout → late stamps → settle |
| A3 | confirm_return edge trusts a pre-stop snapshot; second stamp can seal without a quote | SQL (+edge) | **0193**: `confirm_return_tx` refuses to SEAL as the second stamp with `p_quote` null once `run_ended_at` is set (`quote_required`); edge re-prices and retries once |
| A4 | return notification routes drop the booking id | client | `notification-route.ts` returns `{ pathname, params: { bid } }`; cold-start test |
| A6 | 「반환 확인 요청」 collides with the club title (0069) | client | probe club membership; club → session screen; regression with the real payload |
| A7 | seal screen opens a stale failure receipt (`runResult.settled=false`) | client | receipt is booking-scoped and reloads settlement from the server before render |
| B5 | 「귀가 확인이 필요해요」 (0188 escalation) is disableable | SQL | **0193**: the writer emits it as `safety` and the always-on list carries it; pin with 예약 알림 off |
| B8 | tombstone does not invalidate a push already queued in the same window | SQL | **NOT BUILT — recorded gap**: pg_net has no dispatch-time recheck; the window is one transaction; an outbox is a separate slice (queue) |
| C9 | pre-0190 tombstoned + fully-paid runners keep bank details | SQL | **0193** guarded one-time cleanup — vacuous for this deploy (production is 0156; `payouts` never had a writer, so no runner was ever "fully paid"), kept because it is cheap and correct |

## Codex output (verbatim)

```
# Codex Adversarial Review

Target: branch diff against e21dfd7
Verdict: needs-attention

Do not ship: return recovery can strand earnings, notification routing breaks recovery and club returns, and privacy gaps remain. The c1da937 collection-parity fix is verified; 68 targeted Deno tests passed. SQL suites were inspected but not executed.

Findings:
- [high] Slice A — A missing second stamp blocks earnings indefinitely (supabase/migrations/0188_run_end_ceremony_wiring.sql:278-299)
  If either party never stamps, this branch sends one notification and leaves the booking active forever. Subsequent sweeps exclude the notified booking. The runner remains unpaid and work-gated; no app or edge caller invokes force_return_tx or ops_gated_runners. An owner using the old binary also cannot provide the new stamp.
  Recommendation: Add bounded ops escalation and a reachable, audited resolution action that prices, settles, and collects. Implement database changes in 0191 and test permanent counterparty inaction.
- [high] Slice A — Zero-stamp timeout destroys the normal settlement path (supabase/migrations/0188_run_end_ceremony_wiring.sql:300-311)
  If the runner's phone dies after end_run commits and neither party stamps within two hours, this update moves the booking to incident_review. Later confirmations can record both stamps but cannot settle: 0096 permits settlement only from active, and force_return_tx also rejects incident_review. A successfully performed run becomes unpayable through the shipped 1:1 settlement paths.
  Recommendation: In 0191, preserve a settlement-capable recovery state or add an explicit adjudicated settlement path for these escalations. Pin stop → timeout → both late confirmations → successful recovery.
- [high] Slice A — The price guard still trusts a stale pre-stop snapshot (supabase/functions/transition-booking/confirm_return.ts:178-187)
  A confirm request can read bk.run_ended_at as null and obtain no quote before a concurrent end_run commits. If the other party stamps before this request obtains the booking lock, confirm_return_tx accepts the second stamp and seals without settling because p_quote is null (0096:159–166). The edge permits this interleaving; both client confirmation buttons then disappear.
  Recommendation: Never call this ceremony's confirmation RPC without a valid quote, regardless of the earlier booking snapshot. Add an interleaving test where the stop commits between pricing and confirmation.
- [high] Slice A — Return notification links discard the booking ID (app/src/lib/notification-route.ts:108-111)
  These routes return only a pathname. push.ts forwards it without setting runnerJob.bookingId, while return-seal.tsx:103–107 resolves the booking solely from bid or that in-memory store. A cold-start notification therefore opens 'no return to confirm'; with stale store state, it opens a different booking and can stamp that booking instead.
  Recommendation: Return a destination containing params: { bid: refId } for every runner return notification. Test cold starts and a store containing another booking ID.
- [high] Slice B — The missing-dog escalation remains disableable (supabase/migrations/0189_urgent_titles_and_tombstoned_push.sql:96-100)
  The always-on list omits '귀가 확인이 필요해요'. Migration 0188:304–311 emits that title as booking when nobody has confirmed the dog's return and the booking escalates to incident_review. With booking=false, the new classifier suppresses this safety-critical push as an ordinary booking notification.
  Recommendation: In 0191, classify the actual custody-escalation payload as safety, preferably at its writer. Test the real escalation with booking notifications disabled rather than pinning the urgent family to exactly three titles.
- [medium] Slice A — The new return title collides with club notifications (app/src/lib/notification-route.ts:73-90)
  The assertion that clubs cannot emit this title is false: session_confirm_return writes '반환 확인 요청' with the club booking ID (0069:124–126), and the club screen still calls it. The title skips the club probe and now routes runners to the marketplace return screen, whose confirmation RPC rejects club bookings.
  Recommendation: Probe club membership for this shared title and route club returns to their session screen. Add a regression using the actual session_confirm_return notification payload.
- [medium] Slice A — The completed ceremony opens a stale failure receipt (app/app/runner/return-seal.tsx:368-372)
  run.tsx:789 initializes runResult.settled=false and stores an estimated payout; this change removes the code that updated those values after settlement. The seal screen navigates to done without refreshing them. Consequently, a successfully settled run displays 'settlement not reflected' and instructs the runner to settle again (done.tsx:223–245). Re-entry can also display another run's cached record.
  Recommendation: Make the receipt booking-scoped and load its measurements, ledger amount, and settlement status from the server before displaying it.
- [medium] Slice B — Tombstoning does not invalidate queued pushes (supabase/migrations/0189_urgent_titles_and_tombstoned_push.sql:185-195)
  The live-profile check occurs before net.http_post queues a payload containing the token and message. A notification transaction can observe a live profile, then deletion can commit before that transaction commits or the asynchronous worker sends. Neither deletion nor dispatch rechecks the recipient, so the queued notification can still reach the deleted account. Suite 220's HTTP stub does not exercise this ordering.
  Recommendation: Use a recipient-aware outbox and recheck tombstone state at dispatch, with deletion invalidating pending deliveries. Add this through 0191 and test deletion between enqueue and dispatch.
- [medium] Slice C — Previously retained bank details never reach cleanup (supabase/migrations/0190_payout_sweep_lock_and_retention_release.sql:376-381)
  Cleanup runs only during a new payout. A runner tombstoned and fully paid before 0190 retains their bank details under the previous implementation. After upgrading, deletion retries still short-circuit (0115:230–234), and another payout rejects the already-paid rows. No backfill or sweep releases these existing account numbers and holder names.
  Recommendation: Add a guarded cleanup in 0191 for existing tombstoned runners with no unpaid ledger rows, using the same profile-lock discipline. Include a fixture tombstoned and fully paid before the upgrade.

Next steps:
- Keep landed migrations unchanged; implement the database fixes in 0191.
- Add regression coverage for the identified interleavings, notification destinations, and pre-upgrade retained data.
- FINDINGS: 9
VERDICT: REJECT
exit=0
```

## Resolution (2026-09-22 04:48)

0193 `075863b` (+ suite 224) answers A1, A2, A3, A4, A6, A7, B5 and C9 as routed above; B8 remains a recorded gap
(pg_net has no dispatch-time recheck — an outbox is its own slice). Re-review scheduled for the 07:41 KST one-shot
after the Codex reset; the deploy letter stays parked until it returns.
