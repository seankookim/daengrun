# Gap sweep — PARTIAL results (2026-09-25 03:4x KST; the workflow hit the Claude session limit at 03:44, which resets 07:10)

Nine code-verified finders returned **127 raw findings** (owner-journey 15 · runner-journey 15 · contract-gaps 8 · ui-consistency 18 · less-is-more 19 · backend-logic 13 · ops-notifications 12 · onboarding-first-run 14 · copy-hierarchy 13). The dedup agent overflowed its 64K output limit (it was asked to return every finding in full — fixed for the resume: a merge MAP only). Refuter verdicts before the limit: **20 confirmed, 2 refuted, 105 unverified**; the planner never ran. Resume: 07:12 KST (`wf_ed4f322e-68e`, finders cached, one verifier per lens). Nothing below is built yet.

## Confirmed by an adversarial refuter

| id | sev | kind | title | files | migration? | product? |
|---|---|---|---|---|---|---|
| owner-journey-2 | high | logic-gap | The hero's coral 「내 차례 · 인계하기」 frame fires AFTER the handoff is sealed (picked_up), while the real handoff moment gets a quiet gold 「티켓 보기」 | app/src/components/home-hero.tsx, app/app/owner/home.tsx | no | YES |
| owner-journey-3 | high | feature-gap | 「러너 직접 고르기」 on the request screen opens the leaderboard, which has no way to pick a runner — a dead end in the booking funnel | app/app/owner/request.tsx, app/app/owner/matching.tsx, app/app/leaderboard.tsx | no | no |
| owner-journey-5 | high | logic-gap | Terminal-booking notifications (매칭 만료 etc.) deep-link to the run report, which tells the owner 「러닝이 끝나면 여기서 기록을 볼 수 있어요」 about a booking that will nev | app/src/lib/notification-route.ts, app/app/owner/report.tsx | no | no |
| owner-journey-4 | high | logic-gap | A booking can be held with no pickup address (server accepts NULL), and 주소 관리 lets the owner delete the default/only address with no guard | app/app/owner/request.tsx, app/app/owner/addresses.tsx, app/src/lib/api.ts | no | no |
| owner-journey-1 | high | logic-gap | A late or overrun run strands the owner from the handoff/return seal: the hero routes every late state to 내 일정, which has no door to /owner/meetup | app/src/components/home-hero.tsx, app/src/lib/lateness.ts, app/app/owner/home.tsx, app/app/owner/schedule.tsx | no | no |
| runner-journey-1 | high | logic-gap | Runner home swallows a failed jobs read — a runner holding a dog sees no ticket, no error | app/app/runner/home.tsx | no | no |
| runner-journey-3 | high | logic-gap | Receipt (done.tsx) asserts the wrong phase: '인계해 주세요' on settled runs, '러닝 화면에서 다시 정산' during the return ceremony, coral 다음 요청 while gated | app/app/runner/done.tsx | no | no |
| runner-journey-2 | high | logic-gap | Calendar sends a run-ended (귀가) job to the live run screen, and the run screen has no 'already ended' guard | app/app/runner/calendar.tsx, app/app/runner/run.tsx | no | no |
| runner-journey-5 | high | feature-gap | A runner cannot withdraw from a confirmed booking anywhere, and a dead confirmed booking occupies home forever | supabase/functions/transition-booking/index.ts, app/app/runner/meetup.tsx, app/app/runner/home.tsx, app/src/lib/api.ts | yes | YES |
| runner-journey-7 | high | logic-gap | Opening the meetup screen IS the 출발 event — any tap on a confirmed job within 24h tells the owner '러너 이동 중' | app/app/runner/meetup.tsx, app/app/runner/home.tsx | no | YES |
| owner-journey-6 | medium | feature-gap | After a cancel, expiry or runner no-show the schedule sheet dead-ends at 「더 진행할 작업이 없어요」 — no re-book door, unlike the completed sheet | app/app/owner/schedule.tsx | no | no |
| owner-journey-8 | medium | copy | Runner ranking is labelled 「AI 추천 순 / AI 1순위」 but is a fixed client formula (62 + runs×5, pace distance/4) | app/app/owner/matching.tsx | no | YES |
| owner-journey-7 | medium | copy | Two permanent 「준비 중」 placeholders in the schedule booking sheet promise things the app does not ship | app/app/owner/schedule.tsx | no | no |
| owner-journey-12 | medium | onboarding | Login asserts consent to 이용약관 and 개인정보 처리방침 that exist nowhere in the app | app/app/login.tsx, app/app/settings.tsx, app/app/legal/[doc].tsx | no | YES |
| owner-journey-11 | medium | ui-consistency | 15pt Korean floor breached across fitness, payments and 마이 (14 / 14.5 pt prose, with stale '14pt floor' comments) | app/app/owner/fitness.tsx, app/app/payments.tsx, app/app/my.tsx | no | no |
| runner-journey-4 | medium | feature-gap | A directed (지명) request can only be declined from home's front ticket — 요청함 has no 거절 door | app/app/runner/requests.tsx | no | no |
| runner-journey-6 | medium | logic-gap | 요청함 draws live 수락하기 doors while the work gate is shut; home already draws the blocked sentence | app/app/runner/requests.tsx | no | no |
| owner-journey-15 | low | copy | Handoff screen foot advertises pet insurance under negotiation as a safety assurance | app/app/owner/meetup.tsx | no | YES |
| owner-journey-13 | low | onboarding | A returning owner re-picks their role on every cold launch even though the server already knows it | app/app/index.tsx, app/src/auth-context.tsx | no | YES |
| owner-journey-14 | low | copy | Chat footer promises retention 「러닝 종료 후 30일간」 but the purge is 30 days from message creation | app/app/chat.tsx | no | no |

## Refuted

- **owner-journey-10** — Latin kickers and English status chips (retired app-wide by DESIGN §3b) remain on alerts, pay and meetup — Three of the finding's four load-bearing claims fail on the code and the law book; the residue is a polish item that is already queued as an open Sean decision. (1) Premise: DESIGN §3b:189 retires the latin kicker AS A SECTION-HEADER EYEBROW ("ROSTER", "VERIFIED COURSES"), while DESIGN §2:90 KEEPS "Kickers: latin letterspaced caps, paper.faint (the 'PAYMENT' / 'MOCK · 준비 중' grammar)" — it names pa
- **owner-journey-9** — Retired palette worlds (swamp green, volt, beige) still ship on four owner core screens — The finding's headline — that the schedule greens and volt are a "retired palette" — is contradicted by the recorded rules and the tokens. (1) DESIGN.md:74-76 (commit 6ae2f48, Sean 2026-08-10 "all main tabs") says every screen's SEMANTIC colour system survives the paper migration and names "schedule status rails/badges" explicitly; schedule.tsx:36-39 carries the same ruling in its header ("상태 컬러(레

## Unverified (finder claims only; refuters never ran)

| id | sev | title |
|---|---|---|
| less-is-more-1 | high | 「러너 직접 고르기 ›」 on the request screen sends the owner to a leaderboard where nothing can be picked |
| backend-logic-1 | high | Custody states with no runner action have no exit: picked_up without start_run and active without end_run are swept by nothing, listed nowhere, and cl |
| contract-gaps-1 | high | SOS push tap lands the owner on the run REPORT and the runner on the CALENDAR — not on any contact surface |
| ui-consistency-2 | high | (b) Primary CTA: PaperBtn on 30 screens vs 6 ink-face primaries, 9 hand-rolled coral lips, 3 lip-less/opacity/scale variants, 1 pale fifth colour — an |
| ui-consistency-8 | high | (h) Typography: 16 Korean styles at 14pt on 마이, 13 on fitness, failure text at 14 on payments — with comments still citing a '14pt floor' |
| ui-consistency-12 | high | Screen: safety.tsx breaks 7 patterns — the 안심 센터 is the most off-system customer screen |
| ui-consistency-14 | high | Screen: my.tsx (마이) breaks 5 patterns — sub-floor type, retired kicker grammar, dead sheet styles, 35 hex literals, opacity presses |
| ops-notifications-1 | high | SOS push lands the runner on their calendar and the owner on the report — no contact control at either destination |
| ops-notifications-2 | high | 「예약 시간이 지났어요 — 앱에서 응답해 주세요」 routes both parties to screens that have no check-in answer |
| ops-notifications-3 | high | Live-run owner pushes (러닝 시작 · N km 돌파 · 응가 완료 …) land on the post-run report, which renders an in-progress run as an unmeasured FINISHED run |
| copy-hierarchy-1 | high | 81 Alert.alert sites and 3 inline fail strips print raw (e as Error).message — PostgREST/Supabase English reaches the customer |
| less-is-more-2 | medium | Runner home prints the online state and its explanation twice within one viewport |
| less-is-more-3 | medium | 러닝 가능 시간 has two homes and two 시간 조정 doors on the runner home |
| less-is-more-4 | medium | Runner-home 리워드 card stacks three progress meters and a pulsing ring — a celebration block competing with the 수락 door |
| less-is-more-5 | medium | Owner home (no booking) has five doors into the request screen; the 주간 목표 nudge is a plain duplicate of 미리 예약 |
| less-is-more-6 | medium | Owner home stacks five tinted DrawButtons (coral · paper · volt · lilac · blue) — the emphasis budget is one CTA |
| less-is-more-7 | medium | Shop shows five category chips that are not pressable but styled as an active filter |
| less-is-more-8 | medium | The 샵 tab is a preview with no purchasable item — a pilot tab that competes with the four real ones |
| less-is-more-9 | medium | Alerts rows carry seven decorations each plus a postmark, column header and three latin kickers |
| less-is-more-10 | medium | Community masthead still says 「동네 신문」 and closes with an explanatory colophon the reader cannot act on |
| less-is-more-16 | medium | 안심 센터 spends a third of the screen on an informational 안전 체계 block the customer cannot act on |
| less-is-more-17 | medium | Five customer screens still wear the retired beige/rounded chrome instead of the paper grammar |
| runner-journey-8 | medium | A booking in incident_review disappears from the home ticket and calendar while the dog may still be with the runner |
| runner-journey-9 | medium | start_run failure is swallowed twice — the runner runs for 30 minutes on a picked_up booking and only learns at 종료 |
| runner-journey-10 | medium | Bare router.back() on return-seal and base-pin — dead ‹ and silent no-op on deep-linked single-entry stacks |
| runner-journey-11 | medium | Live run screen has no exit and, on re-entry, labels a server-active run '러닝 준비' |
| runner-journey-12 | medium | Meetup 'waiting for owner' stage has no time anchor and no instruction |
| runner-journey-13 | medium | alerts.tsx is still the lilac world: rounded cards, drop shadows, glass mast, five latin kickers |
| backend-logic-2 | medium | Sealed-but-unsettled rows tell both parties '담당자가 확인하고 있어요' while no operator is told and no console lists them |
| backend-logic-3 | medium | A nomination that is never answered runs until the start time and expires; the owner is never warned and has no door back to the open pool |
| backend-logic-4 | medium | Recurring generator re-notifies a debt-blocked owner on every hourly tick for up to 72h per occurrence, with copy that claims the series is paused |
| backend-logic-7 | medium | F3 remains: a payout_due operator can seat anyone in any class, including themselves in return_strand, and deactivate other operators |
| contract-gaps-2 | medium | Six runner-bound booking titles have no RUNNER_ROUTES entry and all fall to /runner/calendar — money and case decisions land on a screen that does not |
| contract-gaps-3 | medium | '러너 재탐색 중' (runner declined) lands the owner on the post-run REPORT, which has no exit to radar where another runner is chosen |
| ui-consistency-1 | medium | (a) Screen header + back affordance: 1 majority header inlined on ~14 screens, 6 competing back-button shapes, 7 title sizes |
| ui-consistency-3 | medium | (c) Secondary/tertiary actions: PaperBtn secondary (30) / quiet (4) vs 9 hand-rolled outline buttons in 4 border colours and 4 fills |
| ui-consistency-4 | medium | (d) Section headers: 20/800 ink majority vs 17/900, 15.5/900, 16/800, 20/900 — and the §3b-retired '§ KICKER 한글' grammar still shipping on my/cards/co |
| ui-consistency-5 | medium | (e) Loading / failure / empty faces: one sentence in 6 greys, '다시 시도' in 24+ private styles, empty copy at 14–17pt |
| ui-consistency-6 | medium | (f) Sheets: PaperSheet (pageSheet) on 3 screens vs 12 hand-drawn transparent slide Modals with the fake grabber PaperSheet's header names as a defect |
| ui-consistency-7 | medium | (g) Cards & rows: the retired rounded-card grammar (radius 16, '#fff', beige '#DCD6C4' border) survives on 12 screens; hairlines split paper.line vs ' |
| ui-consistency-9 | medium | (i) Colours: retired forest/beige palette and sub-4.5:1 '#BBBBBB' text still painted on paper screens; volt lime as a button fill |
| ui-consistency-11 | medium | (k) Tab bar: 마이's children split — alerts/safety/cards keep the dock, settings/payments/notification-settings/profile-edit/addresses/dog/fitness/rewar |
| ui-consistency-13 | medium | Screen trio: settings.tsx, notification-settings.tsx, runner/bank-account.tsx share one legacy grammar that breaks 5 patterns each |
| ui-consistency-15 | medium | Screen: leaderboard.tsx breaks 6 patterns — the retired forest/beige world intact |
| ui-consistency-16 | medium | Screen: runner/home.tsx breaks 4 patterns on the runner's highest-traffic surface |
| ops-notifications-4 | medium | Pre-run owner titles (러너 매칭 완료 · 러너 재탐색 중 · 매칭 만료 · 일정 변경 수락/거절/만료) fall to the post-run report placeholder |
| ops-notifications-5 | medium | 7 of the 11 ops-addressed titles have no console door and are filtered OUT of the console's 운영 알림 list |
| ops-notifications-6 | medium | The roster offers four alert classes that nothing emits — a chip an operator can turn on and never receive |
| ops-notifications-7 | medium | Opening a notification never marks it read — only 「모두 읽음」 does, so NEW seals and the home bell count outlive the tap |
| ops-notifications-8 | medium | The console's 운영 알림 reads the newest 20 rows of EVERY kind, so an operator's own customer traffic pushes ops bells out of the list |
| ops-notifications-9 | medium | 인계 멈춤 desk tells the operator to 「양측에 직접 연락해」 but carries no phone, no chat door, nothing to contact with |
| onboarding-first-run-1 | medium | Owner display name is never collected; every Kakao owner is seeded as '사용자' and that word reaches runners, the community and club boards |
| onboarding-first-run-2 | medium | Runner home and 요청 tab tell an applicant who has ALREADY submitted an application to '지원하기' — the runner cannot see their status from either tab |
| onboarding-first-run-3 | medium | Applicant runners get a working 온라인/오프라인 switch on home that changes nothing for them (requests.tsx deliberately omits it for exactly this reason) |
| onboarding-first-run-4 | medium | A refused push permission has no way back: nothing in the app detects OS-level 'denied', and 설정 › 알림's four switches look live while iOS delivers noth |
| onboarding-first-run-5 | medium | Login says 'by logging in you agree to 이용약관 and 개인정보 처리방침' but neither document is reachable anywhere in the app |
| onboarding-first-run-6 | medium | Runner types the same 동네 twice: onboarding asks 홈 베이스, the application form asks 활동 지역 with no prefill |
| onboarding-first-run-7 | medium | Application collects 감당 가능한 최대 체중 and 활동 반경, approval writes them to runners, and nothing ever reads them |
| onboarding-first-run-11 | medium | The 샵 tab shows every owner, from day 1, a hard-coded catalogue with invented brand collaborations and '예정' prices |
| copy-hierarchy-2 | medium | Money numbers: 29 bare `toLocaleString()` price sites beside 12 `'ko-KR'` sites and two unused-by-most helpers (fmtWon, won) |
| copy-hierarchy-3 | medium | Alert titles use two grammars for the same event: noun `X 실패` (96) vs sentence `…지 못했어요` (16) — and the same failure is titled both ways on sibling sc |
| copy-hierarchy-4 | medium | The dog is named three ways in customer copy — 아이 (59), 반려견 (68), 강아지 (32) — sometimes inside one component |
| less-is-more-11 | low | 마이 gives owners two doors to /cards and a 예약 관리 row that duplicates the 내 일정 tab |
| less-is-more-12 | low | Passport chrome (마이 · 컬렉션) prints latin kickers and pseudo-data cells the customer cannot use |
| less-is-more-13 | low | 컬렉션 and the 마이 stamp page carry explanatory paragraphs over the word budget |
| less-is-more-14 | low | Requests tab: a 새로고침 chip beside pull-to-refresh, and a footer that restates the confirm dialog |
| less-is-more-15 | low | 결제 관리 return state shows two equal-weight buttons; the return door should be the directed one |
| less-is-more-18 | low | 내 일정 renders 「＋ 새 러닝 예약하기」 under every agenda, duplicating the header ＋ |
| less-is-more-19 | low | 수익 says 「지급 일정 미정」 twice and 주간 랭킹 promises an unbuilt 시즌 보상 |
| runner-journey-14 | low | Chat and runner-profile still carry retired forest greens, beige hairlines and pill radii |
| runner-journey-15 | low | Certification centre and onboarding overspend the word budget: latin kickers, colophon, footnotes, a '1 / 1' step counter |
| backend-logic-5 | low | Both late_booking_sweep and sweep_cancel_money_gaps are dark while ops_flags.late_protocol_live_since is NULL, and no migration sets it — if productio |
| backend-logic-6 | low | F6 remains: the operator's profile id still reaches both parties through bookings.return_force_evidence |
| backend-logic-8 | low | Three party RPCs read (and two lock) the booking before the party gate: open_incident_tx, fetch_checkin, answer_checkin are existence oracles of the F |
| backend-logic-9 | low | expire_unmatched_bookings promises '전액 환불 처리돼요' when a confirmed payment exists, and nothing in the repo writes a refund |
| backend-logic-10 | low | The nominated-accept path skips the applicant tier gate the open-pool path enforces; request_runner checks only that a runners row exists |
| backend-logic-11 | low | accept_reschedule re-checks the runner's slot but never the dog's clash, so a reschedule can double-book the same dog |
| backend-logic-12 | low | A pending reschedule proposal survives the runner departing: nothing clears it and nobody is told it expired |
| backend-logic-13 | low | settle-run is still deployed and runner-callable with zero client callers — a second settlement door to keep in sync for nothing |
| contract-gaps-4 | low | 7 of the 11 ops (`system`) titles the server ledgers have no console destination — the operator gets text they cannot act on from the inbox |
| contract-gaps-5 | low | `session_reconsider_dog` is granted to authenticated with ZERO client callers — a host cannot reverse a dog rejection though the server supports it |
| contract-gaps-6 | low | `notifications` is in the realtime publication but nothing subscribes, and push.ts has no foreground listener — an alert arriving while the app is ope |
| contract-gaps-7 | low | Two owner screens carry two different word tables for the same raw booking states |
| contract-gaps-8 | low | `debugReleasePayouts` wrapper calls an RPC the server revoked from authenticated in 0057 — the dev-lab button is dead by construction |
| ui-consistency-10 | low | (j) Spacing: layout.gutter (15) on 14 screens vs 11 on 10, 16 on 7, 18 on 4, 12/14/20 elsewhere |
| ui-consistency-17 | low | Ops console (7 files) is internally split: ink vs coral primary, circle backs, gutter 11, rounded cards — low because it is Sean-facing |
| ui-consistency-18 | low | Club client: five custom sheets on club/session and the LoadGate/ClubCta kit are consistent within the club world, but the world has no sheet componen |
| ops-notifications-10 | low | 운영 알림 toggle names 4 titles while silencing 11; the settings hint for the console names 2 of its 5 desks |
| ops-notifications-11 | low | notifyOps still falls back to the OPS_PROFILE_ID env uuid — a recipient the roster screen cannot see |
| ops-notifications-12 | low | Payment-failure pushes that say 「설정 > 결제 관리에서 …」 land on the run report instead of /payments |
| onboarding-first-run-8 | low | Runner calendar's empty state sends an applicant to the 요청 tab, which then sends them to apply — a two-hop empty for the one tab that could point stra |
| onboarding-first-run-9 | low | 알림 empty state (what every first-run owner sees on tapping the bell) is a decorative 'EMPTY STATE' tag in a dashed lilac-radius glass box |
| onboarding-first-run-10 | low | 주간 랭킹 (linked from owner home on day 1) is in the retired beige world and its zero-data copy is hype |
| onboarding-first-run-12 | low | Owner 내 일정 empty state tells the customer to go back to home while its own 「＋ 새 러닝 예약하기」 button sits directly beneath |
| onboarding-first-run-13 | low | The first two screens are two products: login is the retired dark volt/forest world, role-select is paper white |
| onboarding-first-run-14 | low | A first request can be submitted with no pickup address at all; the runner is then told to ask in chat |
| copy-hierarchy-5 | low | Runner money vocabulary: 수익 (tab/title) · 정산 · 지급 used as three names in one sentence |
| copy-hierarchy-6 | low | Owner writes a 후기, runner writes a 리뷰 — one feature, two names by role |
| copy-hierarchy-7 | low | Ten 합니다체 sentence tails inside an otherwise uniform 해요체 product (2,042 요-endings, 0 반말) |
| copy-hierarchy-8 | low | `해주세요` (181) vs `해 주세요` (44) spacing split — both forms in rpc-error.ts four lines apart |
| copy-hierarchy-9 | low | 26 ASCII `...` in customer strings against 181 `…`; two loading/placeholder families in one screen |
| copy-hierarchy-10 | low | Same primary action labelled 저장 on four screens and 저장하기 on two |
| copy-hierarchy-11 | low | Date arithmetic retyped in four places outside kst.ts and the weekday array duplicated six times (kst.ts's WD_KO is not exported) |
| copy-hierarchy-12 | low | Two server tokens rendered in parentheses inside Korean sentences on the host console |
| copy-hierarchy-13 | low | Question-form screen titles disagree on particle and person: 「어디서 만날까요?」 vs 「어디에서 출발하세요?」 |

## Confirmed findings — detail

### owner-journey-2 · high · The hero's coral 「내 차례 · 인계하기」 frame fires AFTER the handoff is sealed (picked_up), while the real handoff moment gets a quiet gold 「티켓 보기」

- `app/src/lib/api.ts:1155` — STATUS_MAP picked_up → 'handoff' (comment: 인계 완료 ≠ 러닝 중)
- `app/app/owner/meetup.tsx:237` — `sync.status === 'picked_up'` → stage 'confirmed' → line 762 SEALED block 「인계 완료! 러너가 곧 러닝을 시작해요」 — picked_up means both stamps landed
- `app/app/owner/schedule.tsx:1003` — same state on schedule reads 「인계가 완료됐어요 — 러너가 러닝을 시작하면 실시간 보기가 열려요」
- `app/src/components/home-hero.tsx:244` — state 'handoff' phrase = 「지금 만나요 / {dog} 인계할 시간」; line 354 sub 「러너가 도착했어요 · 만나서 인계해주세요」; line 356 coral DrawButton 「인계하기 · 아이를 넘기고 봉인해요」
- `app/src/components/home-hero.tsx:325` — the actual owner-turn moment (runner_enroute + arrived_at) is `arrivedWaiting` inside state 'confirmed'; line 470 its CTA is ground='gold' 「티켓 보기 · 시간과 장소를 확인해요」, chip 확정됨 — no coral, no 인계 verb
- **Impact:** The owner is told to go hand the dog over after they already did, and during the minute that actually needs them (runner at the door, seal pending) the home screen shows a calm gold ticket instead of the coral 'your turn' — the GO state law (coral = your turn) is inverted on the two states it exists for.
- **Fix (verifier-corrected):** One file: app/src/components/home-hero.tsx (home.tsx needs NO edit — it already passes rawStatus/arrivedAt at home.tsx:587-588 and arrived_at is in MY_BOOKING_SELECT api.ts:6453; drop it from files_to_touch). Server and meetup.tsx stage machine untouched (arrival is a timestamp by design, transition-booking:397-410; meetup already correct at :435-436).

PART 1 — ship now, common to both answers A and B, does not pre-empt Q4: re-word the state==='handoff' (picked_up) frame. In the chip ternary (:200) give 'handoff' its own row: { c: GO_SAGE, t: '인계 완료' } (leave 'returning' on paper.action 내 차례). In the phrase ternary (:245): { top: '인계 끝났어요', bottom: `${name} 곧 출발해요` }. In the early-return branch (:341-360): sub 「러너가 곧 러닝을 시작해요 · 시작되면 실시간 보기가 열려요」 (mirrors meetup.tsx:762-763 and schedule.tsx:1003), replace the coral DrawButton with one quiet non-coral door 「인계 화면 보기 · 봉인을 확인해요」 ground='paper' (or 'gold', never coral) → openNext (already routes handoff→/owner/meetup, which shows SEALED — a real route), keep the small chat button. Keep it in the early-return branch so 미리 예약 stays hidden. Update the header table (:16 「handoff → 알림 줄(코랄 = 내 차례)」) and the `returning` comment (:44-47, which describes handoff as the PICKUP) so the prose stops asserting the old behaviour. Since chip/phrase feed heroSentence (:258-262) the a11y announcement follows automatically.

PART 2 — only on Sean's one-word ruling for A (put docs/decisions/handoff-cta-gating.md to him as written): inside state 'confirmed', when `arrivedWaiting && !isLate`, derive chip { c: paper.action, t: '내 차례' } and phrase { top: '지금 만나요', bottom: `${name} 인계할 시간` } in the SAME ternaries (:200/:245, not in the branch, or VoiceOver drifts), keep the existing true subline 「{runner}가 도착했어요 · N분째 문 앞이에요」 (:425-427, doorWait 

### owner-journey-3 · high · 「러너 직접 고르기」 on the request screen opens the leaderboard, which has no way to pick a runner — a dead end in the booking funnel

- `app/app/owner/request.tsx:983` — 러너 row: `onPress={() => router.push('/leaderboard')}` with action label 직접 고르기 › / 다시 고르기 ›
- `app/app/leaderboard.tsx:55` — only Pressables in the file are back (55), the two tab chips (93) and retry (109); no row press, no /runner-profile link, no draft.preferredRunnerId write (grep over app/app: writes exist only in home, matching, report, schedule, request, runner-profile/[id].tsx:249,747)
- `app/app/owner/matching.tsx:171` — the screen that CAN nominate is reachable only from schedule.tsx's 러너 변경 (rebook mode) — not from request
- **Impact:** An owner who wants a specific runner taps 직접 고르기, sees a dog/runner km ranking with nothing to tap, and must back out; the only real nomination path is the 대기 중인 러너 cards on home → runner-profile → 지명, which the request screen never points to.
- **Fix (verifier-corrected):** Drop the leaderboard variant of the proposed fix entirely — it needs a new RPC column (profile id) and a migration, and the leaderboard is a ranking, not a picker; leave app/app/leaderboard.tsx untouched. Smallest honest change, two files, no migration: (1) app/app/owner/matching.tsx — add mode === 'pick' (pre-booking). Roster: when pick, load fetchCertifiedRunners() (booking-less; the same LiveRunner shape home's shelf already renders, api.ts:1403) instead of fetchAvailableRunnersFor(draft.bookingId); do NOT unify it with runners_available_for (availability predicates are frozen as three distinct definitions). nominate() in pick mode: set draft.preferredRunnerId/preferredRunnerName and router.back() — no requestRunner (there is no booking yet); request.tsx's focus effect (429, 496-499) already re-syncs preferred/preferredName from the draft, and pay() (664-670) already sends requestRunner with it after the hold. Guard the auto-nominate effect (240-260) and the rebook/live copy (332-333) with !pick; sheet CTA label 「이 러너 지명하기」, sub 「예약하면 이 러너에게 먼저 요청이 가요」 (mirrors runner-profile 755). (2) app/app/owner/request.tsx:981-983 — replace router.push('/leaderboard') with router.push({ pathname: '/owner/matching', params: { mode: 'pick' } }); delete the false 「리더보드 → 러너 프로필」 comment and say what actually opens. Keep the labels 직접 고르기 › / 다시 고르기 › — with a real picker behind them they are now true. Add the missing 지명 해제 control the alert at 1427 already promises: when preferred, a small 해제 pressable (e.g. on the ★ chip at 873-877 or as the row's secondary action) that nulls draft.preferredRunnerId/Name and calls setPreferred(null)/setPreferredName(null); otherwise the alert instructs an action with no control. Equally small alternative if matching's radar-era chrome is judged to

### owner-journey-5 · high · Terminal-booking notifications (매칭 만료 etc.) deep-link to the run report, which tells the owner 「러닝이 끝나면 여기서 기록을 볼 수 있어요」 about a booking that will never run

- `app/src/lib/notification-route.ts:448` — destinationForBookingRef default arm for owners: `return { pathname: '/owner/report', params: { bid: refId } }` for every title not in CHAT/incident/meetup/recurring sets
- `supabase/migrations/0017_expire_unmatched.sql:13` — owner-addressed '매칭 만료' notification with ref_id = booking id (also 0037:324, 0060:115)
- `app/app/owner/report.tsx:92` — STATUS_LABEL covers only matching/runner_pending/confirmed/runner_enroute/picked_up/active — no expired/cancelled_*/no_show/refund_pending
- `app/app/owner/report.tsx:690` — `report && !run` face: headline `STATUS_LABEL[report.status] ?? '진행 상황 확인 중'` + line 695 「러닝이 끝나면 여기서 기록을 볼 수 있어요」 + 내 일정에서 보기
- **Impact:** Tapping the 매칭 만료 (or a cancellation/refund) push lands on a page whose headline says 「진행 상황 확인 중」 and promises a record — the opposite of what just happened; the honest state and the next action (re-book) are one more tap away.
- **Fix (verifier-corrected):** One file: app/app/owner/report.tsx. Do NOT change the route. The proposed 「route terminal titles to /owner/schedule」 half is weaker than it claims: owner/schedule.tsx takes no bid param (no useLocalSearchParams on trunk), so the tap would land on the list with nothing selected; and the sheet's action arm for these rows is `selected.status === 'cancelled'` (schedule.tsx:1038-1042), which is the STATUS_MAP-flattened value (api.ts:1160 expired→'cancelled', :1164 refund_pending→'cancelled') and prints 「취소된 일정이에요」 for an EXPIRED booking — the flattening schedule.tsx:75-83 fixed for the badge only. It would also require flipping the pinned default at app/test/notification-route.test.cjs:67 for no gain. In report.tsx, add beside STATUS_LABEL a raw-status map `TERMINAL_FACE: Record<string,string>` = { expired: '매칭이 만료됐어요', cancelled_owner: '취소된 예약이에요', cancelled_runner: '러너가 취소한 예약이에요', no_show: '진행되지 않은 예약이에요', refund_pending: '취소·환불 진행 중이에요' } and in the `report && !run` block (:690-699) branch on `TERMINAL_FACE[report.status]`: headline from the map, body 「이 예약은 진행되지 않았어요」, and keep the existing 「내 일정에서 보기 ›」 button unchanged (it already routes, so no dead button). Gate on `report.status` because it is the RAW server string from fetchRunReportOrNull (:6365), which is the STATUS_MAP law satisfied. Keep the pre-run face for the six STATUS_LABEL keys. Two things to leave OUT: (a) no refund sentence on this face — fetchRunReportOrNull reads no payment row, so 「전액 환불」 here would be a claim with no field behind it (same reasoning as REASON.incident's deliberate no-note at report.tsx:84-88); refund_pending says 진행 중, never 완료; (b) do not wire rebook() into the terminal face in this slice — rebook() (:594-613) prefills preferredRunnerId from report.runnerProfileId, which for cancell

### owner-journey-4 · high · A booking can be held with no pickup address (server accepts NULL), and 주소 관리 lets the owner delete the default/only address with no guard

- `app/app/owner/request.tsx:513` — pay(): gates on chargeLocked, dog, scheduledAtIso, card — never on pickupAddr; line 590 `address_id: pickupAddr?.id` (undefined when addrState==='error' or the list is empty); line 1115 copy 「코스·러너·주소는 지금 안 정해도 돼요 — 위에 보이는 그대로 접수돼요」
- `supabase/functions/create-booking-hold/handler.ts:147` — `if (b.address_id) {` — ownership is checked only when present; line 378 `p_address: b.address_id ?? null`
- `supabase/migrations/0179_booking_hold_idempotency.sql:92` — `p_address uuid default null` accepted into bookings.address_id
- `app/app/owner/addresses.tsx:116` — remove(): confirm Alert → deleteAddress(a.id) with no default/only-address guard and no check for bookings referencing it; api.ts:5146 is a bare delete
- `supabase/migrations/0001_init.sql:170` — `address_id uuid references addresses` (no ON DELETE) — deleting an address referenced by a booking fails with a raw Postgres FK message shown as 「삭제 실패」
- **Impact:** After a failed address read, or after deleting addresses, an owner can pay for a run the runner cannot navigate to (meetup map 「픽업 위치가 아직 지정되지 않았어요」 for the runner too); deleting an address still tied to a confirmed booking surfaces an English FK error.
- **Fix (verifier-corrected):** Client, smallest honest change. (A) app/app/owner/request.tsx: extend the CTA ladder (786-791) and pay() (513) with two arms placed after the 반려견 arms and before 「시간부터」, mirroring the dogs grammar exactly: `addrState==='error'` → label 「주소 확인 다시」, pay() re-runs fetchAddresses (haptic('error') on failure, stays error) — never treat a failed read as 「없음」; `addrState==='ready' && !pickupAddr` → label 「주소부터」, pay() pushes /owner/addresses. Add a useFocusEffect re-read of addresses (same shape as loadDogs at 280, keep 'ready' while refreshing) or the push-to-addresses arm returns to a stale null and books address-less anyway. Drop 주소 from the 1115 copy (「코스·러너는 지금 안 정해도 돼요 — …」) so the sentence stays true. Leave the pickupAddr-with-null-lat case alone — that is the deliberate unpinned→chat path (0060/DS-6). (B) app/app/owner/addresses.tsx remove() (116): when `list.length === 1`, do not present a refusing button — render the 삭제 row's text as 「유일한 주소는 삭제할 수 없어요 · 다른 주소를 먼저 추가해주세요」 (no dead button) or hide it; and map the FK refusal in the catch (PostgrestError code '23503' — match the code, not the English text) to 「예약 기록이 있는 주소는 삭제할 수 없어요」. NOT 「이 주소를 쓰는 예약이 있어요」: the FK blocks on completed/cancelled bookings and recurring_series too, so that copy would imply a live booking that may not exist. Put the 23503 check in api.ts deleteAddress (5153) or the screen, one place. (C) Recommended belt, not a migration: supabase/functions/create-booking-hold/handler.ts:147 — refuse `address_id` absent with HttpError(400, '픽업 주소를 먼저 등록해주세요') before the ownership read; only request.tsx calls createBookingHold (recurring uses series.address_id via 0180; club has its own path), so nothing legitimate loses. Ships with `functions deploy create-booking-hold`; an older client then sees an honest

### owner-journey-1 · high · A late or overrun run strands the owner from the handoff/return seal: the hero routes every late state to 내 일정, which has no door to /owner/meetup

- `app/src/components/home-hero.tsx:190` — openNext: `if (isLate) { router.push('/owner/schedule'); return; }` runs BEFORE the state switch, so handoff/returning/confirmed-arrived all go to schedule once late
- `app/src/components/home-hero.tsx:179` — `isLate = late?.late ?? nextIsPast` — late is always an object when a booking exists, so lateness() alone decides
- `app/src/lib/lateness.ts:120` — CAN_BE_LATE includes 'picked_up' and 'active'; line 135 `started = status==='active'`; lines 145-156 due = startedAt + km*8+25min; +30min grace → late fires 31 min after scheduled (picked_up) or after the expected duration (active), with no read of run_ended_at
- `app/src/components/home-hero.tsx:356` — handoff frame's coral 인계하기 and line 381 returning frame's 인계 확인하기 both call openNext — the only in-app doors to /owner/meetup (grep: home-hero:195 and notification-route:446 are the sole pushes)
- `app/app/owner/schedule.tsx:1060` — runner_enroute sheet branch renders only 「러너가 픽업으로 이동 중이에요 — 일정 변경은 마감됐어요」 + 일정 취소하기; no meetup/seal door; line 1003 handoff branch likewise has no door
- `app/src/components/home-hero.tsx:301` — lateStrip prints 「러닝이 예정보다 N분 길어지고 있어요」 whenever late.started — in the returning state (home.tsx:384 `active && runEndedAt`) this says a run is still overrunning after the server recorded its end
- **Impact:** Runner arrives 31+ minutes late (or the return walk exceeds km*8+25+30 min): the hero's one coral action lands on the 내 일정 list, where the sheet offers only cancel; the owner cannot reach the seal screen from anywhere in the app except a push deep link. On the return, the strip also lies that the run is still running.
- **Fix (verifier-corrected):** Single file, app/src/components/home-hero.tsx, three edits; no lateness.ts / home.tsx / migration change needed.

(1) openNext (:183-197): compute `const arrivedWaiting = next?.rawStatus === 'runner_enroute' && !!next.arrivedAt` above openNext (it is currently declared later at :331 — hoist it, do not duplicate), then reorder: `if (state === 'returning') { router.push({ pathname: '/owner/report', params: { bid: next.id } }); return; }` FIRST (the return seal lives on the report — 0188; lateness never changes that); then `if (state === 'confirmed' && arrivedWaiting && late?.resumable !== false) { router.push('/owner/meetup'); return; }` (keeps T6's protection for arrived_at-null revival AND the 3h ceiling, matching runner/meetup's pastCeiling); then the existing `if (isLate) → schedule`; rest unchanged. `handoff` (picked_up) can stay under the isLate redirect — nothing is owed there and schedule's copy is true; leave its label to Q4.

(2) Confirmed button (:490-497): label must match the new destination (T6 ①'s own law). Use `const lateDoorClosed = isLate && !(arrivedWaiting && late?.resumable !== false)` and gate title/sub/ground on it instead of bare isLate, so the arrived-and-resumable case shows the same 「티켓 보기 / 시간과 장소를 확인해요」 gold button the not-late arrived case already shows → meetup. Do NOT add coral here (Q4 is Sean's).

(3) Returning frame (:370-387): remove `{lateStrip}` from the returning branch only. Its phrase already states the run ended; the elapsed-return fact is owned by the report and the 0193 strand sweep, and the strip's sentence is false there. (Alternative if the fact must stay: add `runEndedAt` to LateInput, pass `liveNext.runEndedAt` from home.tsx:388, and print a distinct 「귀가 확인이 아직이에요 · N분 지남」 arm — more surface, not the smallest change.)

Opti

### runner-journey-1 · high · Runner home swallows a failed jobs read — a runner holding a dog sees no ticket, no error

- `app/app/runner/home.tsx:405` — loadJobs = Promise.all([fetchRunnerJobs(), fetchInFlightRunnerJobs()]) … .catch((e) => console.warn(...)) — no error state is set; jobs stays []
- `app/app/runner/home.tsx:626` — current = jobs.find(...) — with jobs [] there is no 진행 중 ticket, no CTA, no chat row, no 오늘의 루트
- `app/app/runner/home.tsx:380` — the same file already three-states inbox/status/avail (inboxErr, rsErr, availErr) precisely because 'a failed fetch printed a quiet day'
- `app/src/lib/api.ts:2036` — fetchInFlightRunnerJobs throws on error with the comment '실패가 진행 중 없음으로 위장하면 러너 홈이 개를 데리고 있는데 비었다고 말한다' — the throw exists, the screen ignores it
- **Impact:** On flaky LTE a runner mid-pickup or mid-귀가 opens home and it looks like they have no work at all: no 인계 화면으로/반환 봉인 door, no chat row, no late notice. The only remaining door is the work-gate strip (which has its own read). The runner cannot tell 'nothing to do' from 'could not load'.
- **Fix (verifier-corrected):** app/app/runner/home.tsx only. (a) Add `const [jobsErr, setJobsErr] = useState(false);` beside `jobs` (:400). (b) In `loadJobs` (:405-415): call `setJobsErr(false)` at the top, `setJobsErr(false)` again inside `.then` after `setJobs`, and `setJobsErr(true)` in the `.catch` (keep the warn). Leave `setJobs` where it is — a failed refresh already keeps the last-known list. (c) Render one strip in the 진행 중 slot, i.e. immediately before the `<SectionHead title="요청 대기열" …/>` at :1152, guarded `{jobsErr && jobs.length === 0 && (…)}`: a `paper.criticalWash` View with `<Text fontSize 15 fontWeight '700' color paper.critical>진행 중인 일을 불러오지 못했어요</Text>` and a `<Pressable onPress={loadJobs} hitSlop={8} style={styles.togRetry} accessibilityRole="button" accessibilityLabel="진행 중인 일 다시 불러오기"><Text style={styles.togRetryTxt}>다시 시도</Text></Pressable>` — the exact grammar of the status strip at :828-829 and calendar.tsx:363-369. (d) No change to the 오늘 row, 오늘의 루트 or 최근 완료: with `jobs` empty they already do not draw, so nothing false is printed there. Optionally fold the :722-724 comment to say failure now has its own strip. Gates: tsc, check-a11y-roles (the new Pressable carries a role), check-rpc.

### runner-journey-3 · high · Receipt (done.tsx) asserts the wrong phase: '인계해 주세요' on settled runs, '러닝 화면에서 다시 정산' during the return ceremony, coral 다음 요청 while gated

- `app/app/runner/done.tsx:371` — the 인계 sentence '{dog}를 보호자에게 안전하게 인계해 주세요' is gated only on the ops-resolution strip, not on v.settled / rawStatus — it renders on a completed receipt opened from earnings days later
- `app/app/runner/earnings.tsx:520` — every ledger row opens /runner/done?bid= — the settled path is the common entrance now
- `app/app/runner/done.tsx:430` — !v.settled strip: '이 숫자는 앱이 계산한 추정치예요 … 러닝 화면에서 다시 정산하면 실제 금액으로 확정돼요' — on the bid path payout is the ledger row or '—' (:148), never an estimate, and the run screen cannot settle
- `app/app/runner/run.tsx:805` — '[0188] 종료는 더 이상 정산이 아니다 … 정산은 양측 반환 스탬프의 두 번째 것이 서버에서 수행한다' — the strip's instruction points at a door that no longer does that
- `app/app/runner/return-seal.tsx:488` — frame b (dog still with runner, owner not stamped) pushes into /runner/done?bid= via 기록 먼저 보기 — this is the state where the strip renders
- `app/app/runner/done.tsx:565` — primary coral '다음 요청 보기 ›' → /runner/requests, whose accept doors 409 under the work gate while the return is unsealed; no door back to the seal screen exists on this receipt
- **Impact:** A runner mid-귀가 reads a loud red strip telling them their money is an app estimate and to 're-settle from the run screen' — both false — and the only coral button sends them to accept new work the server refuses. Days later, opening any past run from 수익 tells them to hand the dog back.
- **Fix (verifier-corrected):** Single file: app/app/runner/done.tsx. Smallest honest change, all inputs already fetched.

1. Carry one more server fact. Add `runnerConfirmedAt: string | null` to `Receipt` (done.tsx:95-109); set it from `seal.runnerConfirmedAt` in loadReceipt (:143-155, fetchReturnSeal already returns it, api.ts:1752) and `null` in the store fallback (:283-290).

2. Compute one phase, after `v` (:283):
   const phase = !paramBid ? 'estimate' : v.settled ? 'settled' : inCustodyPhase(v.rawStatus) ? 'custody' : 'pending';
   ('estimate' = run.tsx:860 freeze-failed store path; 'custody' = active/picked_up on the bid path, i.e. return-seal frame b; 'pending' = incident_review / refund_pending etc. on the bid path.)

3. Headline sub-line (:371-378, inside the `!strip` branch; the ops resolution strip still wins when present):
   - estimate → keep the 인계 sentence (dog is with the runner, run not frozen).
   - custody → `v.runnerConfirmedAt ? '보호자 확인을 기다리는 중이에요' : <인계 sentence>`.
   - settled / pending → render nothing.
   Rewrite the stale comment at :361-363 (return-seal now exists).

4. Money label (:426): `v.settled || v.payout != null ? '적립 예정' : phase === 'estimate' ? '예상 수익 (정산 미완료)' : '정산 전'` — a ledger row is a server-confirmed net even on a refund_pending row; '—' beside '예상 수익' is not an estimate.

5. failStrip (:430-437): gate on `phase === 'estimate'` only, and fix its wording to the real door: '러닝 종료가 아직 서버에 확정되지 않았어요 — 이 숫자는 앱이 계산한 추정치예요.\n러닝 화면에서 종료를 다시 시도하면 인계 확인으로 이어지고, 양측 확인 뒤 실제 금액으로 확정돼요.' For custody the existing moneyNote ('정산이 확정되면 수익 화면에 반영돼요', :420-424) is already true and sufficient; for pending replace the note with return-seal's own sentence '정산은 담당자 확인 뒤에 진행돼요'. No criticalWash in either.

6. Exits (:558-569): when phase === 'custody', draw the primary PaperBtn a

### runner-journey-2 · high · Calendar sends a run-ended (귀가) job to the live run screen, and the run screen has no 'already ended' guard

- `app/app/runner/calendar.tsx:129` — openJob: router.push(j.rawStatus === 'active' ? '/runner/run' : '/runner/meetup') — no homewardReturnOpen/runEndedAt check
- `app/app/runner/calendar.tsx:204` — in_progress caption '탭하여 러닝 화면 ›' for every in-flight status including run-ended active
- `app/app/runner/home.tsx:196` — stageFor(j) = homewardReturnOpen(j) ? 'returning' : rawStatus, and openJob (:672-677) routes 'returning' to /runner/return-seal — the calendar was left on the two-destination rule
- `app/app/runner/run.tsx:1079` — startRun starts GPS tracking and calls startRunServer; nothing in the mount effect (:297-327) or render reads run_ended_at, so a stopped run is offered 러닝 시작
- `app/app/runner/return-seal.tsx:257` — the seal screen has the inverse guard ('아직 러닝이 끝나지 않았어요' → 러닝 화면으로); the run screen has no mirror
- **Impact:** A runner who has stopped the run and is walking the dog home taps the job in 캘린더 (or the 'TODAY' ticket) and lands on a run screen labelled '초코와 러닝 준비' with a volt 러닝 시작 button — tapping it restarts GPS tracking on a frozen run and the way to the return seal is gone. The home ticket and the calendar disagree about the same booking.
- **Fix (verifier-corrected):** Two files, mirroring code that already exists. (1) app/app/runner/calendar.tsx: add `import { homewardReturnOpen } from '../../src/lib/custody-ping-policy';` and make openJob (:126-130) the same three-way as home.tsx:672-677 — `homewardReturnOpen(j) ? { pathname: '/runner/return-seal', params: { bid: j.bookingId } } : j.rawStatus === 'active' ? '/runner/run' : '/runner/meetup'`; make the in_progress caption at :203-205 follow the same predicate — `homewardReturnOpen(j) ? '탭하여 반환 확인 ›' : j.rawStatus === 'active' ? '탭하여 러닝 화면 ›' : '탭하여 인계 화면 ›'` (gate on rawStatus/runEndedAt, not on the flattened status; the 진행 중 chip can stay). (2) app/app/runner/run.tsx: in the hydrate effect (:297-327), immediately after `const bid = runnerJob.bookingId; if (!bid) return;`, call the EXISTING `fetchRunPhase(bid)` (api.ts:1671, already the owner's guard at owner/live.tsx:256-278) and `if (status === 'active' && runEndedAt) { router.replace({ pathname: '/runner/return-seal', params: { bid } }); return; }` before loadInfo/refreshStartedAt/trace seeding; on a thrown read do NOT redirect (fall through to the existing infoStatus error strip) — a failed read must not manufacture a redirect, and return-seal's own inverse guard covers a stale push. Do not add fields to fetchRunMeta and do not call fetchReturnSeal from run.tsx (heavier joined read; fetchRunPhase is the two-column read purpose-built for this). Out of scope but worth a note in the commit: start_run.ts:52-59's unconditional owner push is the server-side amplifier of this defect and its comment records the suppression as a client-slice question — the client guard closes the path; leave the edge function alone.

### runner-journey-5 · high · A runner cannot withdraw from a confirmed booking anywhere, and a dead confirmed booking occupies home forever

- `supabase/functions/transition-booking/index.ts:3` — action list: runner_accept | runner_decline | request_runner | enroute | arrived | confirm_handoff | start_run | end_run | confirm_return | resolve_return | cancel_owner — no runner-side cancel
- `supabase/migrations/0001_init.sql:205` — transition table permits confirmed → cancelled_runner and runner_enroute → cancelled_runner; the state exists with no writer
- `app/app/runner/meetup.tsx:365` — pastCeiling hides every CTA (:627, :660, :674) and leaves only the late strip '보호자와 먼저 확인해 주세요' — no close/withdraw door
- `app/app/runner/home.tsx:626` — current falls back to jobs.find(rawStatus === 'confirmed') with no date bound; relWhen (:169) prints '지난 예약' but the ticket and coral '픽업 이동 시작 ›' stay
- `app/src/lib/api.ts:1310` — 'a confirmed booking in the past has NO expiry cron of its own (0017 only sweeps matching/runner_pending), so it strands permanently'
- **Impact:** A runner who gets sick the day before, or whose owner never appears and never cancels, has no action: home keeps a red '지난 예약' ticket as their one next action indefinitely, 요청 대기열 sits under it, and the owner learns nothing until the check-in protocol or their own cancel fires.
- **Fix (verifier-corrected):** LETTER FOR SEAN (one question): may a marketplace runner withdraw from a CONFIRMED booking before the appointment, and with what consequence? (a) YES → add `cancel_runner` to supabase/functions/transition-booking/index.ts for marketplace `confirmed` (and `runner_enroute`) bookings: runner-gated CAS to `cancelled_runner`, `cancel_reason = 'runner_withdraw'`, a `booking_faults` row (party runner) so 0117 §9's carve-out and any 완주율/strike rule can read it, notify the owner 「러너가 예약을 취소했어요」 with a re-request door; NO owner fee (0220 §0c already maps cancelled_runner → no_charge/cancelled_by_runner, so the owner-side copy exists). Migration needed only if `booking_faults.source` or the cancel_reason check must admit a new value — the transition edge itself is already legal. Do NOT reuse the 0066 ladder (owner-only). (b) NO → runners never withdraw, ops resolves: then fix the runner_decline 409 text at index.ts:343 (it currently directs the runner to a cancel path that does not exist) and give the runner a chat-first door 「진행이 어려워요 → 보호자에게 알리기」 so the situation has a live action. CLIENT DOOR (either way, after the ruling): a destructive secondary action on the runner HOME ticket (app/app/runner/home.tsx, next to the existing CheckinAnswer mount at :966) and on app/app/runner/calendar.tsx job rows — NOT meetup.tsx (frozen stage machine; the file's own comment at :222-231 says the same). DROP the proposed home.tsx date-bound change: the 0117 clock (live since 2026-08-25) already bounds a past confirmed marketplace ticket to ≤3h10m and opens a runner-answerable check-in at +30min. INSTEAD fix the two stale comments the finding inherited: app/src/lib/api.ts:1310 ('strands permanently — 0017 only') and api.ts:497 ('until Sean sets it') — both predate or ignore the 0126 ⑥ flip and w

### runner-journey-7 · high · Opening the meetup screen IS the 출발 event — any tap on a confirmed job within 24h tells the owner '러너 이동 중'

- `app/app/runner/meetup.tsx:232` — mount effect: runnerEnroute(jobId).catch(() => {}) fires on every mount, before the runner has done anything
- `supabase/functions/transition-booking/index.ts:362` — enroute arm flips confirmed → runner_enroute and notifies the owner '러너가 픽업 장소로 출발했어요' whenever scheduled_at is within 24h (:369-371 refuses only beyond 24h)
- `app/app/runner/home.tsx:973` — the whole ticket body is a Pressable → openJob → meetup; so is the calendar ticket (calendar.tsx:236-239) and the '인계 확인 요청' push route
- `app/app/runner/home.tsx:148` — stageSub('confirmed') = '{dog}에게 출발할 시간이에요' regardless of how far away scheduled_at is; relWhen returns null beyond 24h (:170) so a job 3 days out still shows the coral '픽업 이동 시작 ›'
- `app/app/runner/meetup.tsx:222` — the file itself records this as an open door ('열기만 해도 뒤집히는 문은 열려 있다')
- **Impact:** A runner checks tomorrow-morning's address the night before by tapping the ticket; the owner gets a push that the runner has left, and the booking is now runner_enroute (which also unlocks the address window and changes the late/no-show protocol's premise). Nothing on the runner side said 'this will notify the owner'.
- **Fix (verifier-corrected):** Product call for Sean (the meetup surface is his frozen zone and this changes what 출발 means), but the code shape does not touch the frozen parts. (1) app/app/runner/meetup.tsx — delete the single line `runnerEnroute(jobId).catch(() => {...});` at :234; leave the effect's deps, syncNow(), subscribeBooking and the 8s poll byte-identical. Append `const [departed, setDeparted] = useState(false)` at the END of the state bundle (hook-placement freeze, after `escalation` :126), and in syncNow set `setDeparted(s2.status !== 'confirmed')` beside setArrived (:200) — server truth, same posture as `arrived`. In the existing `!pastCeiling && stage === 'enroute'` action block (:660-671), render when `!departed` a PaperBtn '픽업 장소로 출발 ›' whose handler awaits runnerEnroute(jobId) (server is idempotent: {unchanged:true}) then setDeparted(true), with hint '출발을 알리면 보호자에게 알림이 가요' and a Korean server 409 surfaced inline via the same `/[가-힣]/` idiom as reportArrived (:254-259); when `departed`, render the existing 도착 button unchanged. Dead-button law: show the 출발 button only when `info?.scheduledAt` (:345) is within the server's 24h window (transition-booking:368-371); beyond it render the fact as text ('출발 알림은 러닝 시작 24시간 전부터 보낼 수 있어요'), and render nothing in that slot while info is loading. No new Stage value; the 4-stage machine, polling and confirmHandoff are untouched. Rewrite the now-stale comment at :222-231. (2) app/app/runner/home.tsx — make the confirmed copy time-aware at the render site (:943/:1075): when relWhen(scheduledAt) is null (>24h) or beyond a short window, stageSub returns '{dog} 픽업 정보 보기' and the CTA reads '픽업 정보 보기 ›'; '출발할 시간이에요' / '픽업 이동 시작 ›' only inside the window. (3) No server change: keep the 24h gate and the CAS notify. (4) Update docs/plans/2026-08-21-late-book

### owner-journey-6 · medium · After a cancel, expiry or runner no-show the schedule sheet dead-ends at 「더 진행할 작업이 없어요」 — no re-book door, unlike the completed sheet

- `app/app/owner/schedule.tsx:1042` — cancelled branch renders only 「취소된 일정이에요 — 더 진행할 작업이 없어요」 (expired maps to 'cancelled' via STATUS_MAP api.ts:1159)
- `app/app/owner/schedule.tsx:1049` — no_show branch: 「불발로 처리된 일정이에요 — 더 진행할 작업이 없어요」
- `app/app/owner/schedule.tsx:1034` — the completed branch has 「⟳ 이대로 다시 예약」 prefilling draft.km/pace/runner — the same row is absent from the terminal branches
- `app/app/owner/radar.tsx:238` — expired exits with 「…요청이 만료됐어요 — 새로 예약해주세요」 to /owner/schedule, where the only new-booking door is the list-bottom ＋ 새 러닝 예약하기, not on the booking itself
- **Impact:** The most common recovery (book the same run again after a runner cancelled / nobody accepted / a no-show) has no one-tap path from the booking the owner is looking at; they must scroll out and re-enter every field.
- **Fix (verifier-corrected):** One file plus one ledger line. In app/app/owner/schedule.tsx (re-anchor on the worktree: branches now at ~1064-1078): (1) hoist the completed branch's rebook onPress into a single `rebook()` closure over `selected` and render ONE rebook Pressable (label 「⟳ 이대로 다시 예약」 verbatim, `accessibilityRole="button"`) in three places — completed (behaviour unchanged), `selected.status === 'cancelled'` (covers cancelled_owner · cancelled_runner · expired · refund_pending via STATUS_MAP), and the `rawStatus === 'no_show'` half of the no_show/incident_review branch, placed under the existing state sentence which stays. Do NOT add it to `incident_review` — it is post-custody and 0193 lets ops return it to `active`. (2) Runner prefill: `preferredRunnerId = selected.rawStatus === 'cancelled_owner' ? selected.runnerProfileId ?? null : null` (and preferredRunnerName likewise) — cancelled_runner keeps runner_id server-side, no_show fault is either side (0117), refund_pending may follow an incident, expired-from-runner_pending is a runner who never answered; only cancelled_owner is a state where the runner was fine. The sub-line must key off the COMPUTED prefill, not `selected.runnerProfileId`, so it never promises a 「X 러너 지명」 the draft will not carry: 「같은 거리·페이스{prefilled ? ` · ${name} 러너 지명` : ''} — 시간만 골라요」. (3) Leave draft.routeId alone — neither existing door (this branch, home.tsx:473-484 rebookLast) sets it; widening prefill semantics across three doors is a separate slice. (4) app/scripts/check-a11y-roles-baseline.txt:65 `app/owner/schedule.tsx 16` → 15 once the existing bare rebook Pressable gains its role (the gate fails on a count below the ledger). Gates: tsc, check-a11y-roles, check-device-clock; smoke on simulator: open a cancelled, an expired and a no_show row → sheet shows se

### owner-journey-8 · medium · Runner ranking is labelled 「AI 추천 순 / AI 1순위」 but is a fixed client formula (62 + runs×5, pace distance/4)

- `app/app/owner/matching.tsx:57` — matchFor(): line 66 `exp = min(97, 62 + totalRuns*5)`, line 69 `paceFit = max(58, 100 - |Δpace|/4)`, weighted 0.35/0.3/0.35 — constants chosen in the client
- `app/app/owner/matching.tsx:358` — rail label 「러너 N명 · AI 추천 순」; line 493 tag 「AI 1순위」; line 393 「1순위가 AI 추천이에요」
- **Impact:** The owner is told a model recommended the runner; the percentages in the 응답 신뢰도/러닝 경험/페이스 적합 bars read as measured scores while two of the three axes are synthetic — an honesty-law breach on the one screen where the owner picks who takes the dog.
- **Fix (verifier-corrected):** Split into two halves; only the second needs Sean. (A) Copy-only, no ruling needed — removing a false mechanism claim is the honesty law, not taste: app/app/owner/matching.tsx:358 「러너 {n}명 · AI 추천 순」 → 「러너 {n}명 · 추천 순」; :393 delete the clause 「— 1순위가 AI 추천이에요」 (the sentence already says 세 축을 합쳐 순위를 정해요); :493 'AI 1순위' → '추천 1순위' (keep the topIsPreferred ? '내가 고른 러너' arm). Optionally align the internal comments at :192/:264/:278 and the s.aiTag style name at :694; no data path moves, matchFor is untouched. (B) Sean's call (F23/L-4, already queued; M② said 'the three bars stay' so this reverses part of his pick): keep the Bar track only for 응답 신뢰도 (real respond_rate_pct) and render the other two rows as facts with no track — 러닝 경험 → 「{totalRuns}회」 (null → 「신규 · 데이터 없음」 as today), 페이스 적합 → 「러너 {paceLabel} · 요청 {targetPaceLabel} ({signed Δ}초)」 using the `signed` helper already at :39 and sel.r.paceSec vs targetPaceSec (null → 「기록 전」). matchFor keeps computing exp/paceFit as the ORDERING key only (the M② contract) and `partial` keeps its disclosure line at :521-525. Alternative if Sean prefers bars: relabel the two rows 「러닝 경험 (추정)」/「페이스 적합 (추정)」. No migration; no frozen zone touched; a11y unaffected (Bar is presentational, no Pressable).

### owner-journey-7 · medium · Two permanent 「준비 중」 placeholders in the schedule booking sheet promise things the app does not ship

- `app/app/owner/schedule.tsx:777` — a 110pt box labelled 「코스 지도 준비 중」 renders on EVERY booking sheet, unconditionally (no route fetch, no trace) — the request screen already draws the same course via HeatTrace(fetchRouteById)
- `app/app/owner/schedule.tsx:995` — active sheet: 「바디캠 뷰는 준비 중이에요」 — no bodycam feature exists (matching.tsx:551 says 영상 제공은 아직 지원하지 않아요)
- **Impact:** Every booking the owner opens shows a grey 'coming soon' plate above the numbers, and the live sheet advertises a video view that does not exist — clutter that trains the owner to ignore the sheet (DESIGN §7b, §7).
- **Fix (verifier-corrected):** File: app/app/owner/schedule.tsx only. DO NOT touch the 바디캠 line (:1019-1021, Sean D3=B; re-raise to Sean separately if wanted). Map plate: mirror request.tsx's three-way split in the sheet's route card. (a) Add a per-sheet effect keyed on `selected?.id`/`selected?.routeId`: when `routeId` is non-null, `fetchRouteById(routeId)` with an `alive` guard (pattern request.tsx:477-483) into state `{state:'loading'|'ready'|'error', trace}`; reset when `selected` changes. (b) Render: `routeId == null` → render NOTHING (the name line already says 코스 미지정; do not repeat it); `loading` → keep the 110pt slot with text 「불러오는 중」 (request.tsx:941 vocabulary — loading is not 준비 중); `error` → loud-fail: one `paper.critical` line + 「다시 시도」 Pressable with accessibilityRole="button", same grammar as the payment strip at schedule.tsx:985-996 (never a silent fallback to 준비 중); `ready` and `traceToBox(trace).length > 1` → `<View style={{backgroundColor:'#0e150f', marginTop:10}}><HeatTrace points={traceToBox(trace)} width={W} height={92}/></View>` — HeatTrace requires explicit width/height (request.tsx:68) and a DARK plate (request.tsx:933, s.routeMap/s.nudgeMap bg '#0e150f'); use the list-card 92 (request.tsx:1311) or the original 110 (d06969e drew 278×110), not 48 which makes a route shape illegible; `ready` with <2 points → keep the existing 「코스 지도 준비 중」 plate (the one case where the words are true, per request.tsx:1309-1315 and course/[id].tsx:281). Imports: `fetchRouteById` from '../../src/lib/api', `HeatTrace` from '../../src/components/runcard', `traceToBox` from '../../src/lib/trace' (returns [] for <2 valid points). (c) Rewrite the stale deferral comment at :296-299 so it no longer says the helper does not exist (CLAUDE.md: a comment describing a fix as pending is a false green). Gates:

### owner-journey-12 · medium · Login asserts consent to 이용약관 and 개인정보 처리방침 that exist nowhere in the app

- `app/app/login.tsx:113` — static Text 「로그인하면 이용약관과 개인정보 처리방침에 동의하게 돼요」 — not a Pressable, no route
- `app/app/login.tsx:1` — grep over app/app and app/src: this is the ONLY file mentioning 이용약관; no terms/privacy/legal route exists in app/app (ls shows none) and settings.tsx has no 약관 row
- **Impact:** The owner is told they agreed to documents they cannot read; App Store review and the 개인정보보호법 both expect a reachable privacy policy from the sign-in and settings surfaces.
- **Fix (verifier-corrected):** Split into two phases; only the first is doable now, and it is Sean's wording call (user-facing legal copy is on his list; the handoff and type-density audit both mark this line as his). PHASE NOW (one file, no migration): in app/app/login.tsx:113-115 remove the sentence 「로그인하면 이용약관과 개인정보 처리방침에 동의하게 돼요」 (and the orphaned s.legal style at :139), or replace it with a sentence that is true today — the same reasoning apply.tsx:715-716 already applies on the runner side. Nothing is lost: readiness-review-2026-08-19.md:460 records that the owner's login consent is persisted nowhere, so the line currently records nothing and only asserts. Do NOT add app/app/legal/[doc].tsx from docs/legal/*.md drafts — they are unreviewed, 시행일 미정, and the console-rulings gate says no session drafts or publishes that text. PHASE LATER (rides session-handoff.md open item #1 — counsel returns, 시행일 set, ToS/privacy published): add app/app/legal/[doc].tsx as a static screen rendering the PUBLISHED text (doc ∈ {terms, privacy}), make the login line's two nouns Pressables (accessibilityRole="link") into /legal/terms and /legal/privacy, add one actionRow Pressable in app/app/settings.tsx after 알림 설정 (~:165) labelled 이용약관·개인정보 처리방침, and — per legal-ops-domain.md §2.10/§5.2 and readiness ③ — record owner consent with a version using the existing doc_version idiom (0030/0040) rather than re-shipping an implied-consent sentence. That later phase needs a migration (consent record), so the finding's needs_migration:false holds only for the now-phase edit. The App Store 5.1.1 privacy-link half of the user_impact is a launch gate already tracked as handoff open item #1, not a separate gap.

### owner-journey-11 · medium · 15pt Korean floor breached across fitness, payments and 마이 (14 / 14.5 pt prose, with stale '14pt floor' comments)

- `app/app/owner/fitness.tsx:508` — fitFailTxt/fitFailRetry 14; 522 mastSub 14 (「{dog} · 이번 주」); 551 emptyCopy 14 (「아직 완료된 러닝이 없어요…」); 554/560 bandLabel/bandCap 14 (「이번 주」「체력 나이」); 570/572/580/583/592/600/603/618 punchLabel/statLine/cardSub/ageGate/goalHint/barVal/barX/runMeta all 14 — Korean prose, dim on lilac
- `app/app/payments.tsx:308` — note 14.5 (「카드 연결은 결제 오픈과 함께 열려요」 etc.); 310 failTxt 14 (loud-fail text below floor)
- `app/app/my.tsx:546` — fldV2 14 (district · dog line); 577 recL 14 (「이번 주 거리」 labels); comments at 532/543/571 still cite a '14pt floor' that DESIGN.md:145 raised to 15 on 2026-08-25
- **Impact:** The screens Sean called illegible at 14 still carry it: the whole fitness page's detail layer, every note on 결제 관리, and the passport's data lines.
- **Fix (verifier-corrected):** Three files, no migration, no decision needed (DESIGN.md:145 + 2026-08-31 '15 everywhere' already rule it).

1) app/app/my.tsx — RESTORE the swept style block rather than re-derive it. `git diff 4f4bbed HEAD -- app/app/my.tsx | grep -E '^[-+].*(fontSize|floor|플로어)'` is the exact 22-line list lost by merge 4363daf: microK, roleTagTxt, fldK, fldVSmall, fldV2, idEditTxt, idEditEm, recordKick, recU, recL, recGo, recFailTxt, recFailRetry, secKo, visaFootG, emptT(16)/emptD, drowTitle(16)/drowDesc, btnRoleSwTxt, signoutTitle(16)/signoutSub, fieldLabel → 15 (parents to 16 where the sweep did, to keep hierarchy); comments at 532, 543, 553, 571, btnRoleSwTxt and secNo's '12pt 플로어 면제' → '15pt'. Apply only these style-line deltas — the profile branch also legitimately added recWord 15/28 and restructured the screen; do NOT wholesale-revert. Leave gridK 11.5 / gridV 13 (Latin 'TYPE'/'NO.', 'P · RUNNER', 'DH-xxxx' serial) and mrzTxt 9 (MRZ) as exempt. Record in the commit that this is a re-land of 4f4bbed's my.tsx hunk lost in 4363daf.

2) app/app/payments.tsx:308 note 14.5 → 15 (lineHeight 20 stays); :310 failTxt 14 → 15 and add lineHeight 20 (it has none).

3) app/app/owner/fitness.tsx — the hero geometry is arithmetic, so split the file in two:
   a) INSIDE the pinned hero (mastSub :522, bandLabel :554, bandCap :560, punchLabel :570, statLine :572): fontSize 14 → 15 and KEEP lineHeight 18 (15/18 = 1.20×, the same pairing the sweep used for fldV2/microK/recFailTxt). Then HEAD_H/BAND_H/PUNCH_H/STAT_H/HERO_BIG/COLLAPSE at :39-60 are all unchanged; add a '[FLOOR15 2026-09-xx] lineHeight held at 18 so the constants stand' line to the header, next to the FLOOR14 note. Do not touch the Animated/collapse code.
   b) OUTSIDE the hero: fitFailTxt/fitFailRetry :508-509 → 15/18 (or 15/20); em

### runner-journey-4 · medium · A directed (지명) request can only be declined from home's front ticket — 요청함 has no 거절 door

- `app/app/runner/requests.tsx:9` — imports acceptBooking, acceptReschedule, declineReschedule … — declineBooking is not imported
- `app/app/runner/requests.tsx:589` — doorRow renders a single 수락하기 Pressable for every card, directed or open
- `app/app/runner/home.tsx:476` — declineFront reads inbox[0] only; :1304-1315 the 거절 door exists only on the featured ticket, stubs (:1353-1360) go to 요청함 with '보기 ›'
- `app/src/lib/api.ts:1576` — declineBooking = invokeTransition(id, 'runner_decline') exists and returns runner_pending → matching
- `app/src/lib/api.ts:1307` — a runner_pending request expires only when scheduled_at < now() — until then the owner waits on a runner who cannot say no
- **Impact:** With two directed requests, the second one cannot be declined anywhere: the runner sees 수락하기 only, and the owner sits on '러너 응답 대기' until the start time passes and the booking expires instead of returning to the pool the same minute.
- **Fix (verifier-corrected):** app/app/runner/requests.tsx only (no migration, no server change, no api.ts change). (1) Import declineBooking from ../../src/lib/api. (2) Add a `declining: string | null` state beside `accepting` (:144); a single bookingId cannot tell the two doors of one card apart, which is exactly why reschedAct exists (:151-153) — derive `busyAny = accepting !== null || declining !== null` and make `inert`/`disabled` on BOTH doors read busyAny, with the acting door identified by (bookingId × action) so the non-acting door renders disabledFill+faint per theme.ts:206, not an opacity trick. (3) In doorRow, for `req.directed` only, render a ghost (ink-outline, s.doorGhost) 거절 door beside 수락하기 — never coral, so the 변경 요청 > 지명 > nothing ladder is byte-identical. Copy is home.tsx:481-497 verbatim: Alert.alert('지명 거절', '이 요청을 다른 러너에게 넘길까요?\n보호자에게는 재탐색 알림이 가요.', [{text:'유지',style:'cancel'},{text:'거절',style:'destructive'}]); route the confirm through the existing `asking` guard (:154) so a double-tap cannot stack alerts; on confirm set declining=req.bookingId, await declineBooking(req.bookingId), haptic('light'), then load(); on failure Alert.alert('거절 실패', (e as Error).message) and load(); finally declining=null. Label swap while busy ('거절 중…'), no spinner. (4) Give the new Pressable accessibilityRole="button" — check-a11y-roles-baseline.txt:70 freezes requests.tsx at 6 bare Pressables and a 7th fails the commit gate. (5) Keep open-pool cards accept-only (lab :163: no 거절 concept on an open request). (6) Do not add a client-side declinedIds set here: the directed leg reads runner_id = me AND status = runner_pending, and runner_decline nulls runner_id, so load() drops the card by server truth; the open-leg exclusion is 0056's. Optional, only if the fail-open 0056 log write is a concern: lift 

### runner-journey-6 · medium · 요청함 draws live 수락하기 doors while the work gate is shut; home already draws the blocked sentence

- `app/app/runner/requests.tsx:604` — accept door is disabled only by accepting !== null; the screen never reads the gate (no fetchRunnerWorkGate import at :9)
- `app/app/runner/home.tsx:1263` — gate?.gated → styles.doorBlocked '반환 확인이 끝나면 여기서 수락할 수 있어요' instead of a coral door
- `app/app/runner/home.tsx:1115` — the R1c strip explains WHY and offers the exit; 요청함 offers neither
- `app/src/lib/api.ts:1868` — fetchRunnerWorkGate returns gated/bookingId/waitingOn — reusable as-is
- **Impact:** After a run the runner opens 요청함, reads five cards, taps the coral 수락하기, confirms the Alert, and gets a 409 sentence — the exact dead-after-tap door the honesty law names, on the screen where accepting is the only job.
- **Fix (verifier-corrected):** One file: app/app/runner/requests.tsx. (a) :9 add `fetchRunnerWorkGate, RunnerWorkGate` to the api import. (b) Mirror home.tsx:532-537 exactly: `const [gate, setGate] = useState<RunnerWorkGate | null>(null); const [gateKnown, setGateKnown] = useState(false);` and in `load()` (:326), as an independent side load like status/availability/jobs: `fetchRunnerWorkGate().then((g) => { setGate(g); setGateKnown(true); }).catch((e) => { console.warn('[requests] gate:', e?.message ?? e); setGate(null); setGateKnown(false); });` — the focus reload and the post-409 `load()` in commitAccept's catch (:403) then flip the door with no extra wiring. (c) Say WHY and offer the exit ONCE, not per card: directly above the first request card (after the base-pin door; NOT above the reschedule cards at ~:680-770, whose `accept_reschedule` path is not gated and must stay live), render home's R1c strip when `gate?.gated && loaded && !loadErr && live.length > 0` — WHY copy keyed on `gate.rawStatus === 'incident_review'` / `gate.waitingOn` taken verbatim from home.tsx:1131-1143, and the exit `반환 봉인 찍기 ›` → `router.push({ pathname: '/runner/return-seal', params: { bid: gate.bookingId } })` under home's exact predicate `gate.waitingOn !== 'owner' && !!gate.bookingId` (home.tsx:1120-1123, :1146). Copy `gateStrip/gateDot/gateWhy/gateSub/gateExit` (home.tsx:2067-2075) into `s` — they are a local StyleSheet, not importable — and give the strip `accessibilityRole="button"` with `disabled` when there is no exit. (d) In `renderRequest`'s doorRow (:589-609) and only there: `gate?.gated ? <View style={[s.doorGhost, s.doorOff]}><Text style={{ fontSize: 15, fontWeight: '700', color: paper.text, lineHeight: 20 }}>반환 확인이 끝나면 수락할 수 있어요</Text></View> : <Pressable …unchanged…/>`. Keep the `<Pressable>` element in sou

### owner-journey-15 · low · Handoff screen foot advertises pet insurance under negotiation as a safety assurance

- `app/app/owner/meetup.tsx:770` — Pressable → /safety: 「펫보험 파트너십 협의 중 — 사고 시 안심 센터에서 바로 도와드려요」 on every stage of the seal screen
- **Impact:** At the moment the owner hands over the dog they read an insurance line about a partnership that does not exist yet; what users are told about safety is Sean's call (CLAUDE.md §Operations).
- **Fix (verifier-corrected):** Step 1 (Sean, one question, no code): is there an active pet-insurance partner negotiation today? This is the '[Sean 확인 필요 — 보험사 접촉 여부]' flag already open in docs/investor/fact-pack.md:133; answer it there so the app and the deck stop disagreeing. Step 2a (if YES): no change — the copy is true and the finding closes. Step 2b (if NO): one commit touching all three surfaces so the 'one sentence everywhere' invariant holds: app/app/owner/meetup.tsx:770 and app/app/runner/meetup.tsx:762 → 「사고가 나면 안심 센터에서 바로 도와드려요 ›」 (keep the Pressable → /safety; do NOT delete the foot — it is the only non-hold safety door on the owner handoff screen); app/app/safety.tsx:206 펫보험 InfoRow → either omit the row (honesty law: bind real fields or omit) or 「아직 보험이 적용되지 않아요」 — Sean's wording. Update the comment at owner/meetup.tsx:767-768 and the spec's smoke line (honesty-batch-sunbaek-spec.md:380) so the next grep expects zero '협의 중'. Verify by executable-line grep for '협의 중' across app/app = 0 after the edit, not by reading the comment.

### owner-journey-13 · low · A returning owner re-picks their role on every cold launch even though the server already knows it

- `app/app/index.tsx:137` — RoleSelect always renders the two halves; the only redirect is `!auth → /login` (line 22); tapping 보호자예요 re-runs a profile read, a role write and a dogs count every launch (56-107)
- `app/src/auth-context.tsx:33` — hydrateRole() reads profiles.role at launch into appSession.role but nothing routes on it
- `app/app/my.tsx:451` — 역할 전환 already exists on 마이 (and settings.tsx:148), so the launch gate is not the only way to switch
- **Impact:** Every session starts with a decision screen the owner already answered; 'direct their attention' says the first frame should be the hero, not a fork.
- **Fix (verifier-corrected):** Smallest honest change, two files, no new predicates, no new reads: reuse `start()` itself. (1) app/src/auth-context.tsx — promote the hydrated role from private module state into the context value: `profileRole: 'owner' | 'runner' | null` and `roleResolved: boolean`, set from the existing hydrateRole read (row → role; no row or error → null, still resolved; keep the `appSession.role` assignment and rules ① fail-open / ② never-overwrite-a-pick exactly as written; add the ~3s cap the 2026-08-20 plan S1(1) specified so a hung read resolves to null → fork). (2) app/app/index.tsx — a module-level `let autoStarted = false;` and one effect: when `!loading && auth && roleResolved && profileRole && !autoStarted && busy == null` → `autoStarted = true; void start(profileRole);`. Nothing else changes: `start()` already does the dogs-count / district first-run gate (:93-111) and falls through to onboarding, fails LOUDLY with the 다시 시도 alert (:31-38), mints the runner row idempotently (:84-91), and `push`es so onboarding keeps its back control (:118-123, onboard/owner.tsx:192, onboard/runner.tsx:91). While it runs, the buttons already show '시작하는 중…' and are disabled (:126-135) — no dead control, no fabricated frame, and a failed or absent profile read paints the enabled fork (fail-open preserved). Both roles are handled symmetrically for free. The once-per-process guard is what closes the POP_TO hazard: 역할 전환 (my.tsx:451, settings.tsx:148, unchanged) lands on index a second time and the fork shows, so the switch path stays live. Optional polish, Sean's call: while `!roleResolved` (≤3s) render only the wordmark instead of the labelled fork, so a returning owner's first painted frame is home rather than a disabled fork; and skip the redundant `update({ role })` when `role === profileR

### owner-journey-14 · low · Chat footer promises retention 「러닝 종료 후 30일간」 but the purge is 30 days from message creation

- `app/app/chat.tsx:630` — 「안전을 위해 모든 대화는 러닝 종료 후 30일간 보관돼요」 under every thread
- `supabase/migrations/0014_comments_crons.sql:63` — purge_old_chat: `delete from chat_messages where created_at < now() - interval '30 days'` — keyed to created_at, not run end
- **Impact:** A message sent two weeks before a run is deleted 16 days after the run; the sentence overstates what is kept for exactly the safety case it cites.
- **Fix (verifier-corrected):** Client-only, one line: in /Users/seankim/dev/daengrun/app/app/chat.tsx (HEAD :773 / b46adb9 :630) replace 「안전을 위해 모든 대화는 러닝 종료 후 30일간 보관돼요」 with 「모든 대화는 보낸 날부터 30일간 보관돼요」. State the anchor the server actually uses (created_at), not merely drop 「러닝 종료 후」 — 「30일간 보관돼요」 alone leaves a reader assuming run end or "from now". 「보관」 reads as a floor, so the sentence is true both if `purge-chat` is registered in production (deleted on the first 04:00 KST sweep after 30 days from send) and if it never was (0177 says nobody has read that cron.job row). Keep the sentence Korean, 15pt, `colors.dim` as now; no server, migration, or legal-doc change is needed because privacy-policy.md:113 is unanchored. Do NOT edit 0014's stale 「(안내 문구와 일치)」 comment (shipped migration). If Sean instead wants the stated behaviour — retention keyed to run end — that is a retention-policy decision for him (awaiting-sean.md §0-sexdecies treats retention rows as statutory), and the slice would be a migration re-keying `purge_old_chat()` via `chat_threads.booking_id` to `bookings.run_ended_at` (0083:164) with an explicit fallback anchor for bookings that never ran (cancelled/expired), plus an unswallowed `purge-chat` registration with a `cron.job` readback pin in the 0177 pattern — not part of this fix.

