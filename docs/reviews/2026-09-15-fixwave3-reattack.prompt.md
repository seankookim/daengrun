You are re-attacking FIX WAVE 3 in daengrun (Expo + Supabase), inside a frozen git export of trunk. Your previous review (docs/reviews/2026-09-15-fixwave2-reattack-verdict.md — read it) returned REJECT/1: R2 WRONG because arms seven and eight of payments_reconciliation() both emitted payment_id = NULL and the group-by invariant (116 C11 / 120 J4) groups NULLs together. SCOPE — only: supabase/migrations/0174_reconciliation_group_key.sql (+ its header's reasoning), supabase/tests/204_reconciliation_group_key_suite.sql, the moved pins in supabase/tests/116_charge_suite.sql (search C11) and supabase/tests/120_g1_ops_cutover_suite.sql (search J4), and supabase/functions/_shared/ops.ts (confirm it reads no column of this query). The fixer added a ninth output column row_key = payment_id::text for arms one–six and 'booking:'||booking_id for arms seven and eight (a SUBJECT identity, deliberately NOT arm-prefixed — they argue an arm-prefixed key would defeat C11's duplicate detection; say whether you agree), had to DROP and recreate the function because a returns-table cannot widen in place (check the ACL is restated in the same file and nothing depended on the old signature), and moved C11/J4 to group by row_key without changing what they assert. Questions: is any consumer broken by the new column or the drop; can two rows for one subject still collide or, conversely, can one payments row now escape duplicate detection; are C11/J4 weakened; does the new suite's K1 actually manufacture the collision before asserting it resolves (a pin whose number is a delta it caused). CLOSED (cite line) · PARTIAL · WRONG. [measured]/[inferred], file:line, under 60 lines. End with exactly two lines: FINDINGS: <n> and VERDICT: <one of APPROVE, APPROVE-WITH-FIXES, REJECT> for 0174 alone.
