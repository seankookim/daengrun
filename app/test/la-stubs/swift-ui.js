// Test stub for '@expo/ui/swift-ui'. The widget's JSX only needs the component IDENTITIES —
// the JSX factory (jsx-shim.js) records them as plain objects, so a string tag is enough.
module.exports = {
  HStack: 'HStack',
  VStack: 'VStack',
  Text: 'Text',
  Image: 'Image',
};
