# check-device-clock.mjs baseline — known device-clock reads. Opened 2026-08-27 with 12 entries,
# immediately after the KST class fix landed nine other sites. ONE is left.
#
# ── 2026-08-27, the same day: the Intl-fallback family is GONE (12 → 1) ────────────────────────
# Eleven of the twelve were ONE thing. Every `Intl.DateTimeFormat` site in this app passed
# `timeZone: 'Asia/Seoul'` in its `try`; those lines were the `catch` — device-local — plus two
# weekday fallbacks that sat INSIDE the try (`wk >= 0 ? wk : d.getDay()`), a third state neither
# branch described: Intl works, honors the zone, and the English weekday token fails to match,
# splicing a device-local weekday onto a KST month/day.
#
# ⚠ THE OPEN QUESTION WAS NOT ANSWERED — IT WAS DELETED, AND THAT IS THE POINT. Whether those
#   branches were live code or unreachable turned on one fact SOURCE CANNOT SETTLE: does this
#   build's Hermes have a `timeZone`-honoring Intl? (package.json has no `expo-localization`, no
#   `@formatjs/*`, no polyfill, and app.json sets no jsEngine, so it rested entirely on whatever
#   Expo SDK 57 / RN 0.86 ships. And a `try/catch` only catches a THROW — a Hermes that HAS
#   Intl.DateTimeFormat but silently IGNORES `timeZone` would take the try, return a device-local
#   label, and never reach the catch at all, which is the state no branch was written for.)
#   `kst.ts` has NO Intl dependency (fixed +9; Korea has no DST), so converging these sites onto it
#   removed the catch branches and the question together. Nothing here waits on that measurement
#   any more, and nobody should re-derive it: it only ever mattered if we kept the dependency.
#
#   The converged sites, and what each now calls:
#     app/club/[id].tsx     ticketParts → kstCal + kstClock · ddayLabel → kstDayIndex (kstYmd gone)
#     app/payments.tsx      linkedLabel → kstYearMonthDay
#     src/components/charge-states.tsx  dayLabel → kstMonthDay
#     src/lib/api.ts        kstParts    → kstDateLabel + kstAmPm (its 28 call sites unchanged)
#   Every rendered string is byte-identical to the old `try` branch — 284,070 comparisons against
#   ICU under four zones (UTC · America/New_York · Asia/Seoul · Pacific/Kiritimati), 0 mismatches.
#   ONE deliberate copy change: payments.tsx's `catch` printed 「2026. 8. 26」 where its `try`
#   printed 「2026년 8월 26일」 — two different sentences for one field. The try's survives.
#
# ⚠ `owner/pay.tsx:90` — CONVERGED AND REMOVED 2026-08-28, so this ledger is now EMPTY.
#   It was the last entry, and it was left here as "the obligation rides whichever slice revives
#   the screen". No slice revived it; the codex #11/#12 slice rewrote the sentences on that same
#   screen instead, and leaving a device-local clock beside a pass rewriting what the payment
#   surface may honestly claim made no sense. `holdLabel` now reads
#   `kstClock(kstCal(Date.parse(exp)))`, which is **format-identical** to the expression it
#   replaced (hour and minute both 2-padded, colon-separated) — only the ZONE changes, and the
#   server that writes the hold expiry is KST-fixed.
#   ⚠ Consequence worth writing down: the `Intl.DateTimeFormat` on that same screen (`whenLabel`)
#   is now the LAST executable one in app/ + src/, and the Hermes question below still hangs on
#   it alone. It is NOT a device-clock read — it passes `timeZone: 'Asia/Seoul'`, so this gate
#   does not and should not flag it — but its `catch` falls back to `toLocaleString('ko-KR')`,
#   which is device-local AND a different sentence from its `try`. The cheap discharge is the
#   same one: converge it, do not measure it. If someone measures anyway, it must run in the app
#   bundle and NOT in Node, on a device set to a non-Seoul zone (a Seoul device cannot fail this
#   check — the same reason the Seoul arm of every mutation in this family shows 0 failures):
#     new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Seoul',hour:'2-digit',hour12:false})
#       .format(new Date('2026-08-25T22:00:00Z'))     // honors zone => "07"; ignores => device hour
#
# Delete a line when its site is fixed — the gate FAILS on a stale entry, so this ledger shrinks
# honestly and can never silently absorb a regression on a line somebody just fixed.
#
# ⚠ THE LEDGER IS EMPTY AND THAT IS NOT THE SAME AS THE GATE BEING OFF. An empty baseline means
#   every future hit is a NEW hit and fails immediately, which is the strongest state this file
#   has ever been in — do not add a placeholder line to "keep the format".
